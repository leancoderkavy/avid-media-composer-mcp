# Native clip Comments

A subsequent isolated record-loading experiment reproduced the same extra metadata track with no comment or column edits: save/reopen control was unchanged, while UI Record loading followed by save/reopen added the filler track. See [viewer qualification](NATIVE_VIEWER_QUALIFICATION.md). This narrows the earlier restoration failure to a reproducible loading/save workflow; it does not explain the separate `Column is not modifiable` rejection or authorize ignoring metadata differences.

**Post-UI restoration limit:** the subsequent explicit clear returned verified empty native readback before and after reopen. Full decoded baseline comparison nevertheless failed: the target sequence had gained one ordinal-3 `DescriptiveMetadata` track containing a 120-frame `FILL`. Independent comparison found this was the only decoded difference after the comment cleared; original source-bin/media hashes remained unchanged. The retained `observed-after-ui.avb` SHA-256 is `6d0937becc427a9970b9dcbc016f8dfcb0ebc8e851560d52f606f5dbeaf8f8ea`. The fixture remains with that additional metadata track and the adjusted bin view; it is not a fully restored baseline. No offline bin overwrite, track deletion or automatic compensating edit was performed. The UI-loading/view-change/save sequence needs a separate controlled test before assigning cause. The strict verifier correctly continues to reject full restoration.

**Populated UI evidence (2026-09-06):** after a deliberate guarded close/reopen and baseline decoded-graph check, a fresh set operation succeeded on the same fixture. Native readback survived another close/reopen. Read-only saved verification found exactly the expected comment change. Computer use resolved `MCP_Comment_70ef2dbe8bb6` through the full tab menu, scrolled/rearranged the bin columns, and observed the complete value `MCP UI review - Sonoma comment visible` under Comments. Double-clicking the row loaded `MCP_PCM_AAF_Selects.Copy.01.Copy.02` in the record timeline; no timeline edit was performed. Bin-view arrangement changed during inspection. Evidence: `.avid-mcp-analysis/native-comment-ui-611ac5bb-08a8-4255-9068-e56eb9236cef/`. This demonstrates populated UI display on this fixture, not a general automatic recovery policy or proof of the initial rejection's cause.

The initial staged verifier incorrectly expected a `.Copy.01` name suffix and stopped after the successful write/save/readback. It now matches the canonical MOB identity and checks uniqueness before mutation. The separate `verify-set` mode completed fresh native and saved verification without replaying the write. The retained `set-started.json` records the deliberate pre-write reopen; the first recovered `set.json` incorrectly recorded that flag as false, which is fixed for future runs without rewriting that historical artifact. `set --reopen` is an explicit fixture experiment, not a shipped automatic workaround. Closing bins can save unrelated unsaved work, so callers must not generalize it to arbitrary user bins.

**Current qualification limit:** a later attempt to populate the previously cleared `MCP_Comment_70ef2dbe8bb6.avb` failed with native error 55, `Column is not modifiable.`, after preview passed. Subsequent read-only inspection still returned Comments as a non-readonly String declaration and the clip value as empty. The saved bin hash remained `2cfc1d91e29110544e45ab6098acf10c53a2d74c758e9b1a1f63c50160ab2fd7`, its decoded mobs matched baseline, and original media/source-bin hashes were unchanged. No compensating write or automatic retry was performed. Earlier successful set/clear evidence remains valid, but repeatability after UI interaction is unresolved; the cause has not been established. A writable declaration is necessary for this adapter, not a guarantee of host acceptance.

The staged `scripts/research/qualify-native-comment-ui.mjs` harness provides `set`, `restore <absolute evidence directory>` and read-only `inspect <absolute evidence directory>` phases for this exact owned fixture. Writes require the expected saved hash and current comment, use preview/apply, and compare saved decoded mobs after reopen. An exclusive phase-start record prevents replay after an uncertain attempt. The observed set attempt failed before a successful set receipt existed, so restore was neither eligible nor invoked. Evidence: `.avid-mcp-analysis/native-comment-ui-9a036cdf-1e6e-41f1-a04e-4b2c34a55bbb/`. The successful two-phase path of this new harness remains unqualified. Regression tests separately cover host rejection and simulated connection loss after mutation, requiring single-use-token refusal and explicit state inspection.

Computer-use inspection on 2026-09-06 opened the cleared disposable comment bin (navigator suffix `70ef2dbe8bb6`) and its **Choose Columns** dialog. The visible alphabetical section contained Color, Color Space, Color Transformation and Creation Date, with no Comments entry between them. The dialog was cancelled without applying a view change. This is a narrow negative observation, not proof that Comments is unavailable elsewhere in the UI. No populated comment was tested visually. Native readback and saved-attribute evidence below therefore remain separate from UI display acceptance; the next UI test must inspect a deliberately populated owned fixture and resolve its exact bin identity.

Saved AVB inspection now captures `_USER.Comments` as mob `comment`: null means the attribute was absent; a string, including an explicit empty string, means it was present. Values and saved snapshot strings are bounded at 65,536 characters; invalid values are rejected. Snapshot capture and mob discovery return `commentStatus` as `absent`, `recorded`, or `not_recorded` for historical snapshots without this field. Comparing old snapshots without comment capture against new captures can therefore report a coverage change; it must not be mistaken for a proven edit.

`avid_diff_saved_snapshots` includes comment changes in decoded mob comparison. Actual MCP capture/reconnect of the retained native baseline/set/clear AVBs through one owned staging path detected exactly one comment-only changed mob for setting and clearing; baseline versus cleared had zero decoded changes with opaque coverage still incomplete. Original retained hashes stayed unchanged. Evidence: `.avid-mcp-analysis/saved-comments-20a5d735-53bb-44be-a86c-47d4d5d3b8f7/evidence.json`; reproducible command: `node scripts/research/qualify-saved-comments.mjs`.

The Windows adapter provides `set_clip_comment` through `avid_native_preview` and `avid_native_apply`. Supply `bin`, `mobId`, `expectedComment` and `comment`. Only the exact Comments field is supported; callers cannot select arbitrary metadata columns. New comments allow at most 1,024 printable ASCII characters; an empty string requests clearing. This avoids extending the observed native Unicode-loss risk to new writes.

Preview and apply both require a declared writable String Comments column and exactly one matching current value from the empty-inclusive clip read. Missing rows are unavailable, never assumed empty. Changed values, missing columns and read-only declarations refuse the operation before writing. Apply additionally requires edit authority, a single-use unexpired token, matching captured state and the native write lock. The write dispatch checks the expected listener owner.

After SetMobInfo, the adapter rereads the scoped clip values and checks both exact value equality and any native reported failures. `commentVerified` reports this readback independently of `persistenceVerified`, which remains false. A false result or transport error requires inspection; no automatic replay or compensating write is performed. This does not establish atomic editor isolation, undo grouping, application-restart persistence or arbitrary field-edit support.

`scripts/research/qualify-native-comment.mjs` creates an owned copy of the fixed Sonoma PCM/color sequence, checks its initial empty Comments value, sets an ASCII comment, saves/reopens and checks it, then clears and repeats. It compares decoded timeline mobs and original source-bin/media hashes, retaining every operation and the before/set/clear AVBs for inspection. It stops on an unverified result instead of blindly restoring or retrying.

For package-level qualification, `node scripts/research/qualify-installed-comments.mjs` creates a fresh archive and managed installation, binds the native harness to the installed entry checksum, and performs that same live write workflow on another new owned copy. It then runs the independent saved-attribute verifier. This command writes to its disposable Avid bin; it is separate from read-only installation diagnostics. It uses the existing Windows editor and checkout Python oracle, so it does not qualify clean-machine dependencies or AI-client approval behavior.

Actual fresh-package execution passed on `MCP_Comment_70ef2dbe8bb6.avb`. Both set and clear survived reopening, decoded mobs matched only the expected comment change, and independent saved-attribute inspection passed. Archive, installed entry, original bin and media hashes were preserved. Evidence: `.avid-mcp-analysis/installed-comments-cfe65622-e3e5-4af7-a366-c57f4c5e9a82/evidence.json`; native operation records: `.avid-mcp-analysis/native-comment-7099fec5-6ea3-426c-ac85-73ceb9061982/evidence.json`.

Actual qualification passed on `MCP_Comment_d097e352ed0f.avb`: both set and clear returned verified native readback, survived bin close/reopen, and retained identical decoded timeline mobs. The original color bin and preview MP4 hashes stayed unchanged. Evidence: `.avid-mcp-analysis/native-comment-d684d7b5-4806-4a62-b15d-31c7e7a6c9df/evidence.json`.

Independent `verify-native-comment-attributes.py` reads of the retained AVBs found `_USER.Comments` absent at baseline, present with the exact test text after setting, and absent again after clearing. Its output is `saved-comment-attributes.json` in that evidence directory. All retained file hashes were unchanged by inspection. This qualifies this host/fixture; it does not prove equivalence of unknown binary fields or atomic undo.
