# Completion ledger

### Native non-UUID batch deletion

Full check passed: 657 TypeScript tests, 46 Python tests, 140 tools, five skills, stdio/HTTP and fresh-package/Python/AAF checks. Log: `.avid-mcp-analysis/check-nonuuid-markers.log`. Installed package schemas match the checkout; live deletion evidence below uses the development server on the qualified Windows host.

Extended deletion input to UUIDs or the observed native UMID spelling after reload. IDs normalize for duplicate/membership checks, but dispatch preserves Avid's current spelling. Existing scoped preview, owner/project checks, exact remaining-record verification and single-use tokens remain unchanged. Creation still requires UUIDs. Tests cover malformed formats, case-only duplicates, exact native spelling and outside-request preservation.

Actual MCP removed two reloaded non-UUID markers, saved/reopened and verified the third unchanged, then separately removed that marker and saved/reopened to an empty list. Retained AVBs independently decoded to exact expected marker survivors with unchanged non-marker MOB fields/warnings. Original sources remained unchanged. Evidence: `.avid-mcp-analysis/native-nonuuid-removal-0b015032-f28d-4806-a8a8-80dd8856cb82/evidence.json`. The owned marker-trim fixture is now marker-empty. This qualifies explicit deletion/persistence for that spelling/build, not atomic undo, arbitrary formats or full-plan completion.

### Reopen invalidates pre-trim native marker UUIDs

Executed verified native close/open on owned `MCP_TrimMarkers_9b4780a2.avb` after the marker-bearing trim/undo. All three native `guid` values changed to the saved non-UUID IDs. Other marker fields, saved bin bytes, decoded MOBs/warnings and original source hashes remained unchanged. Evidence: `.avid-mcp-analysis/marker-trim-reopen-b542f396-6647-48c2-ac7b-76f4f661251a/evidence.json`. The research harness records each response before assertions and retains a hash-checked reopened AVB/decoded graph.

This disproves native UUID continuity across this bin reload and identifies a remaining batch-delete contract gap: its UUID-only input excludes these observed IDs. No marker write or automatic rekeying was attempted. Broader non-UUID edit support and application-restart behavior require further qualification. Research/docs only; prior 655 TypeScript/46 Python full check remains applicable to unchanged production code.

### Marker-bearing trim exposes saved identity changes

A new owned sequence completed observed UI trim/save/undo/save with three picture/audio markers. Clip timing, marker positions and text restored, but every saved marker ID changed on trim and again on undo. Native UUIDs remained unchanged in-session. Original sources were unchanged; the fixture is saved and out of trim mode with three retained markers. Exact undo and cross-session identity continuity are not established. Evidence: `marker-trim-a6781039-2bfd-477f-993a-50a2867b1a1f/verification.json` under `.avid-mcp-analysis`.

The production saved-trim verifier now emits `SAVED_TRIM_MARKER_IDENTITIES_CHANGED` with bounded counts and explicit false verification flags, without private marker text/IDs. It retains exact graph comparison and never remaps identities. Regression tests cover forward/inverse identity changes, unchanged markers and unrelated text changes. Full check passed: 655 TypeScript tests, 46 Python tests, 140 tools, five skills, transports and fresh-package/Python/AAF checks (`check-marker-trim-identities.log`).

A fresh managed installation captured the actual retained AVBs, reconnected through MCP, rejected both directions with the typed diagnostic, and read all three raw non-UUID saved IDs. Archive, entry and input hashes stayed unchanged: `installed-marker-trim-d3046125-fcda-4fb2-a635-23b0445b36ae` / `marker-trim-refusal-2a90fe11-0715-4cd6-8e79-6f582adc45e6`. This uses the existing Windows/Python host and does not qualify unattended UI execution, exact undo, clean-machine onboarding or full-plan completion.

### Independent batch cleanup and decoded baseline restoration

Extended the native removal harness with exclusive hash-checked snapshots at initial 100 markers, after adding/saving one unrelated marker, after deleting/saving the 100, and after separate final cleanup/save. Actual run passed on owned `MCP_Batch_fe73fd13.avb`: `native-batch-removal-830c95f9-e387-4f0e-8b24-14db79244b61`. The fixture is now marker-empty. Original source-bin/media and prior scale evidence hashes were unchanged.

Added `verify-saved-batch-removal.py`. The independent saved-file run verified counts 0/100/101/1/0, every marker's native/saved text/track/declared position, unchanged original raw records after adding the unrelated marker, and unchanged unrelated raw marker after deleting the 100 (excluding only binary object indices). All decoded MOBs, markers, warnings and structural counts in the cleaned snapshot matched the original pre-marker snapshot exactly. Other decoded timeline fields matched at every stage. Raw and decoded checks remain separate from opaque effects and byte equivalence.

The strengthened verifier also binds all five AVBs to capture-manifest checksums and preserves evidence/manifest/input hashes. Reports: `saved-removal-verification.json` and `saved-removal-manifest-verification.json` in the removal evidence directory. A read-only negative probe with a substituted manifest checksum was refused before publication. Actual native cleanup, independent verifier and JavaScript syntax/diff checks passed. Production code is unchanged, so the full suite was not repeated; the preceding 653 TypeScript/46 Python/full-package check remains the latest. Atomic undo, restart, broader edit recovery and full-plan acceptance remain open.

### Stereo saved-marker input mapping and 100-marker capture

Created owned `MCP_Batch_fe73fd13.avb` and applied 100 native markers alternating V1/stereo A1. Native save/reopen verified every requested field; source bin/MP4 hashes remained unchanged. Before/persisted snapshots and events are retained in `native-batch-markers-91656937-1c10-4d04-b644-0c455d3363b5`. The fixture retains its 100 markers for follow-up.

The first saved-marker comparison failed because audio notes lived on channel 2 inside a stereo channel combiner; the decoder correctly returned unresolved positions (`saved-markers-15f18725-e93d-4964-83e0-0aebaee74241`). Factored the existing strict equal-length stereo source recognizer out of the timeline decoder and reused it for marker paths. Both channel paths preserve declared-input uncertainty; unknown modes, reversal, parameters, keyframes, rates, lengths or layouts remain unresolved. New tests cover both channels and refusal variants.

Corrected actual MCP capture/read/reconnect passed across six pages: exactly 100 occurrences/UUIDs, 50 picture and 50 sound, with all text/color/track/frame fields matching native post-save readback (`saved-markers-4de52d08-241a-4f09-87d2-f1fd976806ca`). Independent raw-record/path and decoded-graph comparison matched all declared positions and other timeline fields against the pre-marker snapshot. The generalized harness still passes the two-marker cleanup fixture. No cleanup of this retained scale fixture, complete graph equivalence, arbitrary audio effects or full-plan completion is claimed.

Full `npm run check` passed: 653 TypeScript tests, 46 Python tests, 140 tools, five skills, stdio/HTTP and fresh-package/Python/AAF checks. Log: `.avid-mcp-analysis/check-stereo-saved-markers.log`. The prior unsupported-audio comparison was not a native write failure: all markers persisted successfully and the saved resolver refused an unrecognized component path until support was added.

Fresh managed-package qualification passed the same 100-marker AVBs across reconnect: `installed-saved-markers-22434ebc-a44d-49c7-b6f1-1a2c1da8fa8e` / `saved-markers-797309a4-59ee-46c5-b815-2626e3b1d077`. It preserved all protected snapshots, evidence, archive and entry hashes. This validates the installed package on the existing host, not clean-machine dependencies or named-client GUI onboarding.

### MCP saved marker capture and discovery

Promoted the bounded marker collector/location resolver into packaged Python and added `avid_saved_markers` for scoped, bin-disambiguated, paginated snapshot reads. New captures retain IDs, text, color declarations, raw offsets, paths and explicit direct/effect-input/unresolved locations. Legacy snapshots return `not_recorded`, distinct from zero markers. Validation checks owner, mapped bounds, track, rate and uncertainty; global traversal/marker limits supplement existing capture size/hash safeguards. The inventory is now 140 tools and five skills. Saved diffs include marker records while preserving legacy coverage differences.

Actual retained Sonoma AVB capture/reconnect passed with two markers matching native post-save GUIDs, text, frames 15/75 and picture track 1; before/after-cleanup snapshots returned zero and ambiguous MOB lookup was refused. Evidence: `saved-markers-6f0283d1-bf27-488c-a74c-f347215dc610`. Fresh managed-package capture/reconnect also passed in `installed-saved-markers-91e42d28-b85f-4203-aa78-c0853c5a5b4f` / `saved-markers-1292de87-b976-4e78-980a-3d61d8408d17`. Inputs, archives and entries remained unchanged. This is installed evidence on the existing Windows/Python host, not clean-machine or named-client onboarding.

The first package check caught the missing new Python file in an archive packed before the manifest fix. A later package test caught marker paths inherited by unrelated synthetic MOB fixtures; cloning the marker fixture independently corrected the harness. Failures are retained in `check-saved-markers.log` and `check-saved-markers-package.log`. Broader audio/effect/rate mapping, unsaved editor state, restart/undo and full-plan acceptance remain open.

The final full `npm run check` passed: 653 TypeScript tests, 45 Python tests, 140 tools, five skills, stdio/HTTP checks and fresh-package/Python/AAF checks. Log: `.avid-mcp-analysis/check-saved-markers-final.log`. Installed synthetic marker pagination preserved unresolved coordinates and continued correctly in a second MCP process. No release or full-plan completion is claimed.

Final-code managed-install rerun also passed after tightening the signed component-offset bound: `installed-saved-markers-108f89f1-725a-46c0-aa8c-64c6e6ecf107` / `saved-markers-a809aa01-2b0d-43bb-b7ee-35dd8e7583c2`. Both actual marker records matched retained native readback across reconnect; empty before/cleaned inventories, ambiguity refusal and unchanged protected hashes passed.

### Declared saved marker locations

Extended the read-only marker research inspector to follow bounded same-rate sequence paths, accumulate component positions and apply subclip bounds. Unknown effects, transitions, invalid offsets, mixed rates and inconsistent sequence lengths return unresolved coordinates. Existing recognized equal-length color-adapter input paths retain explicit `declared_effect_input` status rather than claiming verified effect output. The actual retained two-marker AVB snapshot resolves frames 15 and 75 on picture track 1, matching native evidence; report: `.avid-mcp-analysis/native-batch-markers-cc75c541-21e6-44a8-8863-3793d2a19fe4/saved-marker-position-verification.json`. Saved snapshots/evidence hashes remained unchanged.

Three regression tests cover direct positions, frame zero, subclip bounds, malformed paths, transitions, mixed rates, lengths, offsets and opaque effects. The initial actual-file probe exposed pyavb list slicing returning raw object references; materializing the component list fixed the traversal and the retained fixture then passed. This is research infrastructure for saved marker support, not a production decoder or full graph/render proof. General audio/effect/rate mapping and the original full-plan acceptance remain open.

Validation: all 44 Python tests passed. The final verifier additionally compared positions/track labels against the retained post-save native readback, using exclusive copied snapshots at `C:/Users/kavyr/AppData/Local/Temp/avid-marker-position-7asgtqv7`; it passed without overwriting the earlier report. Diff checks passed. TypeScript production code is unchanged, so its prior full-suite result is not represented as a new run.

### Independent saved marker records and before/after snapshots

Extended the live batch harness with exclusive hash-checked before/persisted/cleaned AVB snapshots. Fresh real native run passed in `native-batch-markers-cc75c541-21e6-44a8-8863-3793d2a19fe4`, owned bin `MCP_Batch_ca92028d.avb`. New read-only research inspector/verifier found zero/two/zero TMBC records, matched requested UUID/name/comment/user/color labels and component reference paths, and compared all existing decoded timeline fields across the three saved snapshots. Original source bin and MP4 hashes were unchanged. Retained report: `.avid-mcp-analysis/native-batch-markers-cc75c541-21e6-44a8-8863-3793d2a19fe4/saved-marker-verification.json`.

Both markers (native frames 15 and 75) store component offset 15 on different nested components with zero marker MOB IDs. This changes the next implementation step: resolve component ownership/position before exposing sequence coordinates. General marker decoding, opaque effects, track/rate mapping and complete graph equivalence remain unverified. Production code is unchanged; the actual native round trip, independent Python verification, JavaScript syntax and diff checks passed. The full suite was not repeated for research-only changes. Full-plan acceptance remains open.

### Saved decoded fields after marker cleanup

Added a read-only verifier that snapshots the cleaned 100-marker fixture and unchanged copy source, decodes both, matches the target via the recorded native MOB ID, and compares all decoded composition fields except the expected new composition ID/name. All four other MOB records matched exactly; warnings matched after remapping the composition ID. Source, target, original MP4 and prior removal-evidence hashes remained unchanged. Evidence: `.avid-mcp-analysis/batch-cleanup-graph-6e4630fc-ab64-47b6-aa56-e78c1e422a5a/evidence.json` with retained AVB/JSON snapshots.

The initial strict complete-graph assertion failed because both contain opaque color-adapter components. The final report retains `coverageComplete: false` and reports complete graph equivalence as unverified, rather than clearing warnings or accepting full equivalence. There is no pre-marker graph snapshot, so this compares post-cleanup decoded state to the source rather than proving exact pre/post restoration. Production code is unchanged; actual decoder comparison, syntax and diff checks passed. Full-plan saved-edit fidelity remains open.

### Verified native batch marker removal

Added `delete_markers` preview/apply for 1–100 explicit UUIDs. It normalizes caller IDs, requires exactly one current match per ID, binds the full marker list and dispatches the observed native spellings once under the existing owner/host lock. Post-read verifies exact remaining-record preservation and a stable project before reporting markersRemovedVerified. Missing/duplicate/stale requests are refused; partial deletion and unrelated changes fail verification without replay. Existing native DeleteMarkers allowlisting is reused; inventory remains 15 reads/16 writes and 139 MCP tools.

Actual MCP testing on the retained 100-marker V1/stereo A1 fixture added one preservation marker, removed the original 100 in a single request, saved/reopened and verified only the unchanged preservation marker remained. Separate cleanup and save/reopen then verified an empty marker list. Original source-bin/media and prior scale evidence hashes were preserved: `.avid-mcp-analysis/native-batch-removal-58c97a86-ce53-42f7-b502-893cf2abd953/evidence.json`. The earlier scale fixture is now cleaned of its test markers; whole-bin graph/byte restoration is not claimed.

All 80 focused native tests and full check passed: 651 TypeScript tests, 41 Python tests, 139 tools, five skills, transports and fresh-package/Python/AAF checks (`.avid-mcp-analysis/check-native-batch-removal.log`). Packaged review guidance includes explicit batch removal and separate persistence checks. Atomicity, restart, other host versions and full-plan acceptance remain open.

### Native 100-marker mixed-track qualification

Extended the owned-fixture harness with `--scale`: 100 markers across V1 and stereo A1, including offset zero, are saved/reopened and retained for follow-up. The initial V1/A1/A2 request was refused during preview; direct track readback confirmed one stereo sound track and no A2. No batch was dispatched in that attempt (`native-batch-markers-fa4a3eb5-f0bb-41b1-b55e-19afe1374398`). A unit case separately reproduced omitted frame-zero offset handling; the verifier now uses the native protobuf zero default.

The corrected real MCP run passed application (`markersVerified: true`), all 100 marker fields, save/reopen and original source-bin/media hash preservation: `.avid-mcp-analysis/native-batch-markers-9b534448-7a38-4dd6-b345-5c08f6455e92/evidence.json`. Markers remain in `MCP_Batch_29084e01.avb`; baseline restoration and whole-bin graph equality are not claimed. The packaged review skill now describes batching, UUID normalization, stereo-track discovery and default-field readback. Full check passed with 647 TypeScript tests, 41 Python tests, 139 tools, five skills, transports and fresh-package/Python/AAF checks (`.avid-mcp-analysis/check-batch-frame-zero.log`). This qualifies the supported count on this track layout/build, not every rate, track layout, Unicode or atomic/restart behavior. Full-plan scope remains open.

### Batch UUID identity and post-read project guards

Negative tests reproduced case-only duplicate UUID acceptance and success after an authorized project change during batch readback. Marker UUIDs now normalize to lowercase during preview; collision/readback matching ignores letter case, while prior marker records remain subject to exact preservation checks. A final project check prevents verified success after a switch. Separate coverage confirms that changing an existing marker fails verification and consumes the token. These tests inject host state changes; no live editor project switch or extra marker write was performed for this patch.

All 76 focused native tests and full check passed: 647 TypeScript tests, 41 Python tests, 139 tools, five skills, transports and fresh-package/Python/AAF checks (`.avid-mcp-analysis/check-batch-marker-identity.log`). Pre-fix failures are retained in `batch-identity-before.log`. Earlier actual two-marker persistence/restoration evidence remains separate. This improves operation guards without claiming atomic concurrent-edit protection, larger-batch acceptance or full-plan completion.

### Guarded native batch markers

Added `add_markers` preview/apply for 1–100 caller-identified markers on an existing 30 fps clip/sequence. Preflight checks unique GUIDs, existing markers, duration bounds, track inventory, source membership and printable ASCII text. One AddMarkers dispatch is owner-bound; post-read verifies every requested field, one-frame length, user, prior marker preservation and total count. Partial verification never reports markersVerified, and tokens cannot replay. Native allowlist now contains 15 reads and 16 writes.

The first actual batch created both requested markers but verification failed because Avid omitted the default picture enum. That response remains retained. Comparison now applies the qualified protobuf default. A no-replay fixture recovery verified persisted fields and explicitly deleted those two markers, restoring the original marker list. A fresh corrected-code run then passed preview/apply (`markersVerified: true`), save/reopen, field verification, explicit deletion and marker-list restoration: `.avid-mcp-analysis/native-batch-markers-c8750760-3e8d-4822-8248-a04621e44ba3/evidence.json`. Original source-bin/media hashes remained unchanged in both runs. Initial/recovery evidence: `native-batch-markers-62921df6-59f3-47cb-8ad4-b4bdb287eb02`.

Full check passed: 644 TypeScript tests, 41 Python tests, 139 tools, five skills, transports and fresh-package/Python/AAF checks (`.avid-mcp-analysis/check-native-batch-markers.log`). Tests cover omitted enum defaults, partial readback, replay refusal, duplicate IDs, batch/text bounds, offsets and missing tracks. No atomicity, full-bin graph restoration, 100-marker scale, Unicode, mixed-track or cross-rate host qualification is claimed. See NATIVE_BATCH_MARKERS.md. Full-plan scope remains open.

### Resolve a native MOB to its current project bin

Added `avid_native_read` query `mob_bin` backed by locally derived GetBinFromMob. It requires a MOB ID and accepts exactly one absolute existing AVB path within the current authorized project, with project checks bracketing the call. Malformed/multiple/outside-project/non-AVB results and project changes are refused; unrelated native fields are omitted. No bin is opened and no write occurs. Native allowlist: 15 reads and 15 writes; MCP inventory remains 139 tools.

Actual two-process MCP qualification resolved both known Sonoma disposable sequence IDs to their expected color/load bins in all four lookups, preserving source/bin hashes: `.avid-mcp-analysis/native-mob-bin-6a3b9ad3-5d1e-4f07-960e-9a091bc779d6/evidence.json`. Responses are retained before assertions. Full check passed with 641 TypeScript tests, 41 Python tests, five skills, transports and fresh-package/Python/AAF checks (`.avid-mcp-analysis/check-native-mob-bin.log`). See NATIVE_MOB_BIN.md for usage and boundaries. Saved membership, duplicate identity behavior, closed bins, missing-ID host behavior and Source-viewer alias mapping are not qualified by these two known IDs. Full-plan scope remains open.

### Installed summary review after reconnect

Added `qualify-installed-summary-review.mjs`, which installs a fresh checksum-selected branch archive and starts its MCP entry from a foreign working directory with inspect-only capability and no model configuration. Two independent processes read the overview and both leaves from the existing Sonoma synthetic-note boundary evidence. Reconstructed input text/hash/kind, original sources and exact excerpts matched the prior checkout results on every node and after reconnect. Evidence, summary, transcript, original MP4, archive and installed entry hashes were preserved.

Actual run passed: `.avid-mcp-analysis/installed-summary-review-2f6cd477-c4de-4422-98b6-e255d3a6f83e/evidence.json`; log: `.avid-mcp-analysis/installed-summary-review.log`. Per-pass responses are retained. This establishes installed read/reconnect behavior on the existing Windows/Node host, not model quality, clean-machine dependencies or named-client GUI acceptance. Production code is unchanged; syntax/diff checks passed without repeating the full unit suite. Full-plan scope remains open.

### Inspect transcript and generated-child model inputs

Summary node reads now return recipe-reconstructed model input text, its UTF-8 SHA-256, input kind and ordered child IDs. Leaf inputs come from transcript chunks; overview inputs concatenate generated child summaries. Legacy records without a recipe return null input rather than guessing. This makes inherited omissions/errors reviewable without loading a model or modifying saved records. Unit tests compare every reconstructed input/hash against captured generation calls and verify legacy absence.

Actual cached-model/MCP boundary qualification passed: `.avid-mcp-analysis/summary-boundaries-b91594cc-5065-4612-b691-99491865144b/evidence.json`. Every leaf input matched its independently retained direct-model input; overview input matched ordered child text. Original Sonoma MP4 hash remained unchanged. The source note is synthetic; the generated National Guard fabrication remains visible in a child and the overview input, not accepted as fact. Packaged selects guidance now distinguishes child agreement from original transcript support. Full check passed with 638 TypeScript tests, 41 Python tests, 139 tools, five skills, transports and fresh-package/Python/AAF checks (`.avid-mcp-analysis/check-summary-model-input.log`). This improves review evidence, not automatic entailment/coverage scoring or overall summary-quality acceptance. Full-plan scope remains open.

### Summary repetition and inherited minimum length

Ran a 36-generation comparison of baseline, stronger repetition controls, removed minimum length, and both changes over eight editorial fixtures plus a short observation. Effective model configuration exposed the inherited 56-token minimum; installed runtime source applies it separately from minimum new tokens. Removing it fixed short-input padding but lost important longer-input decisions. Repetition penalties introduced factual errors including a changed delivery time and unsupported report details. No candidate was adopted and no checkpoint recipe changed.

Evidence: `.avid-mcp-analysis/summary-repetition-84ad0dea-c09b-43a9-be16-4e32859c8d47/evidence.json`; initial comparison: `summary-repetition-510b0f78-7c05-42ec-a352-24a09b054c34`. Added reproducible benchmark with effective-setting assertions and incremental output retention. Manual findings are recorded in SUMMARY_QUALITY_QUALIFICATION.md. These are synthetic fixture/model observations, not source-video dialogue or full semantic-quality acceptance. Syntax/diff checks passed; production code unchanged, so the full suite was not rerun. Full-plan quality requirements remain open.

### Refuse new runtime imports while setup is locked

Published-runtime status previously ignored a sibling setup lock, allowing a matching historical receipt to pass inference preflight. Status now keeps tree consistency separate from `setup_lock_present`/`passed: false`, and new runtime imports refuse that state. Internal installer status can recognize only its exact owned lock record; success still passes the existing final lock-ownership check and release. Unit coverage verifies lock preservation, refusal and successful local pinned-component loading after removing the test-owned fixture lock. Existing installer reuse tests pass.

Actual fresh Windows runtime installation, reuse and cached summary inference passed, followed by owned-lock status/refusal and identical inference after removing that fixture lock. Runtime lockfile hashes stayed unchanged and changed-tree refusal still passed. Evidence: `.avid-mcp-analysis/runtime-install-proof-c6c0abb4-e0b8-4b25-8b2a-a23eb324b6d6/evidence.json`. The generated summary remains repetitive/truncated; no quality claim follows from identical output. Full check passed: 638 TypeScript tests, 41 Python tests, 139 tools, five skills, transports and fresh-package/Python/AAF checks (`.avid-mcp-analysis/check-runtime-inference-lock.log`). This does not clear real retained locks, stop already-loaded models, prevent concurrent setup/import races or close general lifecycle recovery. Full-plan acceptance remains open.

### Diagnose unpublished model runtime setup

Runtime status now reports absent, locked or retained setup state for an existing cache without a published runtime. It enumerates at most 512 entries, returns only direct matching staging directories, marks truncated inventory, and never parses unknown lock contents or imports staged code. It explicitly does not establish worker termination or authorize lock removal. Existing published-runtime validation remains unchanged. Tests cover empty cache, retained staging, unreadable-as-content lock diagnostics and bounded enumeration without worker execution.

The actual installer crash harness passed at completed lock and staging-manifest writes before npm started. Live and stopped status reports matched, contenders/restarts remained refused, and lock/staging bytes were preserved. Evidence: `.avid-mcp-analysis/runtime-setup-crash-9b0ed3b5-0241-4727-9148-ad3e5e2e42d0/evidence.json`; actual CLI read: `.avid-mcp-analysis/runtime-setup-status-cli.json`. Full check passed with 638 TypeScript tests, 41 Python tests, 139 tools, five skills, transports and fresh-package/Python/AAF checks (`.avid-mcp-analysis/check-runtime-setup-status.log`). This closes missing-runtime diagnostics, not automatic recovery, orphaned npm termination, rollback/uninstall or clean-machine acceptance. Full-plan scope remains open.

### Preserve the installation-to-recovery harness chain

The preceding lifecycle extension removed the first installation by default, breaking the existing `qualify-package-removal.mjs` consumer. Package deletion is now selected explicitly with `--remove-packages`; the default again retains both installations after configuration rollback/removal. Unknown arguments are rejected before creating fixtures. Local setup documentation describes both paths.

Actual execution of the corrected default followed by the separate removal harness passed on fresh current-branch installations. It verified live-server and added-file refusal, manually staged intact quarantine, refusal while a server ran from that quarantine, recovery after shutdown, successful recovered-server ping, and explicit removal of both packages with the original archive preserved. Evidence: `.avid-mcp-analysis/package lifecycle ccc08299-3833-475d-b6ca-bbfed88e2ad1/evidence.json` and `removal-evidence.json`; logs: `package-recovery-chain-install.log` and `package-recovery-chain-removal.log`. This simulates the intact pre-deletion interruption state; it is not a forced process crash, partial-deletion repair or power-loss proof. Production code is unchanged. Syntax/diff checks passed; prior 2d00361 CI/CodeQL passed. Full-plan acceptance remains open.

### Installed package removal after configuration rollback

Extended `qualify-package-install.mjs` beyond its prior two-install activation/rollback/configuration-removal coverage. Actual Windows execution refused package removal while its stdio server remained live, then successfully pinged that same server. After closing the client, explicit removal succeeded and filesystem inspection confirmed the owned installation directory absent. The second installation's complete tree remained unchanged; client configuration and source archive hashes were preserved. Evidence: `.avid-mcp-analysis/package lifecycle 83cbd96f-6fe4-4106-9af4-0dc2ea1fae9a/evidence.json`; log: `.avid-mcp-analysis/package-lifecycle-removal.log`.

The harness retains the live-refusal response before assertions and uses the existing receipt-checked removal CLI outside the target installation. It creates only isolated fixture installations. Production code is unchanged; this real installation/lifecycle run passed rather than repeating the full unit suite. This does not close named-client GUI onboarding, clean-machine dependencies, optional-model removal, cross-version data compatibility or Mac removal. The full plan remains incomplete.

### Malformed native diagnostic filtering

Four negative cases reproduced unknown-field disclosure when oversized or malformed structured diagnostics fell back to raw text. The fallback now replaces malformed encoded or structured content with a bounded generic diagnostic while preserving ordinary plain-text errors and known fields from valid objects. The cases failed before the fix and passed afterward; 78 focused native tests passed. Installed-package smoke additionally checks known type-55 serialization and malformed/oversized field omission.

Full local check completed with 636 TypeScript tests, 41 Python tests, 139 tools, five skills, both transports and fresh-package/Python/AAF checks: `.avid-mcp-analysis/check-native-rpc-diagnostic-filter.log`. An actual read-only native GetBinColumnInfo probe still returned type 9 for a unique nonexistent bin, created no bin and preserved source-bin/media hashes: `.avid-mcp-analysis/native-rpc-error-4112c9d2-815b-4a05-b710-5a132c77f8fe/observation.json`. This probe covers NativeClient and common error serialization, not a live MCP tool dispatch. No failed write was replayed. Prior head 7663f26 CI and CodeQL passed; remote checks for this fix remain separate. Full-plan acceptance remains open.

### Observed Source navigation and offset-format correction

Computer use navigated the disposable loaded Source sequence from frame 0 to 60 and 119, with read-only MCP verification of exact viewer/MOB/frame after each completed move. An attempted -119 return reached frame 70, exposing interpretation as 1 second 19 frames rather than 119 frames. The explicit -210 correction returned to frame zero. All successful checkpoint bin/source-bin/media hashes matched, and the failed response remains retained. See NATIVE_UI_SEEK_QUALIFICATION.md for exact inputs, evidence directories, source-counter differences and scope.

Added a read-only position observer that preserves responses before asserting expected frames, and updated packaged selects guidance to require fresh native position readback and avoid raw-integer offset assumptions. No native seek or unattended UI executor was added. Fresh-package/Python/AAF checks passed with 139 tools and five matching skills (`.avid-mcp-analysis/package-source-navigation.log`); syntax and diff checks passed. Production code is unchanged and the full unit suite was not rerun. Prior 807a6b7 CI/CodeQL passed. Broader navigation and full-plan acceptance remain open.

### Structured native RPC rejection diagnostics

Full `npm run check` passed: 632 TypeScript tests, 41 Python tests, 139 tools, five skills, both transports and fresh-package/Python/AAF checks (`.avid-mcp-analysis/check-native-rpc-errors.log`). Remote checks for this change remain separate; the full implementation goal is not complete.

Native response rejection now preserves `NATIVE_RPC_REJECTED` through MCP error serialization instead of classifying it as an internal connector error. Known numeric native error types and bounded HTTP/gRPC diagnostics are retained; unknown fields in parsed structured diagnostics are omitted. `operationOutcome: not_verified` and `nextStep: inspect_state_before_retrying_write` do not authorize replay or certify no mutation. Seven tests cover the observed column error, bounds, malformed/plain/Unicode diagnostics and unsupported structured values.

A read-only actual native GetBinColumnInfo probe for a uniquely nonexistent bin returned type 9 through the new error path. It created no bin and preserved original source-bin/media hashes. Evidence: `.avid-mcp-analysis/native-rpc-error-e2a93c97-7e9a-48ad-aeaa-ac8250313dca/observation.json`; harness: `scripts/research/qualify-native-rpc-error.mjs`. This verifies native transport/error formatting without repeating a failed metadata write. Generic transport and other application-level failures retain their existing handling.

### Native Record request failed exact viewer verification

Final full `npm run check` passed after removing the experimental option: 625 TypeScript tests, 41 Python tests, 139 tools, five packaged skills, transports and fresh-package/Python/AAF checks (`.avid-mcp-analysis/check-native-viewer-boundaries.log`). The earlier 627-test run covered the temporary extension and is not evidence of a qualified Record feature. Production adapter code is unchanged from the preceding commit; tests and user guidance now explicitly retain the observed boundary.

Investigated the installed descriptor for frame navigation. It declares no seek method or position argument on LoadMobsIntoViewer. A temporary adapter extension encoded an explicit Record request for the owned load fixture through preview/apply. Avid completed the request but returned the target only in Source, so exact viewer verification failed. Independent request encode/decode preserved Record. The experimental public option was removed rather than exposing known incorrect behavior. Original fixture/source-bin/media hashes stayed unchanged at inspection; no automatic retry or save followed.

Evidence: `.avid-mcp-analysis/native-record-viewer-a7c7f56c-41b5-4be2-9e9b-007e945b71c5/observation.json`. The read-only `verify-native-record-viewer-refusal.mjs` verifies the retained request/response and qualified schema encoding without replaying the write. Added assertions for the Source request body and refusal of unsupported viewer/position arguments; packaged selects guidance keeps Record loading on the observed UI path. Prior 583142b CI and CodeQL passed. Full-plan Record execution/seek support remains open.

### Isolated record-loading metadata change

Created a fresh owned PCM/color copy with a staged record-load qualification harness. Initial and extra close/reopen control graphs matched exactly. Computer use confirmed the bin through the full tab menu and loaded its only sequence into Record without comment/column edits. Native viewer readback confirmed its exact MOB. The loaded-but-unsaved file still matched control; after close/reopen, the only decoded change was one 120-frame DescriptiveMetadata filler track. Original source-bin/media and server-entry hashes remained unchanged. Evidence: `.avid-mcp-analysis/record-load-0c802313-caf2-4f5a-9be6-18ae92a5ddcb/`; detailed scope and hashes in NATIVE_VIEWER_QUALIFICATION.md. The additional track is retained as a difference, not accepted as baseline restoration.

Updated the packaged selects workflow to inspect loading changes before establishing the immediate pre-edit saved baseline, preserve the earlier snapshot and avoid silently discarding metadata tracks. Added a regression requiring an otherwise exact trim with an additional descriptive-metadata track to fail. All 18 trim-verifier tests and typecheck passed. Fresh-package/Python/AAF checks passed with 139 tools and five matching skills (`.avid-mcp-analysis/package-record-load-workflow.log`). Full unit suite was not rerun; this does not add a shipped record-loading executor or resolve the separate Comments write rejection.

### Comments visible after reload; post-UI graph restoration failed

Read-only saved inspection found a visible Comments column already present, and native GetBinInfo reported the fixture open/unlocked. A deliberate guarded close/reopen preserved the decoded baseline; a fresh comment set then succeeded and survived another reopen. The staged verifier exposed its own incorrect hardcoded copied-name suffix after writing. Replaced name matching with canonical MOB identity, checked target uniqueness before mutation, and added read-only verify-set recovery rather than replay. Actual recovery verified only the expected saved comment change. Fixture experiment: `.avid-mcp-analysis/native-comment-ui-611ac5bb-08a8-4255-9068-e56eb9236cef/`.

Computer use resolved the exact bin through its full tab menu and observed the complete test text under Comments after rearranging/scrolling columns. Double-clicking the row loaded the fixture in the timeline. Subsequent explicit native clear/readback/reopen succeeded, but full saved-graph restoration failed on one added 120-frame DescriptiveMetadata filler track. Independent inspection confirmed this was the only remaining decoded difference and original media/source-bin hashes were unchanged. The saved candidate and failure are retained; the verifier was not weakened. Fixture baseline restoration and the cause of the additional track remain unresolved. Harness syntax/diff checks passed; no production code changed. Earlier ff174a2 CI was still running at this turn's inspection.

### Populated Comments attempt exposed a host rejection

Added a staged exact-fixture UI qualification harness with exclusive write-phase claims, expected saved hashes/current values, independent saved-graph comparison and read-only inspection. Actual set was rejected with native error 55, `Column is not modifiable.`, although preview and the writable String declaration passed. No replay or restoration was attempted. Read-only inspection returned an empty comment, unchanged saved bytes/decoded baseline and unchanged original source-bin/media hashes. Evidence: `.avid-mcp-analysis/native-comment-ui-9a036cdf-1e6e-41f1-a04e-4b2c34a55bbb/`. The populated UI path and successful new-harness restore path remain unqualified; the error's cause remains unresolved.

Two regression cases cover native rejection and simulated transport loss after mutation. Both require the apply token to remain consumed, exactly one dispatch, and explicit readback of the actual value. All 66 native tests passed; harness syntax and diff checks passed. No production code changed and the full suite was not rerun for this research/test checkpoint.

### UI comment-column inspection and current status reconciliation

Computer use reopened the disposable comment fixture and inspected Choose Columns. The observed alphabetical Color-to-Creation Date section did not include Comments. Cancel closed the dialog without applying changes; no comment edit or save was invoked. Populated-value UI acceptance remains open, as detailed in NATIVE_CLIP_COMMENTS.md. The timeline remained Copy.05 at 01:00:02:00. This observation does not replace native or saved-bin verification.

Reconciled IMPLEMENTATION_STATUS.md with the latest full local check (621 TypeScript, 41 Python, 139 tools, five skills), historical client/package counts and already-integrated provider accepted-connection checks. Remote CI and CodeQL for e0ec2a8 are now verified successful. This documentation-only checkpoint does not add production functionality or claim full-plan completion.

### Installed read regression and explicit doctor paths

Full local check passed: 621 TypeScript tests, 41 Python tests, 139 tools, five skills, transports and fresh-package/Python/AAF checks (`.avid-mcp-analysis/check-doctor-path-overrides.log`). Previous 92379f1 CI/CodeQL passed; current-change remote checks remain pending. Broader native editing, clean-machine setup and full-plan requirements remain open.

Fresh managed-package app/project/bin/clip/track/viewer reads, saved timeline checks and bin/clip-column reconnect passed after the owner lookup change: `.avid-mcp-analysis/installed-native-e2459fac-98f2-4dec-a41e-e98bfb2ad655/evidence.json`. This broadens read regression evidence without claiming native writes or new-host qualification.

Setup review found --doctor ignored accepted root/output/native path flags. Added doctor configuration merging that honors explicit absolute path overrides while preserving unspecified environment settings and rejecting relative/path-list roots. Updated CLI help/local setup. Actual CLI execution returned exactly the specified owned root/output and successfully read the explicitly configured Avid executable (`.avid-mcp-analysis/doctor-explicit-paths.json`). All four focused doctor tests passed, including environment preservation and invalid overrides. The doctor remains read-only; no dependency installation or client configuration mutation was introduced.

### Faster native owner lookup with fresh verification retained

Full local check passed: 620 TypeScript tests, 41 Python tests, 139 tools, five skills, transports and fresh-package/Python/AAF checks (`.avid-mcp-analysis/check-native-owner-cim.log`). A direct call against the actual Avid listener with Node as the expected executable also rejected with NATIVE_OWNER_MISMATCH. Current-change remote verification remains pending.

Read-only measurements found owner lookup dominated native latency. The installed Windows NetTCPIP CDXML maps Get-NetTCPConnection to ROOT/StandardCimv2/MSFT_NetTCPConnection and Listen to State 2. Switched the native guard to a direct filtered Get-CimInstance query of that provider, preserving fresh per-RPC owner lookup, executable path resolution, PID/start identity and schema binary hashing. No ownership caching or omitted guard was introduced.

Five-sample comparisons matched original/direct owner results exactly. Observed median full GetAppInfo time fell from 1,571 ms before to 941 ms after; saved fixture hashes stayed unchanged. Evidence and limitations: NATIVE_READ_PERFORMANCE.md, especially `.avid-mcp-analysis/native-read-timing-a2931e8c-000a-4cb3-93f8-bc5ffbe4162c/evidence.json`. All 69 focused native/owner tests passed, covering fresh calls and absent/ambiguous/foreign/failed owner refusal. This is one-host latency evidence; broad performance, concurrency and the full implementation plan remain open.

### Packaged review guidance for Comments

Updated the original avid-review-markers workflow to cover requested whole-clip Comments alongside timestamped markers. It uses the new column/value reads, requires exact expectedComment and writable declarations, preserves user text rather than silently converting unsupported input, and distinguishes native readback from historical saved evidence. It explains not_recorded/absent/recorded comment status and keeps unrelated bins outside persistence checks.

Fresh `npm run smoke:package -- --with-python` passed with 139 tools, five matching installed workflow files, tool-reference discovery and Python/AAF checks (`.avid-mcp-analysis/check-review-comments-skill.log`). No production code changed and the full unit suite was not rerun. This verifies packaged guidance, not an additional model-driven write, broad client acceptance or full goal completion.

### Fresh installed-package native comment workflow

The native comment harness accepts an absolute installed entry and expected checksum, records that identity and rechecks it after execution. Added a separate installed-comments qualifier that packs, creates a managed installation, runs live set/clear on a new owned copy, and independently reads retained AVB attributes. The ordinary installed-native read qualifier remains read-only.

Actual execution passed on MCP_Comment_70ef2dbe8bb6.avb: set and clear each survived save/reopen; decoded mobs matched only the expected comment field change; independent baseline/set/clear attribute reads passed. Package/archive, installed entry, original bin and original media hashes were unchanged. Evidence: `.avid-mcp-analysis/installed-comments-cfe65622-e3e5-4af7-a366-c57f4c5e9a82/evidence.json`; native evidence: `native-comment-7099fec5-6ea3-426c-ac85-73ceb9061982` under the analysis root.

Both script syntax checks and actual execution passed. No production code changed; the full unit suite was not rerun. Prior 42e023a CI and CodeQL passed. This qualifies fresh-package writes on the existing licensed Windows host, not clean-machine dependencies, model approval flows, atomic undo or broad native editing. Full goal remains open.

### Saved Comments capture, discovery and semantic diffs

Full local `npm run check` passed: 615 TypeScript tests, Python checks, 139 tools, five matching skills, transports and fresh-package/Python/AAF checks (`.avid-mcp-analysis/check-saved-comments.log`). Native harness syntax also passed after updating its expected graph. Prior bb897c3 CodeQL passed while CI remained running at the last check. Current remote checks and broader plan qualification remain open.

The AVB parser now reads only the known _USER.Comments field with bounded string validation, preserving absence as null and explicit empty strings as text. Snapshot validation retains this optional field for backward compatibility. Capture and paginated mob discovery distinguish absent, recorded and historical not_recorded status. Full mob diffs now detect comment-only changes; unknown historical comment coverage is not equivalent to a known absence.

Actual MCP staged the retained native baseline/set/clear AVBs under one owned path, captured each, reconnected, and paged mob discovery. Set and clear each produced exactly one comment-only changed mob; baseline-to-cleared produced zero decoded changes while retaining incomplete effect coverage. Original retained inputs stayed unchanged. Evidence: `.avid-mcp-analysis/saved-comments-20a5d735-53bb-44be-a86c-47d4d5d3b8f7/evidence.json`. Updated the native research harness to expect the newly captured comment field rather than claiming it is excluded from decoded mobs. No new native mutation was performed this turn. Focused validation passed: 14 Python timeline tests and 28 TypeScript snapshot tests.

### Guarded native Comments set and clear

Added `set_clip_comment` with exact expectedComment, bounded printable-ASCII new text (including empty clearing), writable String column checks, empty-inclusive value reads, existing edit/token/state/lock gates, expected listener-owner dispatch and explicit commentVerified readback. Native failure reports or differing values stay unverified; no automatic retry/restoration is attempted. Tests cover stale values, missing/read-only columns, authority, reported/unapplied writes, clearing and consumed tokens.

Actual MCP created a separate owned copy in MCP_Comment_d097e352ed0f.avb, set a comment, saved/reopened, cleared, and saved/reopened again. Each native value matched; decoded timeline mobs and original source-bin/media hashes stayed unchanged. Independent pyavb inspection found _USER.Comments absent/present-with-exact-text/absent across retained baseline/set/clear bins. Evidence: `.avid-mcp-analysis/native-comment-d684d7b5-4806-4a62-b15d-31c7e7a6c9df/evidence.json` and `saved-comment-attributes.json`. See NATIVE_CLIP_COMMENTS.md.

Full local check passed: 614 TypeScript tests, 40 Python tests, 139 tools, five skills, transports and fresh-package/Python/AAF checks (`.avid-mcp-analysis/check-native-comments.log`). Prior 8eba71f CI/CodeQL passed; current-change remote checks remain pending. Unicode, arbitrary metadata fields, atomic undo, full application restart and remaining plan requirements stay open.

### Explicit empty native clip-column reads

Full local `npm run check` passed: 608 TypeScript tests, Python checks, 139 tools, five matching skills, transports and fresh-package/Python/AAF checks. Log: `.avid-mcp-analysis/check-native-clip-columns.log`. Current-change remote verification remains pending.

Investigating safe comment edits showed ordinary GetMobInfo omitted Comments even though the bin declares it. The locally extracted request contract exposes includes_empty_columns and only_visible_columns; an explicit empty-inclusive read returned 179 rows including an empty Comments value. Added scoped `clip_columns` to `avid_native_read` with bounded name/value output, duplicate refusal, protobuf empty-string defaults only for returned rows, and post-read membership/project/path checks. Ordinary clip reads are unchanged. This establishes a needed read precondition; comment writes and clearing remain unqualified.

All 58 native tests passed. Actual MCP results matched direct native values, a second MCP process returned identical data, and the saved bin hash stayed unchanged: `.avid-mcp-analysis/native-bin-columns-3ea0731d-7fde-4787-83bb-2abfa3dbc97d/evidence.json`. See NATIVE_BIN_COLUMNS.md. Prior 065a770 CI/CodeQL passed; c1bfbc1 CodeQL passed while CI remained running at this check. Broader full-plan requirements remain open.

### Actual AI interpretation of saved effect counts

Added and executed `qualify-codex-effect-report.mjs` against the preceding Sonoma saved-color qualification. Codex made exactly one `avid_saved_sequence_complexity` call; its payload matched an independent ProjectSnapshots read. The structured answer correctly reported two EFF2_LUTSFX nodes, incomplete coverage, unknown render cost and unknown enabled state. The explanation distinguished saved declarations from verified behavior. Source bin, server/client executable and input evidence hashes were unchanged. Evidence: `.avid-mcp-analysis/codex-effect-report-95a54bd7-37df-4e05-b46b-91a41666953d/evidence.json`.

Script syntax and actual execution passed. No production code changed and the full unit suite was not rerun. Prior ff11c88 CI/CodeQL passed; 065a770 CodeQL passed with CI still running at this check. This verifies one actual model read, not broad report interpretation, installation from scratch or native editing. Full goal remains open.

### Saved direct effect declaration counts

Full local `npm run check` passed: 604 TypeScript tests, Python checks, 139 tools, five skills, transports and fresh-package/Python/AAF checks (`.avid-mcp-analysis/check-effect-declaration-counts.log`). Current-change remote verification remains pending; prior ff11c88 CodeQL passed while platform CI was still running at the check.

Extended `avid_saved_sequence_complexity` with bounded counts by saved direct TKFX ID, distinct/identified/unidentified totals and explicit truncation above 100 lexically sorted IDs. Stored opacity and incomplete coverage remain; no render-cost, enabled-state or recursive-effect interpretation is inferred. Snapshot tests cover repeated IDs, unknown IDs, non-effect nodes, truncation and empty inventories. All 27 focused snapshot tests passed. Actual Sonoma capture/reconnect returned exactly two EFF2_LUTSFX nodes with unchanged source bin hash: `.avid-mcp-analysis/saved-color-effects-a149cb34-f75d-4c24-a101-df23f59c0608/evidence.json`. This advances effect reporting; unqualified stereo-combiner trim and broader full-plan requirements remain open.

### Installed effect-input usage pagination

Extended fresh-tarball acceptance with two synthetic saved color-effect mobs. Default installed usage omits effect inputs; opt-in one-result pages preserve opaque nodes, nested input offsets and incomplete status. A second MCP client continues the first client's cursor to the other mob without changing saved snapshot bytes. Existing keyframe-diff and malformed-declaration checks still pass. Actual `npm run smoke:package -- --with-python` succeeded with 139 tools, five matching skills and Python/AAF checks; log `.avid-mcp-analysis/check-installed-effect-usage.log`. Syntax checks passed. No production code changed and the full unit suite was not rerun. Synthetic installed-record acceptance is separate from the preceding real Sonoma saved-bin evidence.

Updated IMPLEMENTATION_STATUS.md to reflect native column discovery, declared effect inputs and the combined exact-PCM/improved-color export. Previous 61fcb4f CI passed; 7dac340 CodeQL passed while its platform matrix was still running at this check. Full plan remains incomplete.

### Opt-in saved color-input usage discovery

Full local `npm run check` passed: 603 TypeScript tests, Python checks, 139 tools, five matching skills, transports and fresh-package/Python/AAF acceptance. Log: `.avid-mcp-analysis/check-effect-input-usage.log`. Remote checks for this increment remain separate.

`avid_saved_source_usage` accepts `includeEffectInputs` (default false). Opt-in results include supported saved equal-length color-adapter inputs without fabricating top-level direct source ranges. Matching nodes retain TKFX/opaque declarations and nested input references, carry `effectInputOnly`, and keep completion false across pages. The option must stay fixed with the revision/source ID during pagination. Unsupported effect kinds and rate/length mismatches are omitted.

Actual PCM/color Sonoma capture/reconnect and one-result-page usage calls found both declared input starts, 2850 and 3300, while preserving the retained bin hash. Evidence: `.avid-mcp-analysis/saved-color-effects-1b8951fb-e8ff-4008-ad96-b255d898fa0b/evidence.json`. Focused snapshot tests passed (26), including opt-in/default behavior, preserved opacity, empty continuation and mismatched inputs. See COLOR_ADAPTER_INPUT_TRACE.md. This adds saved source discovery, not rendered source correspondence, general effects, live unsaved graphs or full-plan completion.

### Installed native bin-column reconnect qualification

Extended the native column harness to bind execution to an installed entry checksum and compare a second MCP process with the first and direct native reads. Fresh managed-package qualification now includes this check alongside existing project/bin/track/viewer reads and independent saved timeline verification. Actual execution passed: 179 declarations matched, both server entry and bin hashes stayed unchanged, and package archive identity was preserved. Evidence: `.avid-mcp-analysis/installed-native-7e590d97-ebfe-402b-a94c-ee39ac9dfe00/evidence.json`; column evidence: `native-bin-columns-85d2bb18-9e3f-48b4-ab91-6487f5b301df` under the same analysis root. Both script syntax checks passed. No production code changed; the full unit suite was not rerun. This qualifies installed read/reconnect behavior on the existing host, not clean-machine prerequisites, named GUI clients, writes or general editor coverage.

At this check a2cb853's macOS Node 20/24, landing and CodeQL passed; Windows CI was still running. The broader full-plan requirements remain open.

### Cross-platform bin-column assertion repair

The edc063b Windows Node 20/24 jobs, landing and CodeQL passed, but both macOS jobs failed because a new non-disclosure assertion searched the entire result for `private`, which also occurs in macOS's legitimate `/private` temporary paths. The test now checks exact column-array equality and absence of the injected `unrequested` property. This still rejects leaked extra column fields without rejecting an authorized bin path. All 54 focused native tests passed locally; production code is unchanged. Remote verification of this correction remains pending, and the full-plan gaps below remain open.

### Live native bin-column discovery

Added `bin_columns` to `avid_native_read`, using the locally derived GetBinColumnInfo contract. The native allowlist now has 14 reads and 15 writes. Inspection is bin/project scoped, rechecks project/path after the call, bounds total columns/string lengths, rejects duplicate names and malformed flags, preserves whitespace names and strips unrelated fields. Protobuf defaults are materialized. Avid's writable flag is not connector write authorization or proof of arbitrary metadata editing support.

Actual inspect-only MCP returned 179 columns from the disposable PCM/color bin, with Comments as its single custom column. Results matched direct native calls before and after; the saved bin hash stayed unchanged. Evidence: `.avid-mcp-analysis/native-bin-columns-90a1ccb2-9907-48df-9d6d-8f46b6e7189c/evidence.json`; see NATIVE_BIN_COLUMNS.md. Full local check passed: 602 TypeScript tests, 40 Python tests, 139 tools, five matching skills, transports and fresh-package/Python checks (`.avid-mcp-analysis/check-native-bin-columns.log`). Previous 4b3f839 CI passed. This adds live metadata discovery, not arbitrary field edits, custom-column creation, native timeline manipulation or full-plan completion.

### Installed color snapshot and workflow acceptance

Extended fresh-tarball package checks with synthetic saved effect records. The installed MCP server preserves automatic-conversion/LUT declarations, parameter/keyframe fingerprints and opacity; a second client reads the identical range, traces the declared input with explicit incomplete status, detects a same-name keyframe change and rejects an incomplete automatic-conversion record. The authorized source fixture remains byte-identical. This checks installed record handling, not new native editing, media decoding or Python capture evidence.

Package checks now also compare all five installed workflow skill files with checkout content after newline normalization, in addition to discovery metadata/tool-reference validation. Actual `npm run smoke:package -- --with-python` passed with 139 tools, five matching skills, exact tool definitions, both Python isolation/recovery and AAF checks. Log: `.avid-mcp-analysis/package-color-snapshot-recovery.log`. Script syntax passed; no production code changed and the full unit suite was not rerun. Current 6060c8f CodeQL passed while CI remained in progress at the check; prior full-plan gaps remain open.

### Combined native audio/color qualification

Verified frame timing on the refreshed older AAC sequence; all 120 best matches align with source presentation times, but duplicate output audio channels remain. Created and saved a separate copy of the prepared-PCM sequence, applied UI Color Adapters refresh to that copy, and exported through guarded MCP. The same output now has exact source-clock 24-bit stereo PCM with distinct channels, all 120 technically verified frames, mean full-raster SSIM 0.958711 and maximum diagnostic presentation-time residual 0.334 microseconds. Original bin/media hashes and non-picture track structure stayed unchanged. Combined evidence: `.avid-mcp-analysis/native-color-fixture-3bfb1d55-f1d4-4675-936c-bd2ab3cf8694/combined-evidence.json`. See NATIVE_RENDER_QUALIFICATION.md for the source-bin baseline recheck and all comparison artifacts.

Added recognition/validation of the observed automatic-conversion XML wrapper, preserving its flag/list name as declarations. Actual MCP capture/reconnect/input tracing passed on the new saved bin (`saved-color-effects-c73b53bc-9a3f-44e2-a5f9-79f4d759dacb`). Updated the export workflow guidance to distinguish stereo metadata, actual PCM preservation and color refresh evidence. Full local check passed with 596 TypeScript tests, 40 Python tests, 139 tools, five skills, transports and fresh-package checks (`.avid-mcp-analysis/check-automatic-color-declarations.log`); the 25-test snapshot suite also passed the added automatic-field consistency assertions. Prior ba5fa0c CI/CodeQL passed. General native refresh automation, color residual/perceptual fidelity, other source formats and all remaining full-plan requirements stay open.

### Diagnostic color-adapter input tracing

Added bounded recognition of the saved color adapter's single equal-length picture input. It requires the supported LUT declaration, non-reversed/zero mode/scalar flags, one input track, one same-rate/full-length source clip and optional zero-length fillers. Saved nodes remain opaque with a separate `effect.inputReference`; trace steps explicitly mark `effectInputOnly` and the whole trace remains incomplete. This follows declared input chains without asserting rendered output correspondence. See COLOR_ADAPTER_INPUT_TRACE.md.

Actual MCP capture/reconnect of the refreshed Sonoma bin returned both original input identities and [2850,2910)/[3300,3360) ranges. Tracing composition [30,90) returned [2880,2910) and [3300,3330), retained incomplete status and preserved the bin hash. Evidence: `.avid-mcp-analysis/saved-color-effects-61e132e4-2905-49ce-bd55-4fb7204fa85c/evidence.json`. Tests reject rate/length mismatches, reverse/mode/scalar flags, nested effects, sound inputs, extra clips and nonzero fillers.

Full local check passed: 596 TypeScript tests, 40 Python tests, 139 tools, five skills, transports and fresh-package/Python checks (`.avid-mcp-analysis/check-color-input-trace.log`). Previous 11d7b4a CI and CodeQL passed. General effect interpretation, native refresh/edit automation, rendered source correspondence and remaining full-plan requirements are still open.

### Same-name effect keyframe change detection

Added versioned bounded fingerprints of supported saved parameter/keyframe declarations to opaque TKFX nodes and snapshot validation/diffs. This catches changes beyond readable LUT labels without flattening effects or asserting rendered equivalence. See SAVED_EFFECT_FINGERPRINTS.md for supported classes, resource bounds and the inclusion of saved parameter UI fields. Unit tests cover deterministic hashes, byte changes, unsupported/cyclic/oversized values and saved same-name effect diffs with incomplete coverage retained.

Actual refreshed-bin MCP capture/reconnect preserved both fingerprints and source bytes (`saved-color-effects-99434052-78f3-4424-89e2-66ab8d720440`). A disposable offline copy with a persisted and independently reread one-unit keyframe-level change produced exactly one changed sequence; only its first effect keyframe fingerprint changed (`effect-keyframe-diff-640efdb0-8fdc-4e1e-bb73-0b34962991cd`). Both evidence folders are under `.avid-mcp-analysis`. Earlier harness attempts did not persist the nested mutation; retaining and marking its parent before saving corrected the fixture. Original retained source stayed unchanged.

Full local `npm run check` passed: 595 TypeScript tests, 39 Python tests, 139 tools, five skills, transports and fresh-package/Python checks (`.avid-mcp-analysis/check-effect-fingerprints.log`). Previous bd502ec CI and CodeQL passed. Current-change remote verification and all broader native editing/color/render/source-mapping requirements remain open.

### Saved linear-LUT declarations

The refreshed fixture's CFUserParam payload declares the same full-to-video-level scaling label observed in Avid UI: 10-bit black 64, white 940 and an Inverted element. Added bounded recognition of this single-LinearLut XML shape under the observed parameter/value UUIDs. Snapshot range reads preserve the optional declaration; effects remain opaque and the flag is not interpreted as applied transform direction. Unknown/disabled/duplicate/controlled parameters and malformed/oversized/DTD XML are not interpreted. Unit tests cover recorded declarations, invalid values, disabled/duplicate parameters, XML rejection and saved-schema bounds.

Actual MCP capture/reconnect returned both declarations with the retained bin hash unchanged: `.avid-mcp-analysis/saved-color-effects-14fd3cac-acd4-4f59-b67f-a8e54bd12864/evidence.json`. An initial invocation against stale compiled TypeScript correctly rejected the new Python field; rerunning after the normal build passed. Full local `npm run check` passed: 594 TypeScript tests, 38 Python tests, 139 tools, five skills, transports and fresh-package/Python checks (`.avid-mcp-analysis/check-linear-lut-declarations.log`). Previous head a9bc437 passed CI and CodeQL. General source mapping through effects, actual LUT math, native refresh automation and broader fidelity remain open.

### Saved color-effect declarations through MCP

Inspected retained before/after AVB objects: the refreshed picture clips are wrapped by two `EFF2_LUTSFX` effects with PRLS parameters and FXPS keyframes; nested source IDs/track/starts match the original direct clips. Added bounded effect identifier and parameter/keyframe-presence declarations to opaque TKFX nodes and snapshot validation. This improves saved range diagnostics without flattening effects or claiming parameter interpretation/source mapping. Python tests cover roundtrip/bounds and continued opacity; TypeScript tests cover saved range retention, rejected oversized IDs and unsupported source tracing. Actual MCP snapshot capture followed by a new connection returned both effects from the refreshed saved bin, with unchanged hash (`saved-color-effects-8ceef0b0-72dd-4153-bb1b-70988919439f` under `.avid-mcp-analysis`).

Full local check passed: 594 TypeScript tests, 37 Python tests, 139 tools, five skills, transports and fresh-package checks; log `.avid-mcp-analysis/check-effect-declarations.log`. The preceding 8ed214b CI passed all four Windows/macOS Node 20/24 jobs, landing and CodeQL. Current-change remote checks remain separate. General color interpretation, native refresh automation, source mapping through effects and full-plan completion remain open.

### Isolated color-adapter refresh and CI fixture repair

The owned Copy.05 duplicate in `MCP_Color_ca6d9cb31bcc.avb` passed saved baseline sequence comparison, then received UI Refresh Sequence > Color Adapters. Native close/reopen saved only that bin and native export passed 120-frame H.264/1080p30/Rec.709 legal-range/stereo PCM checks. Full-raster source comparison improved mean SSIM from 0.784061 to 0.958711 (minimum 0.901226, RGB RMSE 4.429720, aggregate PSNR 35.203279 dB). The saved graph now reports opaque TKFX picture nodes; full source/effect mapping remains unqualified. Original Copy.05 saved-bin and source MP4 hashes stayed unchanged. Evidence: `native-color-fixture-dccc9bf2-5f8a-46ff-9768-3ec701e901e0` and `full-resolution-dc5135ec-151a-4eee-9fab-d7fdf6048cba` under `.avid-mcp-analysis`. The first verification attempt was refused before mutation because its harness omitted edit authority; its journal is retained separately. Research scripts now prevent replay when verification artifacts exist. No automatic production color correction was added.

Remote head 9caf258 failed CI: macOS matched the ordinary `/private` temp-path component in an overly broad non-disclosure assertion; Windows package discovery rejected a synthetic receipt output path that had not been canonicalized. Tests now assert omission of exact damaged-content and other-source markers and record the fixture output under the canonical directory. Full local `npm run check` passed: 593 TypeScript tests, 36 Python tests, 139 tools, five skills, both transports and fresh-package/Python checks. Log: `.avid-mcp-analysis/check-ci-preparation-fixtures.log`. Remote matrix verification for the fix remains required.

### Live native color-settings investigation

Computer use inspected the current Copy.05 export preset and linked source settings. The preset remains Keep as Legal Range/Rec.709/H.264 8-bit with stereo 48 kHz/24-bit PCM. Source Color Encoding already declares Rec.709 full range and a full-to-video-level scaling adapter with bypass unchecked. All dialogs were canceled without Apply/OK, preset save or export. A bin-tab asterisk appeared after source-settings inspection; unsaved application state is not asserted unchanged. Saved Copy.05 and original MP4 hashes still match retained baselines. See NATIVE_RENDER_QUALIFICATION.md for exact observations. This points the next isolated experiment toward imported-sequence source-transformation propagation/refresh, rather than assuming missing source classification. Color fidelity remains unresolved; no production code or tests changed.

### Explicit MCP recovery after active preparation crash

Extended the active-FFmpeg owner-crash harness through a new MCP connection. Discovery returned the interrupted attempt, status remained unresolved, and an explicit retry created a separate verified output. Normal production video/PCM/timestamp checks passed; independent complete audio/video decode succeeded and FFprobe counted 150 expected frames. New status matched receipt/output checksums. Original source and all interrupted-directory files stayed unchanged. Evidence: `.avid-mcp-analysis/preparation-worker-crash-ba72c0dd-573c-489d-9291-0b68b6ec7a12/evidence.json`. Syntax and actual execution passed; no production code changed and the full suite was not rerun. This qualifies explicit retry after observed worker exit on the bounded fixture; automatic retry, general orphan containment and cleanup remain open.

### Active FFmpeg preparation owner-crash evidence

Added and executed `qualify-preparation-worker-crash.mjs`: actual production preparation with injected FFmpeg input pacing on an owned five-second fixture. Observed the spawned worker alive and more than 64 KiB written, killed its exact owner, and confirmed owner closure. The worker was absent at the first subsequent observation; the 262,180-byte retained MOV failed independent FFprobe with missing moov metadata. Success/failure receipts were absent, status reported unresolved/unknown worker state, and source bytes were preserved. Evidence: `.avid-mcp-analysis/preparation-worker-crash-b0e21a1f-20bf-4618-9c7b-b9bf89d44e75/evidence.json`. Syntax/actual execution passed; no production code changed and the full suite was not rerun. Cause of worker exit, arbitrary descendant containment, long-media behavior and automatic cleanup remain open; this is not a general parent-crash recovery guarantee.

### Complete-file preparation receipt publication

Full local check passed: 593 TypeScript tests, 36 Python tests, 139 tools, five skills, both transports and fresh-package recovery checks (`.avid-mcp-analysis/check-source-clock-publication.log`).

Production preparation now writes and closes an exclusively owned temporary receipt before hard-link publication at receipt.json; existing receipts are never replaced. Normal cleanup only removes the temporary file created by this call. Twelve focused tests/build passed, including competing publishers and unchanged pre-existing receipt. Actual production crash barriers after attempt creation, partial staged receipt write and complete staging all left the final receipt absent; MCP discovery/status/retry passed with source and interrupted files unchanged. Evidence: `.avid-mcp-analysis/source-clock-crash-a9635021-567c-41bf-abc9-d2a11971bcd5/evidence.json`. Unsupported hard-link filesystems fail publication. This addresses interrupted final JSON visibility; mid-worker crashes, cleanup, receipt authenticity and power-loss durability remain open.

### Installed preparation reconnect acceptance

Fresh-tarball package checks now exercise source-clock discovery/status through installed MCP with synthetic saved records. A second client continues the first client's damaged-record cursor, finds the valid attempt, reads unresolved state, matches receipt/output bytes and rejects changed output. Source and attempt records stay unchanged; no FFmpeg/conversion evidence is inferred from this fixture. Actual `npm run smoke:package -- --with-python` passed with 139 tools, five skills, exact checkout definitions, client configuration, Python isolation and AAF checks. Log: `.avid-mcp-analysis/check-installed-preparation-recovery.log`. Script syntax passed; no production code changed and the full unit suite was not rerun. Real conversion/interruption evidence remains separately recorded.

### Reconnect discovery for source-clock attempts

Full local check passed: 592 TypeScript tests, 36 Python tests, 139 tools, five skills, both transports and fresh-package checks (`.avid-mcp-analysis/check-source-clock-discovery.log`). At the preceding remote head 94a43a9, CodeQL, macOS and landing checks had passed; Windows jobs were still running. These remain separate from current local evidence.

Added `avid_list_source_clock_attempts` with scoped checksum-selected source, bounded pages, UUID continuation and unattributed unreadable-record counts. Other-source records are omitted; empty pages retain continuation. Returned UUIDs support existing status inspection without implying completed output or worker state. Eleven focused tests/build passed, including damaged-page recovery, other-source omission and denied/changed sources. Actual MCP one-record pagination found interrupted and completed retries while retaining source/artifact hashes: `.avid-mcp-analysis/source-clock-crash-df5d0e5a-d211-4343-9c85-de070d544753/evidence.json`. Automatic retry, cleanup and worker containment remain open.

### Source-clock preparation status through MCP

Full local check passed: 591 TypeScript tests, 36 Python tests, 138 tools, five skills, both transports and fresh-package definition/client setup checks (`.avid-mcp-analysis/check-source-clock-status.log`). Inventory assertions and smoke gates were updated for the new tool; all remaining scope stays open.

Added `avid_source_clock_status` for explicit saved run UUIDs. Bounded record reads check scoped source checksum, attempt/receipt identity and completed output checksum; conflicting outcomes and changed files fail. Results distinguish unresolved, failure_recorded and receipt_matches_files, always with unknown worker state and no implied essence revalidation or receipt authenticity. Ten focused preparation tests/build passed, including changed/unauthorized sources, changed outputs, conflicts and identity mismatch. Actual interrupted/retried production preparation status calls passed in `.avid-mcp-analysis/source-clock-crash-a9172092-6b09-4a87-9279-9c24a2393050/evidence.json`, preserving source and interrupted artifacts. Discovery, worker monitoring, automatic recovery/cleanup and crash containment remain open.

### Source-clock preparation interruption and fresh retry

Added and ran `qualify-source-clock-crash.mjs` against production preparation using an owned two-second H.264/stereo-AAC fixture. Killed the exact child at instrumented barriers after attempt publication and before success-receipt publication, with no active subprocess. Both left no success/failure receipt. New MCP server invocations recomputed verified outputs in distinct directories; saved receipts matched returned payloads, output checksums passed, and source/interrupted-artifact hashes stayed unchanged. Evidence: `.avid-mcp-analysis/source-clock-crash-e7d2f0fe-991d-4a7f-9886-fbdc6ab969b2/evidence.json`. Syntax and actual execution passed; no production code changed and the full suite was not rerun. Corrected documentation to distinguish caught failure records from abrupt-exit artifacts. Discovery/status, mid-worker crash containment, interrupted receipt writes, cleanup and power-loss durability remain open.

### Gapped video and H.264 B-picture timing qualification

Expanded `qualify-video-timing.mjs` from four to eight actual MCP/save-read cases, covering regular/duplicate/gapped FFV1 and independently confirmed H.264 B pictures at zero/nonzero range starts. Expected statistics derive from FFprobe decoded frame PTS and each stream's recorded rational time base. All cases passed and source hashes stayed unchanged. The gapped fixture had a 266-tick maximum step at 1/1000; B-picture media had constant 512-tick steps at 1/15360 without backward presentation steps. Evidence: `.avid-mcp-analysis/video-timing-d67650a4-0839-40ed-b338-829dc884896c/evidence.json`. Initial gap generation failed because a filter-expression comma needed quoting; corrected generation and all assertions passed. Build and syntax checks passed; production code was unchanged and the full suite was not rerun. This extends discontinuity/codec coverage without claiming missing-frame counts, perceptual sync, general malformed-stream acceptance or full completion.

### Explicit decoded video timestamp accounting

Full local check passed: 588 TypeScript tests, 36 Python tests, 137 tools, five skills, both transports and fresh-package checks (`.avid-mcp-analysis/check-video-timing.log`). Entire Sonoma PTS accounting also matched independent FFprobe and saved readback: 5,725 frames, 1/15360 time base, constant 512-tick steps, zero duplicate/backward steps; evidence `sonoma-full-qc-2838dbfb-5a3c-4fc6-8ef7-278f4d77ec3c` under `.avid-mcp-analysis`, original hash unchanged.

QC now records decoded video time base, first/last PTS, duplicate/backward step counts and delta extrema. It refuses incomplete/reordered/unsafe observations and disagreement with processed frame counts. Persisted reads validate timing/stream/count consistency while preserving historical records. Four real regular/duplicate FFV1 cases at zero/nonzero starts matched independent FFprobe PTS and saved readback with source hashes unchanged. Evidence: `.avid-mcp-analysis/video-timing-f7d91050-afac-4294-aed0-772867788642/evidence.json`; see QC_VIDEO_TIMING.md. This does not infer frame durations, missing frames or perceptual sync.

### Full Sonoma preview QC after endpoint validation

Actual QC covered the original preview's entire declared [0,190.866666) range. Its 5,725 processed video frames matched an independent full ffprobe decode; saved findings and audio timing matched readback through current validation, and the original SHA-256 stayed unchanged. Black/freeze/silence interval arrays were empty; this is not a delivery or perceptual verdict. Evidence: `.avid-mcp-analysis/sonoma-full-qc-910afe88-e723-4fc5-82e9-299f024fd155/evidence.json`; script: `qualify-sonoma-full-qc.mjs`. The original/prepared [60,90) regression also passed independent frame/PCM/timestamp checks and full findings readback: `sonoma-qc-amount-a3c20ec0-d220-451e-a9ac-d7fd19819c49`. Syntax and actual execution passed; production code did not change. At this check e92bdae had passed CodeQL, macOS CI and landing; Windows jobs remained running.

### Validate saved QC event collections

Fresh-package checks passed with 137 tools, five skills and Python/AAF isolation (`.avid-mcp-analysis/check-qc-interval-validation-package.log`). Validation for this increment used the affected report suite, build, real report workflows and package gate; the full unit suite was not rerun.

Saved black/freeze/silence events now reject malformed/oversized collections, invalid numeric intervals, out-of-range endpoints and findings without the corresponding selected stream. Valid open events and historical freeze/silence range-closed records remain readable. Build and all 12 affected report tests passed; actual short-stream and trailing-black report readback also passed with source hashes preserved. See QC_OPEN_INTERVALS.md. This validates stored structure and bounds, not authenticity, perceptual correctness or delivery acceptance.

### Actual AI interpretation of unknown QC endpoints

Codex read the saved short-video QC report through avid_read_qc_report and correctly returned unknown freeze end/duration, 30 processed video frames and four requested seconds. Its explanation rejected a four-second-freeze inference. The harness checked the exact MCP payload against a direct source-verified report read, final structured values, absence of other tool types and unchanged server/report evidence. Actual execution and syntax passed; evidence: `.avid-mcp-analysis/codex-qc-read-c7502fe6-6bd2-4df5-b93f-b6bc8867b764/evidence.json`. See QC_OPEN_INTERVALS.md. No production code changed; broad model/report acceptance remains open.

### Stop inventing open freeze/silence endpoints

Full local check passed: 581 TypeScript tests, 36 Python tests, 137 tools, five skills, both transports and fresh-package checks (`.avid-mcp-analysis/check-open-qc-intervals.log`). Actual short-stream cases also passed after stored-report validation was added: `.avid-mcp-analysis/qc-open-streams-e8290802-7c1e-4366-96b1-9b1392e874b5/evidence.json`.

Reproduced a false four-second freeze from one second of static video in a four-second container. Unclosed freeze/silence findings now return end:null and openAtProcessingEnd:true, rather than assigning the requested end. Closed findings are unchanged. Stored validation checks the new representation while leaving historical reports readable. Actual short-video/short-audio cases and six closed offset cases passed with unchanged sources; see QC_OPEN_INTERVALS.md for evidence and client handling. This corrects event extent without claiming perceptual or delivery certification.

### Preserve black detections open at processing end

Full local check passed: 579 TypeScript tests, 36 Python tests, 137 tools, five skills, both transports and fresh-package checks (`.avid-mcp-analysis/check-black-tail.log`). Repeated real tests with stored-field validation passed: `qc-black-tail-9b48cda7-21dd-4bb7-b002-e15161e67152` and `qc-black-depth-fa1dcea0-89d8-4aa3-bcdc-8bc4c678e1d8` under `.avid-mcp-analysis`.

Reproduced a missed 0.5-second trailing-black interval and last-frame endpoint truncation for longer tails. QC now captures blackdetect transition metadata and returns blackOpenAtProcessingEnd separately, with no inferred end or minimum-duration claim. Saved reads validate its range, uncertainty and selected-video consistency while preserving older reports. Actual four-tail MCP/save-read cases and eight depth/range cases passed, sources unchanged. Evidence: `.avid-mcp-analysis/qc-black-tail-5fdfc91f-e8a3-4f56-80fd-c2bfdf5ee6a9/evidence.json`; see QC_BLACK_DEPTH.md. General threshold/perceptual and delivery qualification remain open.

### Black QC across depth and range

Eight real MCP cases passed for generated 8-bit/10-bit FFV1, full/limited range, each over [0,4) and [0.5,3.5). Raw decoding independently confirmed intended integer luma and frame amounts. QC matched exact source-clock black intervals, 120/90 decoded frames and source pixel/range declarations; persisted reads matched and fixture hashes stayed unchanged. Evidence: `.avid-mcp-analysis/qc-black-depth-270780c2-6df9-40ac-9760-f4ad5bccd412/evidence.json`; see QC_BLACK_DEPTH.md. Syntax and actual script execution passed. No production code changed; near-black thresholds, HDR perceptual behavior and camera/delivery qualification remain open.

### Discoverable color-declaration facets

Fresh-package checks passed with 137 tools, five skills, Codex command validation and Python/AAF isolation (`.avid-mcp-analysis/check-color-facets-package.log`). The complete unit suite was not rerun for this increment; the affected library suite, build, actual media workflow and installed-package gate were run.

Media facet results now include pixelFormat, colorRange, colorSpace, colorTransfer and colorPrimaries counts, each deduplicated per matching file. Only bounded string declarations from video streams contribute; absent values are omitted. The count description explicitly states that different facet values may come from different streams, so clients must use a combined filter to require co-occurrence. Build and all 14 library tests passed. Actual synthetic-tag Sonoma discovery/filtering returned expected SDR/PQ/HLG counts with original and derivative hashes unchanged: `.avid-mcp-analysis/color-filters-3b32d59d-d65b-4815-93bd-88554674dcd8/evidence.json`. This improves metadata discovery without claiming HDR fidelity.

### Color-declaration media filtering

Full local check passed: 576 TypeScript tests, 36 Python tests, 137 tools, five skills, both transports and fresh-package checks including Codex argv (`.avid-mcp-analysis/check-color-filters.log`).

avid_media_facets video filters now accept pixelFormat, colorRange, colorSpace, colorTransfer and colorPrimaries. All constraints must match one recorded video stream; absent declarations do not satisfy an explicit unknown query, and bit depth alone never implies HDR. Unit tests cover normalization, cross-stream false positives, missing values and invalid constraints. Actual MCP indexing/filtering on three one-second Sonoma derivatives tagged SDR/PQ/HLG matched each expected file and rejected conflicting transfer/primaries, with original/derivative hashes unchanged. Evidence: `.avid-mcp-analysis/color-filters-ec0baec1-f77c-4f0d-9788-91a7c58cea20/evidence.json`. Initial fixture generation retained input transfer/primaries despite encoder flags; explicit setparams frame properties corrected the fixture. These are synthetic declarations, not HDR conversion/mastering or visual fidelity proof.

### Model-driven native edit approval boundary

Attempted an authorized temporary rename/restoration of owned Copy.05 through Codex. Native preview succeeded, but Codex refused avid_native_apply with "MCP tool call requires approval, but approval policy is never". The model did not replay the token and read the exact original name afterward. Direct before/after clip metadata, saved-bin hash and original MP4 hash stayed equal. The ordinary edit acceptance failed; a separate explicit --expect-approval-refusal run passed the exact refusal/no-change assertions: `.avid-mcp-analysis/codex-native-rename-407dbc3e-08ee-4d8f-a1df-3fbd1a724578/evidence.json`. Initial failed edit evidence: `codex-native-rename-2ba43262-92f7-4be4-b75b-55cc6c0c57b2`. Script: `qualify-codex-native-rename.mjs`. No approval controls were relaxed. Model-driven editing still requires a client workflow that can obtain approval; this is not an editing or persistence pass.

### Codex model-selected native Avid inspection

Actual authenticated Codex called avid_native_read once each for app, project and open_bins against the existing Media Composer 2024.12.58720 Sonoma session. All three successful structured payloads matched direct NativeAdapter reads before and after; all 23 open-bin SHA-256 values and the server entry hash remained unchanged. The final answer reported the build, project and all returned bin filenames. No other completed tool types appeared. Evidence: `.avid-mcp-analysis/codex-native-read-14f00355-56ad-4aca-8ff4-434b1d40fd9f/evidence.json` and event stream; reproducible `qualify-codex-native-read.mjs`. Syntax and real execution passed. This advances model-driven live native inspection, not writes, atomic session snapshots, playback, fresh-machine installation or GUI qualification. No production code changed.

### Installed Codex setup regression gate

The fresh-package smoke check now decodes the installed Codex argument array, rejects malformed/duplicate environment assignments, and compares its server command, entry and environment exactly with all five JSON client formats. It then connects using that decoded command from a foreign working directory. Installed CLI install/update/remove/restore attempts against an owned Codex TOML fixture are refused and preserve every byte. Actual fresh-tarball execution passed with 137 tools, five skills and Python/AAF package checks: `.avid-mcp-analysis/check-installed-codex-argv-protection.log`. This gate requires no authenticated client or Codex binary; actual Codex/model executions remain separately evidenced. No production code changed.

### Supported Codex setup command generation

Full local check passed: 574 TypeScript tests, 36 Python tests, 137 tools, five skills, both transports and fresh-package checks (`.avid-mcp-analysis/check-codex-setup.log`). The existing package check covers five JSON formats; the separate real Codex CLI probe covers the new argument-array format.

The normal CLI now accepts --client codex and emits a shell-free codex mcp add argument array with validated roots, runtime options and server entry. Codex owns TOML writes; JSON configuration mutation is explicitly unsupported for this mode. A regression verifies exact special-character paths/environment arguments. Actual installed Codex add/get/remove passed in a new isolated configuration directory: `.avid-mcp-analysis/codex-setup-8716bd00-3e09-4f0d-b8a2-9bdb83eb2449/evidence.json`. This closes command generation, not connector-managed Codex update/rollback or clean-machine setup.

### Fresh installed Codex model workflow

Packed the current development server, installed the archive through the managed package CLI into a new isolated directory, and supplied the resolved installed entry plus SHA-256 to the real Codex model harness. Health and hashed file inventory calls passed with exact final-answer values, no other completed tool types, unchanged fixture/client/server bytes, and unchanged archive hash. Evidence: `.avid-mcp-analysis/installed-codex-model-0215ccd0-c9a1-41bb-b621-dfa3eb2a967f/evidence.json`, linked model events under `codex-model-d9f316de-401a-4353-aab8-b93d3b9c370a`. Reproduce with `qualify-installed-codex-model.mjs`. This closes fresh-package model-driven basic read acceptance on the existing Windows host, not clean-machine prerequisites, GUI or native edits. Syntax and actual installation/model execution passed; production code did not change.

### Actual Codex model-selected MCP calls

Installed Codex CLI 0.153.4 reported ChatGPT authentication and completed an ephemeral read-only model run with per-invocation MCP configuration. The model called avid_ping and avid_inventory_project_files with hashes, then returned the unique synthetic filename and exact independently calculated SHA-256. The harness asserts successful structured results, exact arguments, allowed server/tool names, final-answer values, no other completed tool types and unchanged fixture bytes. Evidence: `.avid-mcp-analysis/codex-model-decfa8d3-bb9e-4b20-86cc-c47ff328f576/evidence.json` and retained event stream; script: `qualify-codex-model.mjs`. Syntax and two actual model runs passed. This closes the basic model-driven named-client read workflow only; fresh installation, GUI and native Avid calls remain separate. Claude 2.1.260 remains signed out. No production code changed.

### Retain installer locks when subprocess closure is uncertain

Full local check passed: 573 TypeScript tests, 36 Python tests, 137 tools, five skills, both transports and fresh-package checks (`.avid-mcp-analysis/check-runtime-uncertain-lock.log`). Real pre-npm lock/staging crash tests also passed against the new build: `.avid-mcp-analysis/runtime-setup-crash-9af9d1e7-faa8-4dfc-9f0d-73f27ed0662b/evidence.json`.

Install/audit/import exceptions without successful tree-termination evidence now retain the cache lock and explain the recovery boundary. Errors preserve their cause. Tests cover each stage, missing/failed evidence, contender refusal, and release after no-start or successful-termination outcomes. This addresses lock release after reported termination uncertainty; abrupt-owner containment and normal-exit descendants remain open. See MODEL_RUNTIME_QUALIFICATION.md.

### Runner-owner crash and detached writers

Actual owner-SIGKILL research found a detached Node worker continued heartbeat writes after the production runner's owner closed; a direct worker did not. Both workers stopped cooperatively and PID absence was verified. See PROCESS_TREE_QUALIFICATION.md and `qualify-process-owner-crash.mjs`. This demonstrates that dead installer PID alone cannot authorize stale-lock/staging recovery. Timeout tree termination remains distinct from abrupt-owner containment; automatic recovery and actual npm-child qualification remain open. No production code changed.

### Installer owner interruption

Actual production installer child-process tests passed at completed lock and staging-manifest writes. A contender was refused while the owner remained live; after exact-child SIGKILL and close, restart was also refused with lock bytes and staged manifest preserved and no runtime published. Evidence: `.avid-mcp-analysis/runtime-setup-crash-6c34cb1d-0695-47dc-b112-4369cfd45364/evidence.json`; reproducible script: `qualify-runtime-setup-crash.mjs`. Syntax and real execution passed. This establishes pre-npm crash behavior, not automatic recovery, orphaned npm handling or power-loss durability. See MODEL_RUNTIME_QUALIFICATION.md. Production code did not change.

### Exact summary source spans

Full local check passed: 565 TypeScript tests, 36 Python tests, 137 tools, five skills, both transports and fresh-package checks (`.avid-mcp-analysis/check-summary-source-spans.log`). The preceding pushed head f875960 passed all CI and CodeQL checks; these local changes require their own remote checks after push.

New summary records retain their chunk recipe; node reads reconstruct and return exact UTF-16 transcript spans for descendant leaf inputs, with leaf IDs and source indices. Full segment reads stay compatible. Historical records without recipes explicitly return not_recorded; inconsistent leaf provenance is refused. Existing recipe-one/two checkpoint hashing remains unchanged. Actual cached-model MCP generation/readback passed exact text and span assertions with the original MP4 unchanged: `.avid-mcp-analysis/summary-boundaries-e330bc6d-4ebe-4a39-83cb-fdb832bfb30b/evidence.json`. The model still produced the known unsupported National Guard claim; this improves review provenance and does not close factual quality. See SUMMARY_QUALITY_QUALIFICATION.md.

### Extractive summary quality counterexamples

Evaluated an original source-offset sentence-selection candidate on eight existing synthetic editorial cases plus a cancellation/reinstatement case. Every selected quote matched its exact source span, but sentence budgeting omitted a responsible person and filming prohibition, and deduplication removed a later reinstatement. Raw omitted occurrences are retained so distinct-text coverage cannot conceal temporal information loss. Candidate remains research-only; no production summary backend changed. Committed evidence: `docs/extractive-summary-benchmark.json`; local run: `.avid-mcp-analysis/extractive-summary-ba7ae7da-2d54-48ad-839e-0a64855fd215/evidence.json`. Syntax and actual execution passed. Generated and extractive factual/context acceptance both remain open; see SUMMARY_QUALITY_QUALIFICATION.md.

### Receipt temporary-file ownership

Full local check passed: 563 TypeScript tests, 36 Python tests, 137 tools, five skills, both transports and fresh-package checks (`.avid-mcp-analysis/check-runtime-temp-ownership.log`). Previous production commit 02b4a0c passed CI and CodeQL.

Fixed receipt cleanup after failed exclusive creation: the publisher now opens the temporary file with wx before entering its cleanup region, preserving pre-existing content if creation fails. A deliberate collision test verifies this alongside partial-write and concurrent-publisher cases. Actual child-process interruption at partial-write and post-link barriers passed again with the handle-based publisher: `.avid-mcp-analysis/runtime-receipt-crash-85d46614-d5a6-4d82-a4e8-9172a37d848c/evidence.json`. The fixture dependency tree and abandoned temporaries stayed unchanged. This does not qualify arbitrary external directory replacement or full installer/power-loss recovery.

### Receipt publisher process interruption

Executed actual child-process termination at instrumented partial-write and post-link boundaries in the production receipt publisher. The parent observed each barrier before killing the exact child and awaited its close event. Partial-write interruption left no final receipt and allowed a later successful publication; post-link interruption retained a complete receipt and refused replacement. Abandoned temporary files and the synthetic dependency tree were preserved. Evidence: `.avid-mcp-analysis/runtime-receipt-crash-aeb4f70c-53e5-44b8-9ed3-ea416dad18e8/evidence.json`; reproducible script: `qualify-runtime-receipt-crash.mjs`. Syntax and real execution passed. Full installer crashes, stale-lock recovery and power-loss durability remain open; no production code changed.

### Atomic optional-runtime receipts

Full local check passed: 562 TypeScript tests, 36 Python tests, 137 tools, five skills, transports and fresh-package checks (`.avid-mcp-analysis/check-runtime-receipt.log`).

Model runtime setup now publishes validated complete receipt bytes through an exclusive hard link from a unique cache-root temporary file. Injected partial-write and concurrent-publisher tests prove no partial final receipt and no overwrite. Temporaries left by a process crash remain outside the dependency tree. Actual disposable-copy audit/adoption/status/import passed with an abandoned partial temporary receipt preserved and the original tree unchanged: `.avid-mcp-analysis/runtime-receipt-2027927f-b197-45ee-997c-ec41aa202b44/evidence.json`. See MODEL_RUNTIME_QUALIFICATION.md. Power-loss durability, stale setup-lock recovery and update/rollback/removal remain open.

### Audio timing stream/offset qualification

Expanded and ran the real MCP multistream fixture: audio timing matches selected stream/sample totals and survives saved readback; video-only reports null audio timing. A generated two-track PCM source with a 250 ms delay only on track two produced the expected 192,000 versus 180,000 samples over [0,4), including a 12,000-tick delayed start; both tracks produced 132,000 samples over [1.25,4). An empty delayed-track range failed with no report files published. Both input hashes stayed unchanged. Evidence: `.avid-mcp-analysis/qc-streams-1ee0a6b6-0e14-424a-b79d-8553ed9df39e/evidence.json`. Syntax and actual script execution passed; production code did not change. Broader codec/clock and playback qualification remains open.

### Audio QC timestamp accounting

Audio QC now records bounded integer frame timestamp accounting before loudness normalization, distinguishing sample amounts from adjacent-frame gaps/overlaps. Missing/reordered/unsafe observations and sample-total inconsistencies fail; persisted reads validate accounting and stream metadata while preserving legacy reports. Actual original/prepared Sonoma [60,90) MCP QC matched independently clipped FFprobe timestamps and PCM sample counts, then matched saved-report retrieval. Original: 320 gap ticks, 3,776 overlap ticks, six discontinuities; prepared: none. Source hashes unchanged. Evidence: `.avid-mcp-analysis/sonoma-qc-amount-c0ad7c43-495b-48d6-afe1-e4c5f39efc6d/evidence.json`; see QC_AUDIO_TIMING.md. Full local check passed: 560 TypeScript tests, 36 Python tests, 137 tools, five skills, transports and fresh package (`check-audio-timing.log`). Union coverage, perceptual sync and physical trim handles remain unverified.

### Saved trim coordinate-origin checks

Full local check passed: 555 TypeScript tests, 36 Python tests, 137 tools, five skills, both transports and fresh-package checks (`.avid-mcp-analysis/check-trim-origin.log`).

The saved trim verifier now checks declared bound/duration consistency and refuses explicitly nonzero composition or immediate source origins. Previously, duration and track coverage could accept a synthetic exact edit without qualifying the source coordinate origin. Three regression cases cover composition/source refusal, zero-origin acceptance and inconsistent bounds. Actual forward/inverse/backward MCP checks passed on retained zero-origin Avid captures with unchanged input hashes: `.avid-mcp-analysis/saved-trim-mcp-ea81f1aa-61a0-4490-bcdc-b398d0abd118/evidence.json`. Nonzero-origin mapping and physical-media handles remain open; see NATIVE_UI_TRIM_QUALIFICATION.md.

### Sonoma descriptor versus decoded media timing

Executed a read-only full MP4 comparison using a separately specified authorized file, exact known locator-declaration checks and before/after hashes. Actual video decode counted 5,725 frames versus Avid's 5,726. Audio decoded 9,192,704 samples versus declared 9,164,224; integer presentation accounting found 960 gap ticks and 32,640 overlap ticks across 49 discontinuities, yielding a 9,161,024-sample presentation span (3,200 below the descriptor). Evidence: `.avid-mcp-analysis/sonoma-descriptor-media-4294b24e-7de0-4b12-9806-26928ebfedd6/evidence.json`; script: `qualify-sonoma-descriptor-media.mjs`. Syntax and actual execution passed; source/evidence hashes unchanged. No production code changed. These mismatches constrain the future physical-handle verifier; they do not establish essence identity, correction offsets or playback fidelity. See NATIVE_UI_TRIM_QUALIFICATION.md.

### Saved source descriptors and locator declarations

Full local check passed: 552 TypeScript tests, 36 Python tests, 137 tools, five skills, both transports and fresh-package checks (`.avid-mcp-analysis/check-source-descriptors.log`).

The Python saved-bin index now retains bounded selected descriptor numeric fields, locator variants/identity and physical-media class IDs. Snapshot validation preserves these declarations, and source tracing deduplicates them per visited bin/mob with recorded/absent/not_recorded statuses for historical compatibility. Locator strings are metadata only and never opened. Tests cover real generated AVB serialization, numeric/string bounds, partial declaration persistence and legacy/absent distinctions. Actual Sonoma MCP traversal verified video/audio descriptor rates and lengths, MSML and WINF locator classes; the bin hash remained unchanged. Evidence: `.avid-mcp-analysis/trim-source-trace-51eefa79-0192-47b6-b333-0df257f926df/evidence.json`. Saved forward/inverse/backward trim verification also passed with new descriptor-bearing captures. Physical media identity, unit conversion, handles and broader descriptor families remain open; see NATIVE_UI_TRIM_QUALIFICATION.md.

### Nested trim source trace and exact frame arithmetic

Full local check passed: 550 TypeScript tests, 34 Python tests, 137 tools, five skills, both transports and fresh-package checks (`.avid-mcp-analysis/check-source-trace-arithmetic.log`).

Source tracing now computes bounded relative deltas first and refuses unsafe/negative/empty mapped intervals, preventing intermediate rounding from losing an integer frame. Three regression cases cover exact large-coordinate mapping and invalid ranges. Actual MCP traversal around the restored Avid cut verified six picture/audio paths with the independently decoded offsets: three resolved references then an unresolved endpoint per path. Evidence: `.avid-mcp-analysis/trim-source-trace-20c29ed9-c3e5-4d69-8343-12b5fc3e95cf/evidence.json`; script: `qualify-trim-source-trace.mjs`. The captured input hash stayed unchanged. These results establish saved source mapping for this range; terminal convention, physical descriptors/locators and usable media handles remain open. Commit 96b15df passed all CI jobs as well as CodeQL.

### Trim-state accessibility experiment

Actual computer-use observation found exactly equal accessibility objects for dual-roller trim, the visible Edit history menu and normal editing on the owned Copy.05 fixture. Screenshots distinguished these states; accessibility exposed no track selection, trim counters, mode or contextual history command. The menu was dismissed and trim mode exited without editing or saving; the saved bin retained its baseline SHA-256. Evidence: `.avid-mcp-analysis/native-ui-state-eb39fd34-26f6-45c5-843f-7fc0a1c34b64/evidence.json`; conclusions and required observation work are in NATIVE_UI_TRIM_QUALIFICATION.md. This rules out this accessibility route alone as an unattended editing precondition source; no UI executor was shipped. Commit 96b15df's CodeQL analysis and separate security gate both passed, clearing the previously reported PR findings for that head; remaining CI jobs were still running at this observation.

### CodeQL finding hardening

Full local check passed: 547 TypeScript tests, 34 Python tests, 137 tools, five skills, stdio/HTTP and fresh-package checks. Log: `.avid-mcp-analysis/check-codeql-hardening.log`.

The AAF readiness observer now obtains file metadata and reads the compound-file header through the same open handle, checks that it is a regular file, and closes the handle on every path. Existing final path, checksum, source and deadline checks remain; this does not establish atomicity across the separate Python inspector. A regression refuses an output changed during inspection even when its compound header remains valid. The PostHog notice research downloader now requires the three SHA-256 values from retained prior evidence before publishing downloads, and saves them with inert `.txt` suffixes. Actual pinned downloads and vendor-source comparison passed in `.avid-mcp-analysis/posthog-notices-c6b2d874-3305-491b-868e-6a0be08c4bb6/evidence.json`. These changes address the two reported CodeQL locations; the new remote security gate must be checked separately before claiming alert closure.

### Immediate source-track trim coverage

Saved trim verification now resolves the referenced track by numeric index and media kind, requires unique direct SCLP coverage across before/after ranges, and refuses gaps, filler, opaque/combiner nodes, overlaps and malformed track intervals. Bounds records expose source track IDs/ordinals and media kinds. Tests cover shared picture/sound indices, missing/duplicate tracks, gap crossings in both directions, adjacent direct nodes and unsupported coverage. Actual MCP checks verified the expected six V1/A1/A2 mappings for retained forward, inverse and backward Avid captures with unchanged input hashes: `.avid-mcp-analysis/saved-trim-mcp-f7ec7b6b-9d4d-45d1-86c7-0ae9c0017cc4/evidence.json`. Full check passed: 546 TypeScript tests, 34 Python tests, 137 tools, five skills, transports and fresh package (`check-trim-track-coverage.log`). This validates the immediate declared track only; nested physical handles, online media, UI execution and playback remain open.

### Saved trim declared-source bounds

The existing trim verifier now checks outgoing/incoming baseline and result ranges against referenced same-rate source-mob durations, rejects exhausted bounds and unsafe interval arithmetic, and returns per-track declaredSourceBounds. Actual MCP verification passed retained Avid forward, inverse and backward captures, preserving all four input hashes and rejecting an incomplete track expectation: `.avid-mcp-analysis/saved-trim-mcp-a2593a8b-b4da-4b58-a39f-ce8d90d78e7f/evidence.json`. Full check passed: 540 TypeScript tests, 34 Python tests, 137 tools, five skills, transports and fresh-package checks (`check-trim-source-bounds.log`). See NATIVE_UI_TRIM_QUALIFICATION.md. This validates declared mob duration, not per-track physical handles, online media, UI execution or playback; the shipping edit adapter remains unfinished. Previous research commit a7b5609 passed CI and CodeQL.

### Eight-case local evidence-summary evaluation

Expanded the Qwen3-1.7B q4 candidate evaluation to eight committed synthetic editorial fixtures with predeclared review criteria, raw retained outputs and exact-quote checks. Four outputs matched the requested object shape; four returned arrays. Source facts were generally retained, but repeated notes produced 21 repeated entries, one time-bearing statement had incomplete quote support, and one quoted slate excerpt contained extra backslashes. Sampled peak RSS was 4.73 GiB; generation took 10.897–100.218 seconds per case. See SUMMARY_QUALITY_QUALIFICATION.md and summary-evidence-benchmark.json. The benchmark ran locally with existing weights, all eight results completed, syntax checks passed, and committed fixtures were verified identical to executed inputs. No production model or generation behavior changed; candidate remains research-only. This provides quality/resource counterexamples, not factual acceptance or full completion.

### Summary input boundaries and legacy resume

New summary runs use recipe 2, preferring sentence/whitespace boundaries within existing 2,000-character chunks and preserving UTF-16 pairs on forced splits. Recipe-1 resume retains the original partitioning and input hashes. Tests cover exact text preservation, an intact boundary-crossing assignment, Unicode and legacy prefix reuse. Actual local model/MCP comparison preserved the complete assignment in the new input but still produced unsupported National Guard details; factual quality remains unaccepted. Evidence: `.avid-mcp-analysis/summary-boundaries-7435c586-bb3a-487c-9947-fbd72d98440a/evidence.json`. Actual cancellation/reconnect/resume passed for nine nodes with one unchanged reused node: `summary-resume-5ffd9699-2b8e-4727-9c19-e09b2029e87d/evidence.json`. Both original sources stayed unchanged; attached editorial notes were synthetic. Full check passed: 537 TypeScript tests, 34 Python tests, 137 tools, five skills, transports and fresh-package checks (`check-summary-boundaries.log`). See SUMMARY_QUALITY_QUALIFICATION.md. Previous runtime-notice commit cc5616a passed CI and CodeQL.

### Explicit ONNX runtime notice delivery

Runtime setup now retains bundled, checksum-verified ONNX license and third-party notices outside the dependency receipt tree, using exact Node/Web package-version mappings. It refuses unresearched versions, redirected paths and changed notices, supports concurrent complete-file publication/reuse, and performs no notice writes during ordinary inference. Actual CLI setup created four notice files; repeat setup reused all four with the same runtime tree hash. Cached CLIP, Whisper English/multilingual, DistilBART and Florence inference passed with global fetch prohibited: `.avid-mcp-analysis/offline-models-2fcf1af9-954d-4723-9ff6-0b58a74ae970/evidence.json`. Full check passed: 534 TypeScript tests, 34 Python tests, 137 tools, five skills, transports and fresh-package notice/AAF/isolation/recovery checks (`check-runtime-notices.log`). Setup logs: `install-runtime-notices.log`, `reuse-runtime-notices.log`. See MODEL_RUNTIME_AUDIT.md for scope and source mappings. This does not close native component reconciliation, other dependency notices, legal clearance or model accuracy. Previous commit cc2482a passed CI and CodeQL.

### Optional inference runtime inventory

Added and executed bounded read-only inventory for the receipt-verified local Transformers runtime: 49 installed package manifests, notices/README hashes and 23 native/WASM hashes; 27 absent optional locations remain distinct. Five package locations have no standalone notice file. Exact ONNX Node/Web registry versions match runtime lock integrity strings, and version-correlated immutable upstream license/third-party notice sources are retained. Guid's registry-linked source returned 404; Sharp's bundled README adds native component terms beyond its top-level license. See MODEL_RUNTIME_AUDIT.md and committed JSON inventories. Core and optional-runtime npm audits both reported zero advisories; separate default-branch alerts remain unchanged. Runtime tree unchanged before/after; both scripts passed syntax and real execution. This advances provenance evidence, not complete license clearance, native binary component mapping, runtime notice delivery or cross-platform acceptance. No production code or dependency changed.

### Explicit video QC frame coverage

QC now requires a positive processed-frame count in the terminal FFmpeg progress block and uses passthrough output frame timing. Reports include videoCoverage; saved reads validate duration/stream consistency and distinguish legacy missing coverage from unselected video. Empty selected video ranges fail without report publication. Real MCP checks cover 120-frame full video, 30-frame short video in a four-second container, empty-range refusal and two selectable streams. Original and prepared Sonoma [60,90) each reported 900 frames, matching independent ffprobe frame timestamps, with existing audio sample checks and source hashes preserved. See QC_VIDEO_COVERAGE.md for scripts, evidence paths and limits. Full check passed: 531 TypeScript tests, 34 Python tests, 137 tools, five skills, transports and fresh installation with Python/AAF isolation/recovery. Log: `.avid-mcp-analysis/check-qc-video-coverage.log`. Counts do not certify continuous coverage, CFR, image fidelity or synchronization. Previous media-filter commit 6f79236 passed CI and CodeQL.

### Technical media filtering qualification

`avid_media_facets` now accepts video codec/geometry/nominal rational frame rate, audio codec/channel/sample rate and inclusive duration filters. Each stream-kind conjunction must match one stream; equivalent rational rates match without floating-point tolerance. Results deduplicate selected files, expose matching IDs for visual scope, and count facets over matching files. Unknown metadata does not satisfy requested constraints. These are cached probe declarations, not current content verification or CFR/delivery certification. Six added tests cover cross-stream false positives, invalid/missing values, rational rates, duration boundaries, duplicates and path-scope refusal. Actual MCP filtering and scoped local CLIP search passed on two short owned Sonoma derivatives with different geometry/rates/channels; original and derivative hashes stayed unchanged. Evidence: `.avid-mcp-analysis/media-filters-76b39f88-4c80-4769-a2ee-21153bca23ad/evidence.json`; reproducible script: `scripts/research/qualify-media-filters.mjs`. Full check passed: 529 TypeScript tests, 34 Python tests, 137 tools, five skills, both transports and fresh-package AAF/isolation/recovery. Log: `.avid-mcp-analysis/check-media-filters.log`. The selects skill documents dispatch and empty-result handling. Broad media/model/native-host acceptance remains open. Previous pushed commit 472aaa3 passed CI and CodeQL.

This ledger preserves the complete requested scope across increments. It is not a new, narrower plan. Sources: ROADMAP.md, REVERSE_ENGINEERING_PLAN.md, COMPETITOR_RESEARCH.md, and the user's Windows-first/Mac-later and open-source requirements. The overall goal remains active until each applicable requirement has direct evidence.

| Requirement | Current evidence | Remaining acceptance work |
| --- | --- | --- |
| Original open-source implementation and license/provenance | MIT source; public contracts cited; no competitor implementation incorporated | Audit all additional model/runtime/installer licenses before release |
| Local MCP installation through AI clients | CLI configuration lifecycle; managed package install/status/removal/recovery; five installed CLI formats and generated-command execution; isolated Claude CLI connection/removal/recovery; fresh installed-server native Avid reads; actual Codex model-selected health/inventory through a fresh installed package | GUI and model-driven native-edit workflows; clean-machine system dependencies; broader package and optional-runtime update/rollback/uninstall acceptance |
| Native project/bin/clip/marker reads and writes | Windows 2024.12 adapter with 28 allowlisted native methods; operation-specific preview/apply MCP, persistence and token evidence | Per-action undo/recovery, restart, lock/shared-project and version matrix |
| Timeline reading, range/track search, source mapping | Offline AVB/AAF/OTIO inspection; saved semantic snapshots/diffs and direct source mappings; collection range mapping | Live sequence graph and actual editor range queries |
| Sequences, selects, stringouts, trims, track assignment | Collections and OTIO round trip; source-checked AAF builder; native reference export/selects import; saved stereo-combiner conformance and prepared-PCM two-cut render evidence | General trims/relink/undo; multiple sources/rates and broader sequence conformance; playback and video range/color fidelity |
| Visual/text/image/frame similarity and bounded results | Pinned local CLIP, ranged uniform sampling, reference-frame queries, paginated sample discovery, media/time scopes and shot midpoint embedding with retained ranges; full Sonoma preview detection/index workflow | Broad ranking/detector/resource benchmarks and broader full-length coverage qualification |
| Transcript search/read/ranges/export | Immutable revisions, correction ancestry, deletion and five formats; English/multilingual local Whisper; anonymous diarization and transcript assignment; retained checkpoint/resume evidence | Broad speech/language/speaker accuracy, mixed-language/non-speech behavior, larger model choices and broader correction/recovery qualification |
| Hierarchical summaries and node drill-down | Local generated transcript hierarchy, overview/node queries, source checksum/indices, discovery/deletion | Factual accuracy/omission/truncation benchmarking, broader language/long-media coverage and visual grounding |
| People collections | Pinned YuNet/SFace runtime; bounded indexing, pagination, names/merge/move/recluster/removal; Sonoma crop/deletion and checkpoint/resume workflows | Independent accuracy/resource benchmarks, dense sampling and broader interruption/media qualification |
| Watch folders/shared media cache | Polling CRUD/start/stop/checkpoints and moved-source aliases; persistent analysis-job records with real MCP restart/result recovery | More failure/concurrency/resource qualification; interrupted computation resume and offline/relinked paths |
| QC/reports/thumbnails/exports/copies | Seven-file report/contact sheet; portable ranged thumbnail strips with frame checksums; bounded black/freeze/silence/loudness/timestamp QC with generated-fixture and Sonoma evidence | Camera report depth, multistream/HDR/offset QC, perceptual sync, broader output QA and delivery presets |
| Project snapshots/source usage/complexity | Historical revision/mob discovery, reconnect, scoped pagination, per-bin omission warnings, direct source usage and saved structural counts; bounded exclusive snapshot publication; generated AVB and Sonoma evidence | Live/unsaved graph, deeper nested/effect mapping, qualified mixed-rate conversion, render-cost/relink/turnover reports and power-loss durability |
| Optional Jumper provider for licensed users | Public OpenAPI 1.0 refreshed; bounded health/visual/transcript search; Windows executable/hash/PID-start pairing and accepted-connection HTTP gate; lazy MCP diagnostics; fresh-package fixture dispatch and process-replacement refusal | Runtime version/contract qualification, additional operations and licensed-provider execution |
| Named Windows UI operations | Computer-use research only | Shipping action adapter, focus/shortcut/dialog checks, failure and post-state tests |
| Original workflow skills | Five original skills packaged for ingest/QC, selects, review markers, turnover and export; client setup/examples documented; fresh-install tool-reference validation | Named-client application qualification |
| Optional enterprise adapters | CTMS HAL read and prerequisite diagnostics | Scoped production operations, NEXIS/path mapping/locks, authorized AMT qualification |
| Optional sanctioned Extension | SDK-neutral bridge protocol/tests | Avid SDK/license/package access and installed Extension tests; unavailable SDK is not substituted by native evidence |
| Mac | Explicitly deferred to a Mac by user | Mac implementation and separate real-host/client qualification when working on a Mac |
| Sonoma/end-to-end acceptance | Seven MP4s linked and persisted; real Avid H.264 sequence export with 120 frames and visible source scenes; sources unchanged | Viewer playback, audio/color/frame conformance, failure/recovery and original Premiere source qualification |
| Delivery | Feature branch committed and pushed incrementally | Final full audit, CI/client/host evidence, docs/release artifacts consistent with actual support |

Evidence statuses distinguish implementation, offline tests, simulated adapters, real local media, real Avid response, visible editor state, saved-file persistence, and clean-machine/client qualification. A green package test or tool count cannot close a host/feature requirement.

## Progress checkpoints

- `0d382aa`: native adapter, local search/transcription/artifacts/jobs/collections/setup baseline; complete local package check, 135 TypeScript tests, 51 total MCP tools. Pushed to `codex/open-source-full-plan`.
- Watch/cache increment: real generated MP4 indexed only after stability, restart checkpoint avoided reindexing, moved content retained its ID/transcript, removing the watch preserved media. Runtime script: `scripts/research/qualify-watch.mjs`.
- `5b956c1`: persistent watches/cache aliases; complete local package check, 139 TypeScript tests, 56 tools; pushed.
- Native subclip increment: all-track 30 fps source bounds qualified through MCP and bin persistence; saved AVB confirmed `_START`/`_END` and picture/sound tracks. Initial track-selection discrepancy isolated to native numeric track indexing.
- Saved snapshot increment: real Sonoma bin indexed into 32 mobs; the new subclip mapped exactly to source frames 2850–2880 on three tracks; identical saved snapshots had no semantic differences. Native live sequence reading/creation and Avid interchange remain open.

- People increment: 12 real Sonoma samples produced three face crops; naming, merge, move, recluster, individual removal and whole-index deletion passed with the source SHA unchanged. This proves the workflow, not recognition accuracy.

- Transcript review increment: real stdio MCP calls imported synthetic review text for the Sonoma preview, corrected text/timing/speaker, searched the new revision, exported and inspected SRT timing, and deleted the original revision. Source SHA unchanged; this is review workflow evidence, not speech recognition accuracy.

- Scoped visual increment: 24 Sonoma frames over [60,90) indexed and searched through stdio; the reference frame ranked first with cosine 1, and [80,85) filtering returned exactly four samples. Model-loading/sample/query workflow took 14.964 seconds on this Windows host. Shot detection and semantic accuracy benchmarking remain open.

- QC increment: generated black/static/silent-to-moving/tone MP4 validated event source timestamps within 80 ms in a nonzero-start range. Sonoma [60,90) produced JSON/HTML reports, no detected black/freeze/silence or variable timestamp intervals, and measured -22.5 LUFS/-5.81 dBTP. Both source hashes remained unchanged.

- Configuration lifecycle increment: built CLI installed, updated, restored and removed Avid entries in temporary Claude/VS Code JSON formats; generated commands completed real MCP ping. Rollback preserved unrelated edits. This does not prove either named client UI or package/dependency lifecycle.

- Summary increment: pinned local DistilBART generated three nodes from synthetic editorial notes attached to Sonoma media in 7.191 seconds; leaf source references matched. The observed overview ended mid-sentence, so output now flags missing sentence boundaries and accuracy/quality acceptance remains open.

- AAF host research: exported a real Sonoma master, built a 120-frame three-track composition, imported into a disposable bin, closed/reopened, and verified exact cuts in the saved AVB. Avid visibly showed the timeline; captured video remained black. See NATIVE_AAF_QUALIFICATION.md for export-path behavior, identity remapping and remaining fidelity requirements.

- AAF builder increment: real stdio tools inspected the Avid-exported Sonoma template, validated local locators, created a conforming 120-frame three-track selects AAF and rejected mismatched rates. Python tests verify source preservation, exact output conformance and no overwrite; TypeScript tests cover network/out-of-scope locators. Native import remains a separate adapter gap.

- Thumbnail strip increment: real stdio calls sampled a four-second black/white fixture at 0.5/1.5/2.5/3.5 seconds; independently decoded JPEG luma was 0/0/255/255. Invalid ranges/counts were rejected. Twelve Sonoma samples over [60,90) produced a portable HTML folder and frame-checksum manifest; source hashes remained unchanged. Labels are requested seek times, not exact decoded PTS. Runtime script: `scripts/research/qualify-thumbnail-strip.mjs`.

- Job journal increment: completed Sonoma indexing results recovered identically after a real MCP server disconnect/reconnect. Unit tests cover ordered writes, unfinished records becoming unresolved, pagination and access-scope isolation. No runtime config/credentials persisted. This is persistent status/result recovery, not computation checkpoint/resume; queued/running records from other sessions are never automatically replayed.

- Cancellation qualification: running jobs remain `cancelling` until the worker close event, including a failed termination request; queued work stays bounded and shutdown dispatches no new workers. Worker output is bounded by bytes and discarded during cancellation. Real stdio Sonoma job cancellation reached `cancelled` and remained so after reconnect. Descendant termination and computation resume remain separate qualification work.

- Shot detection increment: real stdio detected the known cut at source second 2 while decoding [1,3), with 60 decoded frames; a static interval returned no cuts. A queued Sonoma [60,90) job decoded 900 frames and reported six candidate cuts. Source hashes remained unchanged. This validates cut timing and execution, not precision/recall against human-reviewed footage. Script: `scripts/research/qualify-shots.mjs`.

- Shot-aware visual increment: full 190.866666-second Sonoma preview request reported 5725 decoded frames and 32 shots; every detected shot had a midpoint embedding with matching source range. Scoped reference-frame search preserved its shot range and returned cosine 1.0. Runtime after preserving video stream offsets was 20.167 seconds; source unchanged. An independent ffprobe decode confirmed all 5725 source video frames. A generated video delayed 0.5 seconds against audio detected its cut at 2.5 seconds, verifying stream-offset preservation. This is workflow coverage of detected shots, not proof that all true cuts were detected or a semantic accuracy benchmark. Script: `scripts/research/qualify-visual-shots.mjs`.

- Native render increment: computer use exported the four-second imported AAF sequence as H.264. Independent extraction showed both expected source scenes despite the black Avid viewer capture. The default test preset downmixed stereo to mono and produced visible level differences, so audio/color fidelity remains open. See NATIVE_RENDER_QUALIFICATION.md and `scripts/research/inspect-avid-render.mjs`.

- Native API render follow-up: ExportFile with the same named preset produced a decoded video/audio checksum listing identical to the UI export. Completed preceded file readiness; a revised fixed-fixture probe waited for stable output and expected metadata, then passed another isolated export and full decode. This is research evidence; the guarded production native export adapter remains open.

- Render verifier increment: reusable output-root/contract/stability/hash/full-decode verification tested on the real 120-frame native output and failure fixtures. Actual decoded frame count is required independently of container metadata. Host-owner callback and no-retry behavior are available; integration with the native action and lock lifecycle remains open.

- Native export action increment: real stdio preview/apply exported and verified the 120-frame Sonoma sequence; replay was rejected. Tests verify lock lifetime, retained lock on uncertain output, missing presets and whole-source duration contracts. The action supports H.264 1080p30 and explicit audio expectations; preset fingerprinting, unsaved graph guards, recovery and source fidelity remain open. See NATIVE_EXPORT.md.

- Export lock recovery increment: scoped/checksum-checked retained-lock inspection and release implemented, with stopped-Avid checks and serialized recovery. Fixtures verify successful release/output preservation and rejection of active/generic/changed/out-of-scope locks. Real MCP with an isolated lock profile refused recovery while the installed Avid ran and preserved the lock/output. After UI exit of the installed Avid, the MCP test with --expect-stopped released an isolated retained-lock fixture, verified absent status and preserved output. Evidence: .avid-mcp-analysis/recovery-proof-11603dc1-41fe-4f51-be8a-d0d54d2a501f/evidence.json. Relaunch reached activation; project restart verification awaits manual startup. CI exposed canonical-parent alias comparison failures on Windows/macOS; the fix keeps canonical root validation and separately validates the output leaf layout, with an alias regression test.

- Speech model increment: pinned multilingual tiny plus existing English tiny.en can be selected in direct transcription or speech jobs, with validated language hints and auto mode. Real stdio Sonoma [60,80) passed both explicit-English direct and automatic-language worker paths with source SHA unchanged. Unit tests cover revision/local-only loading, source timestamps, incompatible language rejection, old job defaults and changed-source refusal. Non-English recognition accuracy, diarization and broader speech qualification remain open.
- Mandarin speech probe: generated original known text with installed Huihui Desktop voice and transcribed it locally through MCP using tiny/zh. Measured 13 edits over 41 normalized reference characters (31.7% CER); exact hypothesis/reference retained in .avid-mcp-analysis/mandarin-speech-7a93457a-4f17-41dc-ad96-2044b8fe70ca/evidence.json. This is a workflow pass with measured accuracy limitations, not language acceptance.
- Mandarin metric clarification: inspecting the retained hypothesis shows traditional-character variants and Arabic 9 versus the reference Chinese numeral. The raw 31.7% CER includes these orthographic differences and must not be described as 13 spoken-content errors; general accuracy acceptance remains open.

- Visual computation recovery: uniform and shot-midpoint indexing now checkpoint committed samples. Explicit resume forks a new run after source/model/image validation and reuses the saved prefix. Real Sonoma worker cancellation followed by MCP reconnect resumed 3 committed embeddings into a complete 120-sample index; original checkpoints and source SHA unchanged. Evidence: .avid-mcp-analysis/visual-resume-7a7e7e0a-ca0c-4856-8a3b-d4882ee4e153/evidence.json. Other analysis kinds, shot detection before plan creation, cleanup and broad concurrency/resource qualification remain open.

- Visual checkpoint integrity: committed sample publication is exclusive and completion publication is atomic. Completed status validates the actual final index against every committed sample. Tests reject sample overwrite, modified index vectors and missing output. The real 120-sample cancellation/reconnect/resume test passed again with 3 reused embeddings after these changes; evidence: .avid-mcp-analysis/visual-resume-8606f65a-746b-4c99-a49b-9912b4221fff/evidence.json. Full local check: 196 TypeScript tests, seven Python tests, 86 tools, five skills and fresh-package checks.

- Shared visual-cache scope: discovery now skips runs with missing/out-of-scope media while filling pages from accessible runs; direct access remains denied. Real MCP generated-MP4 tests narrowed roots, rejected direct access to the other run, moved a disposable source, observed its disappearance, reindexed its new path and restored the original completed index without recomputing embeddings. Source content hash unchanged. Evidence: .avid-mcp-analysis/visual-scope-46131b22-ad08-4564-ba02-96ad6c606db2/evidence.json.

- Summary computation recovery: each generated node is checkpointed with its input hash. Explicit resume validates transcript/model/recipe/structure and creates a new run using the committed prefix. Real MCP cancellation/reconnect resumed one saved node into a nine-node hierarchy from synthetic Sonoma editorial notes, preserving parent checkpoint and source SHA. Evidence: .avid-mcp-analysis/summary-resume-09c01003-f2a2-4494-8531-45eafce1882c/evidence.json. Factual quality, visual grounding, cleanup and other analysis-kind recovery remain open.

- Summary discovery after transcript deletion: listing now reports unavailable runs per row without hiding healthy runs for the same authorized media. Direct status/resume still rejects invalid provenance. Real MCP created two synthetic transcript summaries, deleted only the older transcript using its checksum, paginated both run states and verified unchanged source SHA. Evidence: .avid-mcp-analysis/summary-discovery-605ad13b-29f3-45a3-9ac7-2ef7d0ca0ac0/evidence.json.

- Summary hierarchy integrity: generation and reads reject cycles, shared/unreachable nodes, invalid parent/leaf ranges, duplicate or missing references and omitted nonempty source references. Existing real nine-node Sonoma recovery output passed the new validator. These checks establish graph and reference coverage, not factual accuracy or omission-free prose.

- Summary quality comparison: three original editorial fixtures compared baseline and higher-token/beam settings, with effective runtime parameters recorded. Outputs were identical; manual review found omitted decisions/assignments, repetition and an unfinished sentence. Candidate tuning was not adopted. Reproducible script and findings: docs/SUMMARY_QUALITY_QUALIFICATION.md. This is direct negative quality evidence and leaves factual-quality acceptance open.

- Instruction-summary candidate evaluation: pinned Qwen3-0.6B ONNX q4 ran locally on the same three fixtures with paragraph and decision-list prompts. Paragraphs recovered one prior omission but lost other critical decisions; lists recovered those decisions but produced unsupported category labels and failed the repeated-notes fixture. Candidate remains research-only. Cached execution and exact outputs/resource observations are documented in SUMMARY_QUALITY_QUALIFICATION.md; stronger model evaluation remains necessary.

- Larger instruction-summary comparison: pinned Qwen3-1.7B q4 preserved normal editorial decisions under the unchanged paragraph prompt, but inferred event recurrence from repeated text; the list prompt invented a publication restriction and repeated bullets. Exact fixtures/settings/output and CPU memory/latency observations are retained. Candidate is not yet a production integration or general accuracy acceptance; see SUMMARY_QUALITY_QUALIFICATION.md.

- Summary overview review: overview and intermediate-node reads now return deduplicated original transcript excerpts from all descendant leaves, in transcript order. Direct leaf reads stay scoped to their own references. Read-only MCP qualification checked all nine nodes of existing real-model output against the original transcript; evidence: .avid-mcp-analysis/summary-sources-5ba42e0d-cbd9-4360-ae2b-b90fccac2499/evidence.json. Full local check passed: 203 TypeScript tests, seven Python tests, 89 tools, both transports and fresh tarball installation. This establishes source review access, not factual entailment.

- Speech recovery research: real pinned models reproduced uninterrupted text and timestamps after reloading and replaying one saved generation tensor, across English and multilingual configurations. This qualifies the merge-preserving approach, not production resume or process-kill recovery. See SPEECH_RECOVERY_RESEARCH.md. The experiment exposed that the pinned runtime defaults omitted language to English; it does not detect language. Production now reports the effective English fallback and unsupported detection explicitly. Real direct/job MCP qualification passed with source unchanged: .avid-mcp-analysis/speech-options-b8360ead-e151-4f88-b72f-2e7491074595/evidence.json. Full local check passed (203 TypeScript/seven Python tests, both transports and fresh-package installation).

- Speech computation recovery: persisted token checkpoints, scoped run discovery/status and explicit resume now ship in the branch. Source/audio/model/runtime/input checks gate replay; completion verifies transcript and checkpoint hashes. Real MCP cancellation/reconnect reused two windows into a nine-window Sonoma [0,180) transcript, exactly matching uninterrupted segments while preserving source and parent checkpoints. Evidence: .avid-mcp-analysis/speech-resume-955a0ae2-6e99-4cca-a7cc-9c9d5089ffcb/evidence.json. Extraction/features repeat; cleanup, broad concurrency and speech-accuracy acceptance remain open.
- Speech recovery validation: full local check passed with 205 TypeScript tests, seven Python tests, 92 tools, both transports, package dry run and fresh tarball installation. CI and licensed-host checks remain separate.

- Language detection increment: an original single-decoder-step language-token ranker now powers avid_detect_speech_language. It reports reviewable scores and source/model provenance; exact digital silence returns no candidate. Real MCP correctly suggested en/zh for known synthetic voices and null for silence, preserving source hashes: .avid-mcp-analysis/language-mcp-c0d86230-04f2-4d97-b0ec-29486187b55b/evidence.json. Unguarded model output selected Welsh for silence; broad speech/non-speech and multilingual accuracy remain open. Transcription auto behavior and existing recovery recipes remain explicit English fallback.
- Language suggestion validation: full local check passed with 208 TypeScript tests, seven Python tests, 93 tools, both transports and fresh tarball installation. Preceding speech-recovery commit dc9b929 passed all Windows/macOS Node 20/24, landing and CodeQL CI checks.

- Dense/ranged people indexing: direct and queued paths accept 120 samples per source and 1200 total, with optional source ranges. New indices retain coverage even for zero-face sources and validate returned face times. Crowded frames now fail at over 50 detections rather than silently dropping extras. Real MCP sampled Sonoma [60,120) 120 times, returned 38 faces and validated all requested times with source unchanged: .avid-mcp-analysis/people-range-7ff0a68a-9d0f-44ea-bc5e-7a2f68ffddc1/evidence.json. Recognition accuracy and people computation recovery remain open.
- Dense people validation: full local check passed with 210 TypeScript tests, eight Python tests, 93 tools, both transports and fresh package installation. The Python crowded-frame regression verifies rejection before any face is silently omitted.

- Reference-face search: avid_find_similar_faces ranks cached appearances across up to 20 authorized indices, with media/time filters, score thresholds and bounded output. References use composite index/face IDs; results include index revisions and user-supplied names, not inferred identity. Real inspect-only MCP ranking of 37 Sonoma detections matched an independent cosine calculation, including top-five and half-open time filtering: .avid-mcp-analysis/face-search-1c6072ef-fa75-42ea-999a-2e1abde418ec/evidence.json. Source unchanged. Recognition accuracy and unsampled appearance coverage remain open.
- Reference-face validation: full local check passed with 212 TypeScript tests, eight Python tests, 94 tools, both transports and fresh package installation.

- People index discovery: saved indices can now be found by authorized media ID after client restart, including zero-face indices with retained coverage. UUID pagination and per-index unavailable results preserve healthy discoveries; partial/deleted indices without final records are omitted. Fresh-session inspect-only MCP found the existing Sonoma index and reverified all 37 reference-face matches: .avid-mcp-analysis/face-search-9557a8c2-8f4f-4c67-a701-bad34d932f3a/evidence.json. Avid remains on its activation screen; Claude has an unrelated coding session that was left undisturbed. Native restart and named-client UI qualification remain open.
- People discovery validation: full local check passed with 213 TypeScript tests, eight Python tests, 95 tools, both transports and fresh installation. Prior reference-face commit 0507d2a passed Windows/macOS Node 20/24, landing and CodeQL CI.

- Visual semantic development benchmark: assistant-inspected 32 Sonoma midpoint frames defined 16 frozen scene queries. Actual inspect-only MCP retrieved a labeled match first for 14/16 and within three for 16/16; mean recall@5 was 0.9271. Three absent-scene probes still returned ranked frames, with score overlap against valid queries, so no fixed threshold was adopted. Source/thumbnail hashes unchanged. Evidence: .avid-mcp-analysis/visual-ranking-b9186cd2-7d2f-4be3-b1f7-d7ed3e5f549b/evidence.json. Labels, renderer, reproducible script and limitations are in VISUAL_SEARCH_BENCHMARK.md. This is one assistant-labeled development set, not independent or broad accuracy acceptance.

- Visual query preservation: removed silent tokenizer truncation and reject descriptions exceeding the pinned 77-token context before text inference. Real MCP rejected a 94-token fixture; all frozen positive and negative Sonoma rank/score results stayed exactly equal. Evidence: .avid-mcp-analysis/visual-ranking-09818354-e617-40a7-8aeb-bc4e7eb44d68/evidence.json. Unit tests cover the 77/78 boundary and no inference after overflow. Ranking-quality limitations remain unchanged.
- Query-limit validation: full local check passed with 214 TypeScript tests, eight Python tests, 95 tools, both transports and fresh package installation.

- People recovery backend foundation: optional per-frame checkpoints now bind model/frame/crop hashes and face data, with exclusive publication and explicit contiguous resume. Actual standalone OpenCV process interruption followed by a new-directory resume reused four frames, completed all 120 and matched all 38 uninterrupted Sonoma face records exactly, preserving parent/source. Evidence: .avid-mcp-analysis/face-checkpoints-a0cf6e39-e033-45f0-a7b6-16fdd9b07356/evidence.json. This is not yet MCP people resume or extraction recovery; remaining requirements are explicit in PEOPLE_RECOVERY_RESEARCH.md.
- Face-checkpoint foundation validation: full local check passed with 214 TypeScript tests, nine Python tests, 95 tools, both transports and fresh installation. Existing MCP people behavior remains non-resumable until the adapter/lifecycle integration is implemented and qualified.

- People MCP recovery: new runs checkpoint extraction and face analysis; scoped discovery/status and explicit resume copy verified prefixes into a new index. Real Windows MCP cancelled during extraction and then during analysis across reconnects, checked cancelled Python backends were absent, reused 120 images/34 analyzed frames and exactly reproduced 38 Sonoma face records. Both parents and source stayed unchanged; completed resume rejected. Evidence: .avid-mcp-analysis/people-resume-0396577d-c87b-4ea6-9be1-543e3eeeca86/evidence.json. Face deletion now clears its index's derived analysis checkpoints. Cleanup, broad concurrency/failure and recognition acceptance remain open.

- People recovery validation: full local check passed with 219 TypeScript tests, nine Python tests, 98 tools, stdio/HTTP checks and fresh tarball installation. Actual MCP removal of one face from the disposable resumed index removed its crop and all 120 analysis checkpoints, left 37 face records, preserved every sampled image/source hash, and invalidated original completion verification. Evidence: .avid-mcp-analysis/people-resume-0396577d-c87b-4ea6-9be1-543e3eeeca86/deletion-evidence.json. The original recovery evidence describes the index before this deliberate edit.

- QC stream selection: explicit absolute video/audio stream indices and null-to-skip selectors now allow analysis of alternate tracks. Real MCP qualification generated a four-stream lossless fixture, distinguished static/silent first tracks from moving/tone alternate tracks, verified audio-only output and rejected a wrong-type selector; source SHA stayed unchanged. Evidence: .avid-mcp-analysis/qc-streams-ae26f2c3-2529-4fac-8cba-b74cf94963a2/evidence.json. Script: scripts/research/qualify-qc-streams.mjs. Each report covers its selected pair only; broader multistream coverage, HDR, timestamp offsets and perceptual synchronization remain open.
- QC selection validation: full local check passed with 220 TypeScript tests, nine Python tests, 98 MCP tools, both transports and fresh package installation. Previous people-recovery commit af3c7fc passed Windows/macOS Node 20/24, landing and CodeQL CI; those CI results do not establish real Mac host qualification.

- QC offset correction: generated delayed-video and delayed-audio fixtures exposed early event timestamps from resetting each stream to zero, plus early audio loss during input seeking. Decode-before-trim with range-relative timestamps now preserves delays and early audio. Actual MCP verified black/freeze/silence boundaries within 80 ms for starts 0, 0.5 and 1.5 seconds; source hashes unchanged. Evidence: .avid-mcp-analysis/qc-offsets-07614dfd-223b-493d-94c8-cb1c1c389a8e/evidence.json. Sonoma [60,90) and alternate-track regression proofs also passed (.avid-mcp-analysis/qc-2bfcced4-1ff5-4d06-99ab-b0557b03178c/evidence.json and qc-streams-b82f151b-e7b7-49fd-8914-3eaff9f91367/evidence.json). Late ranges now incur preceding decode work and remain timeout-bounded; broad timestamp discontinuity, HDR and perceptual synchronization acceptance remain open.
- Offset correction validation: full local check passed with 220 TypeScript tests, nine Python tests, 98 tools, both transports and fresh installation. Real offset/stream/Sonoma checks above exercise the changed FFmpeg path; unit success alone does not establish timestamp correctness.

- QC stream context: JSON/HTML reports now preserve an allowlisted set of selected-stream codec, picture/color, timing and audio declarations; absent fields remain null and arbitrary tags are omitted. Actual MCP on generated 10-bit fixtures preserved PQ, HLG and BT.709 transfer/primaries/matrix/range declarations and source hashes, including both saved formats. Evidence: .avid-mcp-analysis/qc-color-97e1ba02-d9aa-40d2-8527-1a23f3e5ad04/evidence.json. Initial encoder-only fixture omitted transfer tags; corrected frame metadata made the intended fixture declarations observable. This is tag reporting, not HDR mastering, tone mapping or perceptual acceptance. The all-black EOF check also confirmed existing detector reporting; no code change was needed for it.
- QC stream context validation: full local check passed with 221 TypeScript tests, nine Python tests, 98 tools, both transports and fresh package installation.

- Isolated package installer: CLI now accepts a local tarball, expected SHA-256 and absolute package root. It copies/rechecks the archive, disables npm lifecycle scripts, audits dependencies, checks expected package/bin identity, qualifies installed MCP ping/tool listing, and publishes an exclusive success receipt in a UUID directory. Real Windows qualification installed the same branch archive twice in paths containing spaces, pinged both, switched a disposable generic client configuration, restored the prior entry and removed it while preserving an unrelated entry and both package entry hashes. Evidence: .avid-mcp-analysis/package lifecycle f99511a5-f0e9-4b70-b40c-9d0a513e5e3f/evidence.json. This is isolated package/configuration workflow evidence; different-version compatibility, package deletion, system dependencies, optional runtime lifecycle and named-client UI remain open.
- Package installer validation: full local check passed with 223 TypeScript tests, nine Python tests, 98 tools, both transports and fresh package installation. Changed archive rejection occurs before creating the destination root; real lifecycle proof is separate from unit checks.

- Managed package removal/status: new receipts bind the complete installed file/link tree. Status verifies location and reports the receipt/tree digest; Windows removal refuses changed files and running Node servers, renames the verified direct child, rechecks and deletes. Intact interrupted removal directories can be explicitly restored after receipt/tree/process checks; partial deletion is not automatically reversible. A first real run retained one renamed directory after a transient process-state failure; explicit verified recovery then removal succeeded. Fresh full lifecycle qualification tested live-server refusal, added-file refusal, intact recovery and removal of both stopped disposable installs, preserving the external archive. Evidence: .avid-mcp-analysis/package lifecycle 49d0e4fe-65b9-464c-ab7c-825bea9c4099/removal-evidence.json. Cross-version compatibility, system dependency/optional runtime lifecycle, arbitrary concurrent writers, external client-reference discovery and Mac remain open.
- Package lifecycle validation: full local check passed with 226 TypeScript tests, nine Python tests, 98 tools, both transports and fresh package installation. Real Windows process/removal evidence above is separate from cross-platform unit results.

- Multilingual auto transcription: new tiny/auto runs rank first-window language tokens, explicitly supply the selected candidate to generation and retain the ranked decision in recipe-two manifests. Resume preserves it; legacy recipe-one runs keep English fallback. Completed manifests are checksum-bound. Actual English/Mandarin synthetic voice tests selected expected languages and matched explicit-language segments exactly; digital silence returned an undetermined-language error. Evidence: .avid-mcp-analysis/speech-auto-bf46421f-6ac6-4b22-9a03-44f6158f60fc/evidence.json. Sonoma actual cancellation/reconnect reused one window into nine with the identical saved language decision and uninterrupted segments, preserving source/parent: .avid-mcp-analysis/speech-resume-438a889c-97f9-42d1-b9a5-724cd2146d4e/evidence.json. Broad multilingual accuracy, mixed-language segmentation, silence/music handling beyond digital zeros and diarization remain open.
- Auto speech validation: full local check passed with 228 TypeScript tests, nine Python tests, 98 tools, both transports and fresh installation; focused speech tests and build passed again after tightening completed-manifest verification. Real selection/recovery results above remain narrower than accuracy acceptance.

- Cross-platform CI follow-up: macOS rejected two hand-written package-lifecycle fixtures because their receipt paths used the temporary-directory alias rather than its canonical path. Production installation already writes canonical paths. Fixtures now call realpath before constructing receipts, preserving the location-mismatch guard. All three focused lifecycle tests passed locally; macOS confirmation requires the new CI run. A fresh computer-use window inventory still found Avid Media Composer Activation, so native restart work remains gated on manual startup.

- Published upgrade compatibility: registry metadata confirmed 1.1.0 has only the server bin. Managed installation now supports that layout via a current-bootstrap setup command carrying an absolute server entry and its checksum; configuration generation rejects changed entry bytes. Actual published archive integrity was verified against registry SHA-512, then installed/audited and activated (27 tools), switched to the unreleased branch artifact (98 tools), and restored through the configuration backup. Both entry hashes remained unchanged. Evidence: .avid-mcp-analysis/published-upgrade-05e6e06a-a369-49b7-b429-de98b4f9c079/evidence.json. Both artifacts declare 1.1.0; hashes distinguish their implementations. This closes that specific configuration/protocol transition, not broad version or newer-artifact downgrade compatibility.
- Published-upgrade validation: full local check passed with 229 TypeScript tests, nine Python tests, 98 tools, both transports and fresh package installation. Previous head 7c99e1f passed Windows/macOS Node 20/24, landing and CodeQL CI.

- Visual grounding candidate research: original benchmark ran pinned local SmolVLM-256M q4 and Florence-2-base-ft q4 on eight pre-inspected Sonoma midpoint frames, preserving source/image hashes. SmolVLM omitted vineyard/barrels and repeated to the token cap on the phone frame. Florence was faster (about 1.1–1.35 s/frame versus 7–10.8 s) and recognized barrels/phone, but both caption modes missed grapevines/climbing action. Exact outputs, revisions, task prompts, sampled RSS and manual comparisons are in VISUAL_CAPTION_QUALIFICATION.md. Florence is a candidate for a source-linked reviewable caption workflow; neither is accepted for unattended factual visual summaries. This is research execution, not a new shipping MCP tool. Script syntax and all three real model runs passed; production code/dependencies were unchanged. Prior head d776087 passed Windows/macOS Node 20/24, landing and CodeQL CI.

- Source-linked frame captions: five MCP tools now generate, read (including verified JPEG image content), discover, correct and delete single-frame Florence captions. Explicit model installation and cancellable caption jobs are available; records retain model/runtime/task, requested seek time, original machine text and source/image hashes. Actual Sonoma direct/barrel and queued/phone generation, stale-checksum rejection, correction preserving original text, reconnect discovery/read, image-payload checksum verification and deletion passed with source unchanged. Evidence: .avid-mcp-analysis/captions-mcp-5f67e007-def3-4104-bae8-1454ec5051fd/evidence.json. Model accuracy, batch/shot coverage, computation recovery, broad concurrency/cleanup and hierarchical visual-summary integration remain open.
- Frame-caption validation: full local check passed with 232 TypeScript tests, nine Python tests, 103 tools, both transports and fresh package installation. Unit tests cover source/image scope/integrity, correction checksums, capability denial and preservation of unexpected files; runtime evidence above covers actual model and MCP behavior.

- Caption batch recovery: four MCP tools and two job kinds now generate, discover, verify and resume 1–120 selected-time captions. Every checkpoint binds its caption checksum and requested time; completion binds the plan and checkpoint list. Resume creates a new run referencing verified saved captions. Actual Sonoma cancellation/reconnect reused one caption into 12; all caption texts and sampled-image hashes exactly matched an uninterrupted run, and parent/source bytes were unchanged. Evidence: .avid-mcp-analysis/caption-batch-0e41594b-4081-40aa-a918-dbfca87795b5/evidence.json. Shared caption correction/deletion intentionally invalidates referencing run verification. Automatic shot selection, model accuracy, cross-process edits, large-media hashing costs, cleanup and hierarchical visual-summary integration remain open.
- Caption batch validation: full local check passed with 234 TypeScript tests, nine Python tests, 107 MCP tools, both transports and fresh package installation/audit. Focused tests cover interrupted prefix reuse, unchanged parent checkpoints, completed-run rejection, edited-reference rejection, invalid time plans, permissions and source changes. Previous head 0218396 passed Windows/macOS Node 20/24, landing and CodeQL CI; new-head CI is separate.

- Visual hierarchy integration: four MCP tools plus visual_summary jobs now build a four-child hierarchy from explicit caption checksums. Leaves preserve current caption text; parent generations record model/revision/runtime/options. Reads verify source/caption/image bytes and return descendant captions for image review; sample endpoints do not claim continuous coverage. New outputs publish exclusively after complete validation. Actual Sonoma generation from 12 captions produced 16 nodes; original leaf text/image hashes, reconnect reads/discovery, invalid checksum/duplicate rejection and summary-only deletion passed with source/captions unchanged. Evidence: .avid-mcp-analysis/visual-summary-cee6df48-f7b8-4620-85e6-c931d8ade1f8/evidence.json.
- Visual-summary quality remains unaccepted: the generated root repeats gray-sky details, propagates a "3D image" description from the 80-second caption, omits later scenes and ends mid-sentence. Direct inspection of the retained 80-second image shows vineyard rows/tree/shadows without evidence for the 3D characterization. Checksum traceability does not establish factual entailment. Per-node visual-summary computation recovery, broader quality/resource/concurrency qualification and long-media coverage remain open.
- Visual hierarchy validation: full local check passed with 238 TypeScript tests, nine Python tests, 111 MCP tools, both transports and fresh installation/audit. Tests cover exact hierarchy/source drill-down, edited-reference invalidation and regeneration with reviewed text, malformed graphs/leaf text, scope/capability rejection, orphan discovery and checksum deletion. Previous head ee1b876 passed Windows/macOS Node 20/24, landing and CodeQL CI; new-head CI remains separate.

- Visual-summary quality diagnosis: individually inspected all 12 retained Sonoma images and added original development reference captions. A four-child hierarchy comparison separated raw versus reviewed inputs for cached Qwen3-1.7B q4 and tested reviewed inputs with DistilBART at 80/384 tokens. Qwen raw root retained only the first four scenes; reviewed root retained 11/12 but lost the sidewalk dining scene in its first parent and introduced unsupported distinct-person wording. DistilBART's larger budget produced exactly unchanged intermediate/root text. Evidence: .avid-mcp-analysis/visual-summary-quality-2faaee97-da6b-4948-89ac-4b143bbbcfb3/evidence.json. All source/image hashes unchanged; no production model/settings change. These development observations reject the proposed simple fixes and do not establish general accuracy.
- Quality research validation: script syntax passed and all four actual hierarchy conditions finished with retained inputs/intermediate outputs. Previous production head a5bbaee passed Windows/macOS Node 20/24, landing and CodeQL CI. Production code and dependencies were unchanged in this research increment.

- Optional runtime installation: explicit runtime-only setup/status now stage, audit, import-test and checksum new dependencies, verify/reuse completed installations and adopt matching legacy runtimes without npm reinstall. Locks and failed staging are retained conservatively; replacement locks and changed trees are not deleted/repaired. Actual fresh install/reuse/inference/change refusal evidence: .avid-mcp-analysis/runtime-install-proof-8b291dd2-87f9-448c-8b7f-438e34c24152/evidence.json. Existing runtime adoption also passed. Runtime update/rollback/uninstall, automatic interruption recovery, system dependencies and clean-machine/client qualification remain open.
- Offline-boundary correction: fresh inference exposed unrevisioned metadata caching inside Transformers.js dependencies. Source inspection found pipeline and tokenizer discovery paths omitting revision/cache/local-only options, so earlier unconditional cached-only claims were too broad. Normal inference now disables remote access, uses pinned local directories and directly constructs supported pipeline components. Actual CLIP text/image, both Whisper variants, DistilBART and Florence completed with zero fetch attempts under a rejecting hook: .avid-mcp-analysis/offline-models-92468052-ab87-4357-a0cc-8a2a47177c03/evidence.json. This is application-level evidence, not native packet capture; no footage upload was observed. See MODEL_RUNTIME_QUALIFICATION.md.
- Runtime validation: full local check passed with 244 TypeScript tests, nine Python tests, 111 tools, both transports and fresh installation/audit. Actual speech-auto, visual-summary and CLIP ranking regressions passed; summary quality defects remain unchanged. Setup tests cover publication/reuse, adoption, failed staging, changed trees, existing/replaced locks and manifests; loader tests enforce fixed revisions, local directories and preflight bypass.

- Client runtime configuration: CLI generation/install/update now carry explicit model, FFmpeg, ffprobe and Python paths plus validated capabilities in all five formats. Default remains inspect-only; empty/unknown capability lists, ambiguous paths and flags on unrelated setup operations are rejected. Updates reconstruct the Avid entry and preserve unrelated entries. Actual generated commands for Claude/Cursor/VS Code/LM Studio/generic indexed/read Sonoma; the generic command generated a barrel caption. Temporary install/update preserved other settings and denied captions after returning to inspect-only. Evidence: .avid-mcp-analysis/setup-runtime-3d505ec5-3923-4634-b866-0bcdc0b65781/evidence.json. Named-client UI and dependency installation remain separate open requirements.
- Client runtime validation: full local check passed with 247 TypeScript tests, nine Python tests, 111 tools, both transports and fresh installation/audit; additional focused path-list/empty-capability checks passed. Previous head bfecaba passed Windows/macOS Node 20/24, landing and CodeQL CI.

- Diarization candidate: evaluated sherpa-onnx 1.13.7 with checksum-pinned pyannote segmentation/ERes2Net models in an isolated Python environment. Original two-voice synthetic turns grouped A/B/A/B in automatic and fixed-two modes; silence produced no segments and mixed voices produced overlapping spans. Full compensated Sonoma input (190.8666875 seconds) executed in about 27–29 seconds; automatic output contained 40 anonymous labels, which are not verified speakers. Evidence: .avid-mcp-analysis/diarization-601e82eb-5f3d-407d-91b5-7507f0ac7175/evidence.json. Source/models unchanged. See DIARIZATION_RESEARCH.md for licenses, hashes and quality limits. MCP integration, transcript alignment, managed setup and broader accuracy remain open.
- Speech timing finding: uncompensated PCM extraction produced 60.304 seconds for a 60-second request and 191.5146875 seconds for the full preview request. Research timestamp compensation corrected sample-clock duration; this requires reviewing the shipping speech extractor and nonzero/delayed/discontinuous audio fixtures before claiming diarization alignment. Production extraction was not changed in this research increment.
- Diarization research validation: all eight final inference conditions completed, Python syntax/model checksum checks and pip dependency check passed. Previous production head 271eadd passed Windows/macOS Node 20/24, landing and CodeQL CI; production tools/dependencies remain unchanged.

- Speech timing repair: production transcription and language detection now compensate the source clock before range trimming, with bounded PCM reads and explicit first-audio selection. Real delayed-tone, packet-gap and Sonoma full/late-range checks passed; interior range samples matched full decoding. Evidence: .avid-mcp-analysis/speech-clock-17a3e7ba-4cc5-479f-8049-1588c029df01/evidence.json. New recipe-three runs prevent old checkpoint reuse; legacy runs remain readable but require a fresh transcription. Actual multilingual-auto cancellation/reconnect reused two of nine windows and matched uninterrupted output: .avid-mcp-analysis/speech-resume-32036e5b-65be-4be4-b9e1-8d0341ba6909/evidence.json. Full local check passed with 247 TypeScript and nine Python tests, both transports and fresh tarball install/audit. Broad timestamp conventions, recognition quality and production diarization remain open.

- Diarization runtime implementation: packaged a bounded local worker with fixed model/runtime verification, anonymous labels and automatic/supplied speaker counts. CLI explicitly installs into stable unique environments, checks dependencies and silent inference, binds a tree/worker receipt and verifies reuse without rerunning installers. Failed directories and existing/replaced locks are preserved; changed trees are refused. Actual Windows fresh install/reuse, A/B/A/B grouping, full Sonoma inference and changed-tree refusal passed: .avid-mcp-analysis/diarization-runtime-19994a23-ddde-41f4-af47-2138a523160b/evidence.json. Full local check passed with 251 TypeScript and 12 Python tests, both transports and fresh-tarball installation/audit. Persisted MCP speaker operations, review/corrections, transcript alignment, job recovery, broader accuracy, lifecycle and release dependency/license audit remain open.

- Persisted speaker MCP: added source-clock analysis, validated worker/PCM/runtime provenance, saved records, paginated spans, completed-result discovery, guarded deletion and diarization jobs. Actual Sonoma MCP cancellation before publication, fresh [60,125) analysis (14 spans), pagination, reconnect, stale-checksum/unexpected-file refusal and deletion passed with source unchanged: .avid-mcp-analysis/speaker-mcp-f8332391-5a97-446a-88bc-89a4fe3aab0d/evidence.json. Full local check passed with 255 TypeScript tests, 12 Python tests, 115 tools, both transports and fresh-tarball install/audit. Label corrections, transcript alignment, stage recovery/incomplete cleanup, concurrency and natural-dialogue accuracy remain open. Previous head 08bdc6e passed Windows/macOS Node 20/24, landing and CodeQL CI.

- Transcript/speaker overlap: added read-only checksum-selected alignment with original transcript indices/text/speaker labels, union coverage, gaps, out-of-range time, sequential/simultaneous ambiguity and explicit candidate truncation. Actual Sonoma MCP alignment covered 15 segments across five pages with inspect-only/no-model configuration; independent interval-union arithmetic matched, stale references were refused and source/transcript were unchanged: .avid-mcp-analysis/speaker-alignment-e03bb90d-246b-48dd-9ce8-ee64746f7266/evidence.json. Full local check passed: 260 TypeScript tests, 12 Python tests, 116 tools, both transports and fresh package installation/audit. This is segment overlap, not verified word/speaker assignment. Corrections, reviewed assignment, broader accuracy and recovery remain open. Previous head 4199534 passed Windows/macOS Node 20/24, landing and CodeQL CI.

- Explicit transcript speaker assignments: added immutable child revisions with selected overlapping labels, optional caller display names, explicit ambiguity/partial-range flags and persisted checksum-bound decision provenance. Added paginated provenance reads compatible with ordinary older revisions. Actual MCP assigned five Sonoma machine-transcript segments, preserved all text/timing and parent/source/analysis hashes, then verified reconnect, three-page provenance retrieval and inspect-only write refusal: .avid-mcp-analysis/speaker-assignments-73abe2fc-d68e-4e0e-bdc6-82bd9204ffff/evidence.json. Full local checks: 262 TypeScript tests, 12 Python tests, 118 tools, both transports and fresh install/audit. Underlying diarization cluster/boundary corrections, word alignment, accuracy and recovery remain open; caller-selected names do not verify identities.

- Speaker interval/cluster corrections: added ordered boundary/relabel/remove/add/merge edits as immutable child analyses with retained model output, parent checksum and independent PCM copies. Effective/machine views distinguish corrections; alignment and assignments consume the selected effective view. Actual Sonoma MCP edits, merge, corrected alignment, reconnect after intermediate-parent deletion, stale/capability refusals and original/source preservation passed: .avid-mcp-analysis/speaker-corrections-9985cde4-ae92-4cb1-bca5-a150e339b958/evidence.json. Full checks passed (264 TypeScript, 12 Python, 119 tools, both transports, fresh install/audit); the complete TypeScript suite passed with 266 after two additional edit-limit regressions. Accuracy, word alignment, recovery/cleanup and concurrency remain open. Previous head 288ee3b passed Windows/macOS Node 20/24, landing and CodeQL CI.

- Diarization dependency audit and bootstrap repair: exact installed-distribution queries found seven distinct CVEs listed for inherited pip 23.2.1. Fresh setup now creates a pip-free venv and executes only a size/SHA-pinned pip 26.2.1 wheel, records its version and refuses silent legacy-bootstrap reuse while retaining existing files. Fresh four-package PyPI audit returned no active entries; native component advisories and core-wheel missing notices remain separate open audit work. Evidence: .avid-mcp-analysis/diarization-audit-6d0f7b0d-00c1-4754-822b-7f5a50a42bb2/evidence.json. New runtime A/B/A/B/full-Sonoma/reuse/change-refusal regression passed: .avid-mcp-analysis/diarization-runtime-607758fe-35dd-4508-ab4e-09fbb37d203a/evidence.json. Full check passed with 268 TypeScript tests, 12 Python tests, 119 tools, both transports and fresh package install/audit. See DIARIZATION_DEPENDENCY_AUDIT.md; this does not close native-library security, redistribution or lifecycle qualification.

- Native diarization audit: imported version APIs report sherpa 1.13.7 / commit 917bed95c8e5c7c18aa4d69fea42e9ef8ef0a60e and ONNX Runtime 1.27.1; NumPy build metadata reports OpenBLAS 0.3.29. Hashed 28 wheel-owned DLL/PYD entries, captured 15 exact source/notice files and queried four resolved source commits in OSV (zero entries, not complete coverage). ONNX Runtime's exact PyPI request returned 404 and remains unavailable. Specific binary phonemizer strings plus the source recipe point to an eSpeak fork carrying GPLv3 terms; broader link/notice/source attribution remains open. Evidence: .avid-mcp-analysis/diarization-native-267d843a-0b17-4b73-9e5b-558aada73339/evidence.json; target tree unchanged. See DIARIZATION_NATIVE_AUDIT.md. This increment changes research/docs only and does not close native security or redistribution gates.

- Transcript lock ownership: correction/deletion now record a unique owner and check file identity plus bounded owner bytes before mutation and cleanup. Detected replacement locks are preserved; errors after publication explicitly require inspecting revisions before retrying. Tests cover replacement before either mutation, replacement after publication and competing writer instances. Actual five-segment Sonoma MCP assignment/reconnect/provenance regression passed with originals unchanged: .avid-mcp-analysis/speaker-assignments-be4dd676-c2b2-4c4d-acaf-3e0bd946906a/evidence.json. Full check passed with 272 TypeScript tests, 12 Python tests, 119 tools, both transports and fresh-tarball installation/audit. This is cooperating-writer coordination, not an atomic filesystem transaction; broad concurrency and recovery remain open. Three legacy Windows-encoded punctuation bytes in LOCAL_SETUP.md were repaired to UTF-8.

- Transcript lock portability follow-up: head 5c8fb41 failed the replacement-after-publication regression on Windows Node 20 (the other three platform/version jobs passed). The lock descriptor now closes immediately after writing its ownership record, before invoking the operation, avoiding an open delete-pending file during replacement on Windows. File identity and owner checks remain before mutation and cleanup. This corrects the earlier cross-platform verification gap; refreshed CI is required.

- Speaker discovery failure isolation: malformed or identity-mismatched records no longer hide healthy results. Library-wide diagnostics after the cursor count unpublished/unclassified candidates and retain at most 20 sorted unclassified IDs without file contents. Missing records do not establish worker termination; all files remain intact. Tests cover 22 corrupt/mismatched records, an unpublished directory, invalid directory names, bounded diagnostics and cursor behavior. Actual inspect-only Sonoma MCP discovery/reconnect preserved every source/artifact hash: .avid-mcp-analysis/speaker-discovery-ceffaac8-5783-42c7-9115-24746bae73b2/evidence.json. Full checks passed with 273 TypeScript tests, 12 Python tests, 119 tools, both transports and fresh package install/audit. Computation recovery and incomplete cleanup remain open. Previous head 187c4b1 passed all Windows/macOS Node 20/24 jobs, landing and CodeQL.

- Speaker audio recovery: new runs publish bounded source/range/options/runtime/worker/PCM checkpoints after extraction. Added checkpoint verification and checksum-selected resume tools plus diarization_resume jobs. Resume copies verified PCM into a new run, persists parent provenance and reruns full inference; published parents, changed inputs/runtime and insufficient permissions are refused. Parent files are retained; no worker termination is inferred. Discovery exposes bounded unpublished candidate IDs, and completed deletion accepts verified checkpoints while preserving legacy records. Actual 180-second Sonoma MCP cancellation after publication, observed terminal job, reconnect and resume with FFmpeg deliberately unavailable matched uninterrupted machine output exactly: .avid-mcp-analysis/speaker-recovery-cb6fae17-4aba-412d-bbe5-08e494c97a89/evidence.json. Source and parent hashes unchanged. Full checks passed with 275 TypeScript tests, 12 Python tests, 121 tools, both transports and fresh package installation/audit. Model-internal checkpoints, incomplete cleanup, concurrent lifecycle and accuracy remain open. Previous head 110e404 passed Windows/macOS Node 20/24, landing and CodeQL.

- Speaker checkpoint validation: persisted options now require explicit count/threshold fields, while request defaults remain unchanged. Extraction, checkpoint reads/reuse and saved-result reads reject nonfinite float32 PCM before native inference. Nonfinite media duration in saved source metadata is also refused. Regression tests cover missing fields, matching-hash NaN/positive/negative infinity, refusal before checkpoint publication/inference and retained source. Actual inspect-only MCP validation against a copied Sonoma checkpoint refused all five malformed cases and accepted the restored copy: .avid-mcp-analysis/speaker-checkpoint-validation-88a31c8a-08b9-4a8c-8244-0a658dad82f1/evidence.json. Original source/parent unchanged. Full local checks passed with 277 TypeScript tests, 12 Python tests, 121 tools, both transports and fresh package installation/audit. This is structural validation, not authenticity or model accuracy; full recovery/cleanup and qualification scope remains open.

- Process lifecycle prerequisite for cleanup: command timeout/output overflow now retain their first failure, stop collecting output, request termination and settle only after direct-child closure. Forced termination is requested after one second; live-process errors do not prove exit, while missing-PID spawn failures settle immediately. Deterministic lifecycle tests cover delayed close, kill errors, escalation, overflow and spawn/runtime failures. Real Windows direct-child timeout/overflow tests verified ESRCH at return and no subsequent file writes: .avid-mcp-analysis/process-close-430301b3-f981-444c-b444-383bfac02bb8/evidence.json. Actual Sonoma cancellation/reconnect/audio-resume remained an exact machine-output match: .avid-mcp-analysis/speaker-recovery-9b1b4b10-458f-4981-9fe2-4e8943549128/evidence.json. Full local checks passed with 281 TypeScript tests, 12 Python tests, 121 tools, both transports and fresh package install/audit. This does not establish descendant-tree termination or authorize cleanup against other writers; incomplete-artifact cleanup remains open. Previous head a286a1a passed Windows/macOS Node 20/24, landing and CodeQL.

- Detached-process research: actual Windows wrapper tests found ordinary descendants absent after timeout/output overflow, but detached descendants remained alive and continued writing in both cases. All owned fixtures then exited through unique cooperative stop files and PID absence was verified. Evidence: .avid-mcp-analysis/process-descendants-dad1b668-d9d1-41f7-a1d5-a5fea62dc3ac/evidence.json. See PROCESS_TREE_QUALIFICATION.md. This contradicts any inference that direct-child closure proves all writers stopped and establishes a concrete prerequisite for incomplete-artifact cleanup. Research/docs only; no process-tree termination guarantee added.

- Windows process-tree termination: failed commands now request taskkill /T /F against their still-live owned parent before direct-child fallback and wait for both taskkill and parent closure. Error details preserve failed/uncertain tree termination; observed exited parents are not targeted. Unit tests cover command ownership, helper timeout/start failure, delayed parent/helper completion and fallback uncertainty. Actual ordinary/detached timeout/overflow descendants were absent at return with no continued writes in all four cases: .avid-mcp-analysis/process-descendants-faaa2251-dcbf-4a25-89e4-67a2385ca1cd/evidence.json. The strengthened --require-stopped regression passed. Actual Sonoma recovery retained exact baseline machine output: .avid-mcp-analysis/speaker-recovery-75401d47-982a-40cf-8f12-6429a382f056/evidence.json. Full local checks passed with 286 TypeScript tests, 12 Python tests, 121 tools, both transports and fresh package installation/audit. Non-Windows tree termination, already-exited/reparented workers, successful-wrapper exit and incomplete cleanup remain unqualified. Previous head 4460022 passed Windows/macOS Node 20/24, landing and CodeQL.

- Guarded incomplete speaker cleanup: new runs bind owner metadata into audio checkpoints. Windows cleanup requires the recorded PID absent, no observed directory-referencing process, available relevant command lines, a valid source/checkpoint and exactly three direct regular files. Published/changed/active/unexpected/ownerless candidates are refused. Validated directories are renamed inside the library, rechecked, then deleted file by file; failures retain remaining quarantine files. Actual Sonoma MCP cancellation/resume followed by unexpected-file refusal and successful stopped-parent cleanup passed: .avid-mcp-analysis/speaker-recovery-e812a246-74e1-4f9d-97b7-3b432dcd12e3/evidence.json. The parent hash-preservation assertion is before cleanup; cleanup then removed that parent, while source and resumed child stayed intact and child machine output still matched baseline. Full local checks passed with 292 TypeScript tests, 12 Python tests, 122 tools, both transports and fresh package install/audit. Tests cover actual live-owner refusal, missing process access, metadata/hash/file changes and retained quarantine on post-move failure. Ownerless/extraction-partial runs, interrupted-cleanup recovery, arbitrary-writer races and non-Windows cleanup remain open. Previous head 7d8cf4a passed all Windows/macOS Node 20/24 jobs, landing and CodeQL.

- Intact speaker-cleanup recovery: added checksum-selected Windows restoration of quarantined audio/owner/checkpoint files after validating direct directory/files, source bounds, bound hashes, absent destination and stopped processes. Partial/changed/unauthorized/active/colliding candidates are refused; post-move verification errors explicitly identify the restored destination. Unit tests restore a quarantine produced by an injected cleanup failure and cover all refusal paths plus source change after checks. Actual MCP restoration of a simulated intact directory copied from Sonoma analysis, collision refusal and subsequent cleanup passed with source/original hashes unchanged: .avid-mcp-analysis/cleanup-recovery-36c6796a-23be-4007-97d5-cd7cb20cb144/evidence.json. Full local checks passed with 293 TypeScript tests, 12 Python tests, 123 tools, both transports and fresh package installation/audit. Partial-deletion/power-loss recovery, legacy unowned runs and arbitrary-writer races remain open. Previous head 5c89515 passed Windows/macOS Node 20/24, landing and CodeQL.

- Native host availability refresh: Media Composer is now observed open in the disposable Sonoma project, so the earlier activation-blocked snapshot is no longer current. Actual inspect-only MCP reads and saved-bin timeline assertions passed: .avid-mcp-analysis/native-refresh-d3ed82a6-a27c-426d-bda4-3f34744d4ca6/evidence.json. The saved 120-frame 30 fps composition retains both 60-frame cuts at source starts 2850/3300 on V1/A1/A2; bin/source hashes unchanged. Computer use observed a nonblack monitor image and playhead advance but unchanged captured imagery, so visual/audio fidelity remains unverified. No full restart was performed in this refresh. Actual bin inspection exposed and repaired Unicode JSON emission under Windows legacy stdout; success/error regression tests preserve Unicode through ASCII-escaped JSON. Full local checks passed with 293 TypeScript tests, 14 Python tests, 123 tools, both transports and fresh package install/audit. Previous head c31114b passed all Windows/macOS Node 20/24 jobs, landing and CodeQL.

- Fresh native render and frame timing: real MCP preview/apply exported the disposable 120-frame Sonoma sequence, verified full decode and refused replay. The complete decoded video/audio checksum listing exactly matches the earlier export. New read-only diagnostic comparison covers all 120 frames and both sides of the cut: each best correlation match is nominal source index minus one, while actual presentation timestamps agree with intended source times within 0.000000334 seconds, consistent with the source's 0.033333-second video start. Source/render hashes unchanged. Evidence and interpretation are in NATIVE_RENDER_QUALIFICATION.md and .avid-mcp-analysis/native-render-mcp-109ad870-3519-4218-ac27-8aa745392be7/. Mono output, level differences and perceptual sync remain unresolved; this is diagnostic timing evidence, not complete fidelity qualification. Research-script execution and git diff checks passed; no production code changed. Previous head cd659d2 passed Windows/macOS Node 20/24, landing and CodeQL.
- Stereo/legal-range native qualification: computer use saved a separate named preset with Stereo and Keep as Legal Range; actual MCP export passed the two-channel PCM contract, all 120 decoded video frames and token replay refusal. Complete frame comparison still supports source-clock cut timing; diagnostic best-match RGB RMSE improved from 19.34 to 10.94 but level differences remain. New per-cut source-clock audio comparison proves the output channels are identical while source channels differ, and correlations below 0.12 within +/-100ms do not support audio preservation. AAF descriptors still reference the original MP4. Evidence: .avid-mcp-analysis/native-render-mcp-efb15996-6232-409c-8bec-0ff94bedb4c9/ and NATIVE_RENDER_QUALIFICATION.md. Original preset/source media retained; no automatic timing/gain correction. Real research scripts and syntax/diff checks passed; no production code changed. Previous head d60f6c0 passed Windows/macOS Node 20/24, landing and CodeQL. Direct Out/mixing, wider audio timing, color conformance and perceptual sync remain open.
- Audio-clock diagnosis and source preparation: the new FFT research comparison searches every sample within +/-2 seconds with deterministic offset/gain/boundary/constant-input self-tests. Both two-second rendered cuts correlate above 0.999998 with averaged source audio, but at different presentation-clock offsets (-297.3125ms and -413.3333ms); both share +1024 samples on the continuous decoded-sample clock and gain about 1.41426. This supports timestamp handling and mixing differences, not a single timeline-slip fix. A separate source-clock stereo PCM MOV now preserves original compressed video essence/metadata, matches normalized audio exactly, and has contiguous audio packets; source unchanged. Evidence: stereo export audio-clocks-4641e532-e6cd-4897-b9a1-e0e475ef24c1 and .avid-mcp-analysis/sonoma-source-clock-857e680b-48a7-4dc9-a52e-478f864ef2b9/. Research execution, self-tests and syntax/diff checks passed. The prepared asset still requires native link/sequence/render, Direct Out or panning, and source-clock/audio/color conformance checks; no automatic timeline or relink change was made.
- Prepared PCM native link/reopen: actual stdio MCP created a unique empty bin, linked the verified source-clock MOV once, read the master, closed/saved/reopened the bin and confirmed stable master identity. Prepared media and original selects-bin hashes unchanged. Evidence: .avid-mcp-analysis/native-pcm-link-4f20d2e6-cab0-41e9-8c6a-a25528ee2898/. Avid reports stereo PCM 48kHz/24-bit and an automatic full-range-to-video-level color transform, but conflicting Image Size versus Raster/Format geometry fields. These are host declarations; decoded geometry and source-clock fidelity remain unproven. The new master has not yet been sequenced/rendered. Native execution and script syntax/diff checks passed; no production code changed.
- PCM AAF sequence and exact rendered audio: exported the new linked master into a stable source-checked reference AAF, built two 60-frame/three-slot cuts through the shipped MCP builder, imported once into a fresh locked/journaled native bin, closed/reopened and rendered through MCP preview/apply. Full 120-frame decode/replay refusal passed. The complete rendered 24-bit stereo PCM hashes exactly match the original source-clock cuts, corresponding channels measure zero lag/unity gain and remain distinct; the previous dual-mono render fails the same strict check. Video source timing still matches, while color differences persist. Saved-bin sound nodes are opaque TKFX, leaving stereo timeline parsing open. Evidence paths and identities are in NATIVE_RENDER_QUALIFICATION.md, especially .avid-mcp-analysis/native-render-mcp-e0f60e5d-67c3-49ac-9ba0-7de71d73453c/. Native/MCP research scripts, positive/negative audio checks and syntax/diff checks passed; no production code changed. Prior head b13dcd8 passed Windows/macOS Node 20/24, landing and CodeQL. Broader ingest/codec/rate, source geometry, color and perceptual qualification remain open.
- Saved stereo channel-combiner parsing: recognized two-child same-rate direct-source EFF2_AUDIO_CHANNEL_COMBINER nodes now expose overlapping channel-labeled source references, with subclip clipping and traversal limits. Unknown/reversed/parameterized/malformed variants remain opaque. Snapshot schema preserves labels through pagination, usage and semantic diffs. Actual stdio MCP against the saved PCM Avid bin verified both channels across the cut and all four audio uses, with bin hash unchanged: .avid-mcp-analysis/stereo-timeline-476d5e14-dece-4037-b38a-5480fd9537bd/evidence.json. Full checks passed with 294 TypeScript tests, 16 Python tests, 123 tools, both transports and fresh package install. This closes this observed combiner's source mapping, not arbitrary TKFX/nested/retime or unsaved/live graph support. See SAVED_STEREO_TIMELINE.md.
- Native range-tag diagnosis: source probe confirms 1280x720 full-range BT.709; the 1920x1080 PCM-sequence render is tagged limited range. A read-only forced-full decode reduces matched-frame RGB RMSE from 10.9395 to 0.8449 over all 120 frames, supporting a range-declaration mismatch. A new checksum-selected copy changes H.264/container range declarations without re-encoding; encoded picture-unit/audio hashes and original render remain unchanged. Ordinary decode of that copy reproduces the low error and exact source-clock stereo PCM still passes. Evidence: .avid-mcp-analysis/render-range-tags-7a26beb3-2085-44f1-97f5-bdb413f52ace/. Scripts, real positive media checks and syntax/diff checks passed; no production code changed. This is a qualified fixture correction, not a general preset/codec override or full-resolution color certification. Prior head b1b7613 passed all Windows/macOS Node 20/24, landing and CodeQL.
- Explicit native color-tag contracts: optional range plus exact space/transfer/primaries declarations now reject missing or mismatched requested tags and report colorTagsChecked separately from source/pixel fidelity. Existing callers remain valid. Actual MCP PCM-sequence export passed requested limited-range BT.709 tags, all 120 decoded frames and token replay refusal; existing native/corrected files passed respective limited/full contracts and the wrong full-range contract was refused. Evidence: .avid-mcp-analysis/native-render-mcp-475a5163-efeb-4a4c-af6c-c1fac61885cd/ and .avid-mcp-analysis/render-color-contract-0baf0f72-67f0-4e34-bd25-56a14a3568fa/. Real testing exposed a final-probe timeout masking the observed mismatch; the verifier now retains the mismatch and timeout cause. Full checks passed with 298 TypeScript tests, 16 Python tests, 123 tools, both transports and fresh package install. No automatic color correction or general color-conformance claim is introduced.
- Full-raster color/detail evidence: new read-only research script compares all 120 1920x1080 RGB frames against presentation-time-matched source frames upscaled with Lanczos, retaining per-frame SSIM/MSE, aggregate PSNR, commands and logs. The separate tagged copy improves mean SSIM from 0.784061 to 0.962604 and PSNR from 27.2113 to 35.4678 dB; both cut-boundary frames improve. Full-raster residual remains and may include compression/conversion/scaling-kernel differences; no universal color-quality threshold is claimed. All three input hashes unchanged. Evidence: .avid-mcp-analysis/full-resolution-08fa8012-6c23-4dbb-b6c1-921bb0522ecb/. Actual script execution, 120-record checks and syntax/diff checks passed; no production code changed. General color qualification, perceptual review and broader host/media scope remain open.
- Explicit native stream-start contracts: optional videoStartTime and per-audio startTime now require observed presentation starts within one microsecond, refusing missing/null/empty/unknown declarations while preserving callers that omit them. A generated four-second fixture with a 250ms audio delay passes the old duration contract and its declared delay but fails an explicit zero-start contract. The checksum-selected Sonoma PCM native render passes zero starts and complete decode with its file unchanged. Evidence: .avid-mcp-analysis/render-start-times-864da221-7faf-4ee0-8cfb-8431d1d1b32e/evidence.json. Full local checks passed with 300 TypeScript tests, 16 Python tests, 123 tools, both transports and fresh package installation. This checks stream declarations, not packet continuity, source alignment or perceptual sync. Prior head 74176b6 passed Windows/macOS Node 20/24, landing and CodeQL CI.
- Reusable AAF selects inspection: new avid_inspect_aaf_selects tool exposes one same-rate direct-master composition's per-track ranges plus AAF/local-media checksums, rejecting unsupported structures and out-of-scope/network locators. Actual stdio MCP recovered all three PCM Sonoma tracks and two cuts per track, preserved input/media hashes and rejected the master-only reference AAF. Evidence: .avid-mcp-analysis/aaf-selects-inspection-16213c99-8fd7-4c1d-89b2-1ca5122e54e7/evidence.json. Generated malformed-file tests cover rates, origins, fillers, source bounds, sequence duration, extra compositions and missing slots. Full checks passed with 301 TypeScript tests, 18 Python tests, 124 tools, both transports and fresh package installation after updating expected tool counts. This prepares the native import adapter; import remains research-only and downstream source-mob/descriptor semantics and live conformance remain open. See AAF_SELECTS_INSPECTION.md. Prior head e217167 passed Windows/macOS Node 20/24, landing and CodeQL CI.

- Guarded native AAF import: import_aaf_selects now uses structural/source inspection, an explicit AAF checksum, empty-bin and 30 fps checks, preset discovery, owner/project/bin/media state binding and a single-use preview/apply token. Native ImportFile dispatch is journaled once; post-dispatch RPC/metadata/source uncertainty retains an import-specific native lock and cannot use export recovery. Actual MCP created a disposable bin, refused wrong checksum/replay/occupied-bin attempts, imported the Sonoma PCM composition, saved/reopened stable identity and verified all six saved video/stereo source references. Evidence: .avid-mcp-analysis/native-aaf-import-mcp-f4ee1204-197e-406e-a665-1984bf55e00a/. Subsequent native MCP export decoded all 120 frames, preserved exact source-clock stereo PCM and matched the prior decoded video checksum; the known color discrepancy remains. Render evidence: .avid-mcp-analysis/native-render-mcp-a50e945b-8be7-4294-86ab-c5723868293f/. Full checks passed with 314 TypeScript tests, 18 Python tests, 124 tools, both transports and fresh installation. Native import-specific recovery/undo, broader rates/media, downstream descriptors and full fidelity remain open. See NATIVE_AAF_IMPORT.md.
- Native import lock recovery and host restart: new avid_recover_native_import_lock requires edit/export and both the retained-lock checksum and a current attempt/bin/source evidence checksum. Status exposes original/current source hashes and changes for explicit review. Recovery serializes, checks the qualified Avid host stopped twice, rechecks evidence, archives it and releases only the lock; import/bin/media are not retried, undone or modified. Actual isolated MCP lock fixtures verified running-host refusal and stopped-host release after normal Avid UI exit, preserving real Sonoma evidence/files: import-recovery-7b57b07c-a81c-4bcf-9b7a-475867ab24ee and import-recovery-6ccf2487-7db1-4f4d-a023-06d511c5abd2. UI relaunch continued the existing trial and reopened Sonoma, followed by successful actual MCP/native saved-timeline checks in native-refresh-020714cc-e2b5-45fe-8d03-94052c2c5c1a. This supersedes the activation-blocked startup snapshot for this host. Full checks passed with 318 TypeScript tests, 18 Python tests, 125 tools, both transports and fresh installation. Prior head a622a62 passed Windows/macOS Node 20/24, landing and CodeQL CI. Arbitrary concurrent writers, missing evidence, power-loss recovery and per-action undo remain open.
- Post-restart export regression: actual MCP in the new Avid process exported the existing imported sequence, decoded 120 frames, matched explicit start/color declarations and refused replay. Complete stereo PCM again matched source-clock cuts exactly, and the decoded video checksum matched the pre-restart render. Saved imported-bin hash and six source references were unchanged. Evidence: native-render-mcp-7e4494af-238a-459f-8fd5-1832ea123316 and ranges-996377f5-820f-4ef2-ac31-284d3db12629 under the original native import directory. The known color error remains, and crash/OS/per-action restart scope remains open. Replaced the stale implementation-status summary (77 tools, no diarization/no PR) with current feature/evidence boundaries and the verified 318-TypeScript/18-Python/125-tool local check. Documentation links and diff checks passed; no production code changed or full package checks repeated in this increment.
- Native source-master AAF export: export_aaf_master now binds a linked 30 fps master's native file/path metadata to an explicit source checksum, records one guarded ExportFile attempt and verifies stable compound-file output, one master, local source identity/hash and same-rate whole-source slot lengths. Uncertainty retains the native export lock; scoped recovery recognizes the reference.aaf/action pair. Actual MCP exported the Sonoma PCM master, refused wrong checksum/replay, consumed the returned reference through the AAF builder and re-inspected all three tracks/two cuts, preserving source/bin and releasing the lock. Evidence: .avid-mcp-analysis/native-aaf-master-mcp-f6012198-7bad-489d-9d85-f4968f0fdcf9/. Full checks passed with 330 TypeScript tests, 18 Python tests, 125 tools, both transports and fresh package install. This removes the fixture-specific reference-export dependency for the supported master subset; general descriptors, multiple sources/rates, audio-only/sequence AAF export and complete fidelity remain open. See NATIVE_AAF_EXPORT.md.
- Chained AAF workflow exposed stereo regression: the newly exported reference and builder output imported, survived save/reopen and retained six correct source references, but used two separate audio tracks. The initial research assertion assumed the earlier combiner layout and stopped; a journal-selected continuation rendered that completed import without repeating it. Strict audio comparison failed with identical output channels. A same-reference/cut follow-up using V1/A1/A2 names still rendered dual mono, ruling out that proposed remedy for this fixture. Evidence: aaf-workflow-mcp-cbe5901f-91b7-4b74-8109-d669dff04bb6 and aaf-workflow-mcp-48ec76c7-1219-41a9-bebe-225198363849. Earlier exact-stereo fixture evidence remains narrower than this workflow. Updated bundled selects/export skills and current status to preserve this limit; fresh installation validated all five skills and their tool references. CI for bfaff54 exposed noncanonical temp paths in the mocked AAF verifier fixture; matching the real inspector's canonical paths repairs the fixture and all nine focused tests pass locally. New CI confirmation remains pending. See AAF_WORKFLOW_QUALIFICATION.md; import grouping/panning investigation remains open.
- Older-reference import control: computer use inspected current import multichannel settings (None; automatic center panning off), then canceled without changes. A checksum-selected source-contract inspector found differing saved audio-rate/length metadata and source-slot IDs, despite matching PCM descriptors. Rebuilding the same cuts from the older reference under the current host state still saved separate audio tracks and rendered dual mono; strict stereo failed while source ranges, full decode, replay refusal and preserved hashes passed. Evidence: aaf-source-contracts-ab62dd0c-b0a2-47af-a525-28d46d798cba and aaf-workflow-mcp-ddf01099-1a1f-4217-bff9-253e9dbfce72. Reference differences alone do not explain the failure; no speculative preset/AAF correction adopted. Research execution, script syntax and diff checks passed. Prior head a68b1ca now passes all Windows/macOS Node 20/24, landing and CodeQL CI. See AAF_WORKFLOW_QUALIFICATION.md.
- Native stereo AAF structure: current saved-bin checksum differed from its old receipt, so export stopped before dispatch. Reinspection retained the same stereo graph; a checksum-bound one-shot research export preserved current bin/media hashes. The exported composition exposes one _TRACK_FORMAT=2 sound track with two-input Audio Channel Combiner operations, giving an explicit authoring target absent from the builder. Added an exact-artifact inspector; independently authored import/render remains unverified. Evidence: native-pcm-aaf-3fb9b57b-f003-410c-88e4-30d808dbd42d. CI on 45f4744 hit five-second timeouts in caption/people filesystem tests; only those test limits increased to 20 seconds, with all ten focused tests passing locally. See AAF_WORKFLOW_QUALIFICATION.md.
- Explicit stereo AAF authoring: sound tracks can request channels=2 and map each select to two distinct source slots. The builder authors the observed Audio Channel Combiner and stereo track format, reopens and verifies mappings; the inspector exposes overlapping channel-labeled cuts and rejects unsupported effects/parameters or unsynchronized inputs. Fresh real MCP build/import/save/reopen/render from the newer reference retained both combiners and all six ranges, matched complete source-clock stereo PCM exactly, and preserved distinct channels. Decoded video checksum matches the earlier PCM fixture; color discrepancy remains. Evidence: aaf-workflow-mcp-51c29267-c57b-4f38-bf30-eb9952868aba. Full check passed: 331 TypeScript, 21 Python tests, 125 tools, both transports and fresh installation; log check-explicit-stereo.log. Updated API guide, skill and status. Broader media/rates/builds, color, relink and undo remain open.
- AAF source-modifier review: composition inspection previously ignored source-clip fade/channel-remapping properties. Both direct cuts and stereo inputs now reject properties outside the supported direct-reference structure. Ten mutation variants cover fade-in/out, ChannelIDs, MonoSourceSlotIDs and SubclipFullLength on independent/stereo inputs, preserving rejected files. All eight focused AAF Python tests passed. Reinspection of the real explicit-stereo selects (SHA fef323a7bdee947ef1476d7bad45b5f708137d4b9a7ad9495be7210858059cad) retained both channel ranges and left the artifact unchanged. This tightens inspection; no host or media mutation was needed.

- AAF modifier full validation passed: 331 TypeScript tests, 22 Python tests, 125 tools, both MCP transports and fresh package installation. Log: .avid-mcp-analysis/check-aaf-modifiers.log. Native stereo artifact reinspection is separate from the previous real import/render evidence; no additional native render is claimed.
- Source-clock preparation MCP: promoted the fixed PCM research recipe to avid_prepare_source_clock_media with explicit absolute stream selection, checksum/path/capability checks, 600-second/4-GiB source bounds, unique outputs and retained failure records. Verification covers video declarations/essence, normalized PCM, zero-origin packet continuity and source/output hashes. Real Sonoma MCP rejected a wrong checksum and produced a MOV byte-identical to the earlier native-qualified research asset (f46de96396ec30be8d41ff3c2f7d8aaf08ba190cdb2295e863ce535e7965bbeb), at a new unlinked path. Evidence: source-clock-mcp-166f5c69-55f8-41ea-8c91-5d349e9a34c1. The first run retained an unverified copy when MOV preserved a tmcd stream; verification now allows only matching source timecode. Added six boundary/failure tests and updated tool inventory to 126. Native use of the new path and broader media/job recovery remain open.

- Source-clock full validation passed: 337 TypeScript tests, 22 Python tests, 126 tools, both transports and fresh tarball installation with five skills. Log: .avid-mcp-analysis/check-source-clock-tool-final.log. Earlier failed inventory/worker diagnostics were retained; the final complete check exited successfully.
- Prepared-path native chain: the newly MCP-generated MOV was linked once in a new bin, saved/reopened, exported to a source-path-bound reference, authored as explicit stereo, imported into MCP_Workflow_8bd26857.avb, saved/reopened and rendered through shipped MCP tools. All six source references, token replay refusals and selected preserved hashes passed. Complete stereo PCM matches exactly with distinct channels; all 120 source presentation times and the prior decoded-video checksum match. The existing color discrepancy remains. Evidence: aaf-workflow-mcp-b53e7873-8af9-4f43-9b86-c7e2039c0d6f. Research syntax/diff checks passed; no production code changed. Prior dc8ed02 passed Windows/macOS Node 20/24, CodeQL and landing CI. Updated SOURCE_CLOCK_PREPARATION.md with native evidence and official FFmpeg recipe semantics; broader media/builds, color, relink and undo remain open.
- Preparation output-size control: FFmpeg now receives the 4-GiB muxer limit during conversion, alongside existing timeout/final-size/full-essence checks. A real 1-MiB-cap experiment returned exit zero with 1,077,432 bytes and only 69/5,725 frames, confirming that the approximate muxer cap cannot prove successful preparation. Added truncation rejection coverage that requires a failure record and no receipt. Normal real MCP Sonoma preparation still matched the prior video/PCM hashes; evidence source-clock-mcp-0056ddd3-4be6-43bd-a065-09fe517f16a6. Limit experiment: preparation-size-limit-2f46ebda-6d57-42c4-937e-466da4726036. Original source preserved.
- Preparation size-control full validation passed: 337 TypeScript tests, 22 Python tests, 126 tools, five bundled skills, both transports and fresh tarball installation. Log: .avid-mcp-analysis/check-preparation-size-limit.log. The muxer cap remains approximate; final size and complete essence verification are required. No new native render is claimed for this safeguard increment.
- Seven-file preparation matrix: real shipped stdio MCP successfully prepared all seven original Sonoma MP4s, including two 4K exports and the 2.68-GB asset. Complete video/normalized PCM verification, zero-origin packet continuity, independent output hashes and original source preservation passed. Coverage includes three video start offsets and fractional average frame rates; native AAF/import support at those rates is not implied. Evidence: .avid-mcp-analysis/source-clock-matrix-12cf27ec-9e9e-4b5e-bdd5-a8e98acc277c/evidence.json. Added repeatable serial research harness and documented scope. Real execution, syntax and diff checks passed; production code unchanged, so no additional full-suite run or native render is claimed.
- Server dependency audit: all 98 installed non-development package versions match the branch lock; all declare licenses and local manifest/notice hashes are recorded. The @posthog/core package lacks a top-level matching notice file, retained as a review gap. Live npm audit reports zero vulnerabilities; five GitHub default-branch alerts concern fast-uri/qs, while this branch locks versions outside their reported ranges. Evidence: server-dependency-audit-4d975931-030f-4c6d-8e17-2b6f63f4aa84. Added repeatable read-only inventory and SERVER_DEPENDENCY_AUDIT.md. Script execution and diff checks passed; no dependency, production code, alert state or release changed. Optional runtime/model/distribution review remains open.
- PostHog notice follow-up: resolved core 1.48.1 tag to immutable upstream commit, retained root multi-license text and manifests, and proved installed UUID vendor source matches by SHA-256. MIT package metadata coexists with an Apache-2.0 component annotation; posthog-node also includes Apache text despite MIT metadata. Added pinned evidence harness and documented why declarations cannot substitute for component notices. Evidence: posthog-notices-2a2fa97f-f09a-4edc-a777-cb7c491a81d5. Real read-only execution and diff checks passed. No dependencies changed; complete attribution/distribution review remains open.
- Video packet-clock verification: preparation now compares all selected video packet PTS/DTS/durations, count-bound to declared frames, retaining negative decode times and reordered presentation times. Missing/changed timing refuses a receipt despite identical compressed bytes. Seven real Sonoma MP4s passed with zero measured clock differences, preserved source hashes and existing essence/PCM checks: source-clock-matrix-4e14318f-f114-4167-93b4-cd4f7c072f1e. Full check passed with 338 TypeScript tests, 22 Python tests, 126 tools, both transports and fresh installation; check-video-packet-clock.log. Native color and broader host qualification remain open.
- Preparation preflight correction: missing/nonnumeric geometry previously passed a less-than-or-equal comparison against NaN. Geometry and frame counts now require positive safe integers; the existing 100000-frame verification ceiling is enforced before conversion. Regression variants cover missing/null/nonnumeric/infinite/fractional/unsafe values for width, height and count. Seven focused tests and typecheck passed. No new full-suite or native execution claim; prior seven-media timing evidence remains separate.
- Multi-master AAF coverage: added a three-cut A/B/A stereo stringout fixture with different source slot IDs and deliberately reversed second-master channel mapping. Reopened inspection preserves every master identity, channel, source start, duration and cumulative position. A 24-fps second master in the 30-fps request is refused before output creation; source hashes remain unchanged. All nine AAF builder Python tests passed. This qualifies offline composition structure only; multi-source native import/render and real media linkage remain open.
- Cross-file AAF reference prerequisite: checksum-selected old/new native exports contain overlapping source identities, including an audio source whose slot numbering differs. The new research copy harness refuses the collision before writing a combined AAF and retains per-file conflicting source slot/rate/length evidence; both source hashes remain unchanged. Evidence: aaf-reference-copy-655d5115-fd61-4ba0-be03-dfe1141545d6. The research command exits nonzero as intended for this unresolved fixture. This rules out blind identity deduplication and leaves graph conflict handling plus independent-media native qualification open; no production merger was added.
- AAF identity control with separately linked prepared path: --prepared-reference selects the checksum-verified native export from the newer prepared-media bin. It still shares three source IDs with the older reference despite distinct locators. Unlike the first pair, the recorded slot summaries match; this is not proof of full graph equivalence. The harness conservatively refuses before output and preserves both source hashes. Evidence: aaf-reference-copy-5633b2c5-17ea-46f1-90e8-69dd96d0d8ab. This rules out assuming path/link independence and motivates full shared-source equivalence checks, with conflicting graphs kept distinct or rejected. No merger success or native import is claimed.
- Shared-source graph diagnosis: a checksum-bound read-only comparator traverses strong properties with depth/object limits and reports weak-reference identities separately. The prepared-path pair has 78/5/4 property-level differences across its three shared sources, including locator paths, channel-label linkage IDs, dates and names. Equal slot summaries therefore do not establish graph equivalence. Evidence: aaf-shared-sources-afcda1b7-0552-4b4f-a975-45e5b2e9c81e. Both hashes unchanged; identity and changed-leaf comparison checks passed. No differences were discarded, no production merge or native acceptance is claimed. Explicit reference remapping or independently sourced collision-free references remain needed.
- Experimental AAF identity remapping: opt-in --remap-collisions assigns fresh IDs to the second graph collisions and updates MobID properties in copied strong graphs without changing originals. Reopened SourceClip references match originals plus the recorded map, master slot contracts/locator sets persist, and the existing builder produces inspectable stereo selects. Both prepared-path and conflicting-slot pairs passed: aaf-reference-copy-93585096-4725-4e1c-9268-4b7b45e287e6 and aaf-reference-copy-ba781b6d-adfa-4568-abe5-12d67f64a63b. A first same-locator assertion failure was corrected to match the inspectors documented unique locator set; failed artifact retained. Source hashes unchanged. Full descriptor/weak-reference preservation, independent media and native import/render remain unverified; no production merger shipped.
- Remapped strong-graph verification: factored the bounded diagnostic serializer and compared every copied mob against its original, substituting only recorded typed MobIDs. Both remapped native-reference pairs have zero remaining strong-property/weak-identity differences; changed-name and missing-mob negative checks detect modifications. Source/output hashes unchanged. Evidence: aaf-remapped-graphs-ddb4cc9f-7810-4b81-a9a7-3b58c04202d8. Original pair diagnostic still reports 78/5/4 differences after refactoring, confirming no normalization hid them. Weak target definitions, independent media and native import/render remain open. Research execution and diff checks passed; no production merger added.
- Weak-target preservation: recursive bounded definition comparison exposed four name changes in each fresh-dictionary merged AAF (data/container definitions). Negative evidence retained: aaf-remapped-graphs-c0ca2353-b01d-4b99-9ce1-bf8b3720fd4d. Research copying now starts from an exclusive copy of the first reference, preserving its dictionary, then appends remapped second-graph mobs. Both regenerated pairs pass complete reachable stored-property and four weak-target comparisons, with source/output hashes unchanged: aaf-remapped-graphs-b63a3f14-8f76-45da-8d31-b4102f8725eb. --original-copy retains the failing control. Implicit schema equivalence, independent media and native import/render remain open; no production merge tool shipped.
- Remapped multi-master native workflow: checksum-pinned selects from the conflicting-slot pair imported once into fresh MCP_Workflow_4adf76df.avb, saved/reopened, preserved two distinct master identities across all six picture/stereo references, and exported 120 decoded frames via the shipped guarded MCP workflow. Token replays refused, selected source/bin hashes unchanged and native lock released. Evidence: aaf-workflow-mcp-be6b7b12-000f-4728-b234-aec848b2fd64; render native-export-9a8b8c10-425a-4b64-b260-458e5a193855/export/render.mp4. Both cuts use source frame 2850 for 60 frames, so existing 2850/3300 audio comparator does not qualify this render. Independent audio/video fidelity, distinct source content and production merger remain open. Real native execution and diff checks passed.
- Remapped native audio conformance: extended the existing independent comparator with an explicit recorded --repeat-first-cut recipe. Complete rendered 24-bit PCM exactly matches two source-clock cuts at 95 seconds, with distinct output channels and unchanged source/render hashes. Evidence: aaf-workflow-mcp-be6b7b12-000f-4728-b234-aec848b2fd64/native-export-9a8b8c10-425a-4b64-b260-458e5a193855/export/audio-comparison-4c92cf05-bab0-4e4d-bdc1-090264d0cb6a. The same render fails the default 95/110-second strict recipe, preserving its negative report/log. Research execution and diff checks passed. This qualifies remapped-graph audio for this repeated-content fixture; independent-content sources, video/color and production merger remain open.
- Remapped render frame diagnosis: explicit repeated-cut recipe covers all 120 frames. Best matches are source index minus one, with presentation times within 0.334 microseconds, consistent with the original video start offset. Declared-range RGB RMSE 9.47986 falls to 1.08758 under forced-full diagnostic decoding; this supports the existing range-declaration issue but does not correct the render or establish color acceptance. Source/render unchanged, decoded frame hash 9ea1792c9a51fe4178bf3dac0caf48074176845db63e628467f1dbdaca44e73b. Evidence beneath aaf-workflow-mcp-be6b7b12-000f-4728-b234-aec848b2fd64/native-export-9a8b8c10-425a-4b64-b260-458e5a193855/export: frame-comparison-721d3e5a-da06-43ee-bf06-3348c8461a55 and frame-comparison-88326e74-1679-4dc1-a2c4-01eaf7ee7f21. Research execution and diff checks passed; full-resolution/perceptual color and independent-content qualification remain open.
- Distinct-content native source reference: the prepared 4K slideshow linked once into a fresh bin, retained master identity through save/reopen, and exported via guarded source-bound AAF MCP. Verified reference has picture/two sound slots at 30 fps and 6840 frames; SHA 1b19ec4a156788f0a47070ab7225fa128ad2f7a7ef42aedff491ab564fa1cace. Original slideshow and prepared MOV hashes unchanged, token replays refused and lock released. Evidence: slideshow-reference-d4f8398a-2f3c-45a6-a03f-2f58516b62c0. This supplies a genuinely different source for the next multi-source build/import/render, which remains untested. Research execution and diff checks passed.
- Distinct-content native stringout: preview/slideshow native references combine without source-ID collisions; all copied properties and reachable definitions match originals (aaf-remapped-graphs-26e29552-d96f-4905-9819-578d8b6eeb81). The two 60-frame cuts at each sources frame 2850 imported once into MCP_Workflow_116ab8c6.avb, saved/reopened with both master identities and six ranges intact, then rendered 120 frames with stereo PCM and replay refusal. Evidence: aaf-workflow-mcp-6f42b30d-86ed-4b72-bc09-d6e596911f87; render native-export-ca9065e5-4734-4711-8e9a-823cf9ffcdda/export/render.mp4. Supplemental media-after-render.json independently confirms both referenced MOV hashes unchanged. The copy receipt predates corrected dynamic limitation wording: this pair is distinct content despite its stale same-media limitation text. Audio/video fidelity and production merger still require acceptance; real native/research execution and diff checks passed.
- Distinct-content audio conformance: independent extraction from the original preview/slideshow MP4s, normalized on the source clock and trimmed at 95 seconds for two seconds each, exactly matches the complete native render PCM. Both channels of each cut match independently and remain distinct. Reversing expected cut order fails equality. Original/render hashes unchanged. Evidence beneath aaf-workflow-mcp-6f42b30d-86ed-4b72-bc09-d6e596911f87/native-export-ca9065e5-4734-4711-8e9a-823cf9ffcdda/export: distinct-audio-e3b7cf19-cd24-42bb-835c-996fa6048a8d. Added reproducible fixed-source verifier; real execution and diff checks passed. Video/color acceptance and production merger remain open.
- Distinct-content frame diagnostics: extracted all 60 expected frames per original MP4 using their separate source offsets; timestamp residuals below 0.334 microseconds. At 96x54 RGB, preview/slideshow RMSE is 9.4666/9.3738 under declared range and 1.1323/1.7201 under forced-full decoding; wrong-source comparisons are 86.4531/87.0586. This supports correct source ordering and the existing range mismatch but is not full-resolution color or timing proof for repeated stills. Source/render hashes unchanged. Evidence: aaf-workflow-mcp-6f42b30d-86ed-4b72-bc09-d6e596911f87/native-export-ca9065e5-4734-4711-8e9a-823cf9ffcdda/export/distinct-frames-b1d8da95-2462-45cd-afb0-877ef161589f. Real bounded decoder run and diff checks passed; production merger and color acceptance remain open.
- Production AAF merge backend: packaged Python merge action accepts 2-16 distinct checksum-selected external-reference AAFs, writes exclusively, remaps colliding MobIDs, retains the first dictionary, refuses conflicting weak definitions and verifies all copied stored properties/reachable targets after reopening. Original hashes are rechecked; failure output is retained without success. Three focused tests cover collision copies, overwrite preservation, invalid inputs/hash and definition conflict. Real preview/slideshow backend merge passed: production-aaf-merge-9c26d959-acd1-40ab-8549-7cea804de96f. Full check passed with 338 TypeScript tests, 26 Python tests, 126 tools, both transports and fresh installation; check-aaf-merge-backend.log. MCP registration/path/media/capability validation remains next; implicit schemas and broader native acceptance remain open.
- AAF merge MCP integration: avid_merge_aaf_references validates capabilities, canonical distinct reference paths, local media locators and checksums; verifies backend source/remap evidence, re-inspects output locators, rechecks media/source/output hashes and publishes a receipt only on success. Failure artifacts retained. Three wrapper tests cover receipt, media mutation, capability/duplicate/hash rejection. Real stdio merged preview/slideshow references then built and inspected 120-frame two-master stereo selects: aaf-merge-mcp-8af7bf95-bce6-4ba8-95a1-1f69bfc07b23. Full check passed: 341 TypeScript tests, 26 Python tests, 127 tools, both transports and fresh install. Log check-aaf-merge-mcp.log. Exact new MCP artifact native import/render remains next; broader formats/color remain open. API guide added.
- MCP-generated merge native chain: exact selects SHA 57f33d2969e2fb9371c85de5cee92fea72e9570f4e2491ef135e39b6a2a789b0 imported once into MCP_Workflow_2de9e2ad.avb, saved/reopened with both master identities/all six ranges and rendered 120 frames. Replay refusal, lock release and both media hashes verified. Evidence: aaf-workflow-mcp-a6a0a460-9132-4d96-b13c-ae3d4385f061. Original-MP4 stereo PCM comparison passes complete/per-cut/per-channel equality and reversed-order negative control: native-export-7d62d9d9-fc66-4402-959c-ba38a2e7f248/export/distinct-audio-ec94f034-45aa-4a2c-8ad2-448f9b67ad19. Research/native execution and diff checks passed. New-render video/color and broader acceptance remain open; prior cea428c CodeQL/landing passed with matrix jobs pending at observation.
- MCP merge video equivalence: completely decoded both checksum-selected distinct-content native renders and compared all 120 frame records plus codec/geometry/pixel format/rate/start/color declarations. Exact match, with both file hashes unchanged: merged-render-video-c9b7c263-e52d-4eb4-8d20-78bbcae0a75a. This connects previous original-source frame diagnostics to the MCP-generated render and preserves the known range discrepancy. Added reproducible read-only comparator; execution and diff checks passed. General color, implicit schemas and broader host/format acceptance remain open.
- Source-link collision regression: added actual MasterMob-to-SourceMob links to the duplicate-ID fixture, with source slot 1 versus slot 10. After production merging, each reopened master resolves to its own distinct remapped source and original slot/length; both input hashes remain unchanged. All four focused merge Python tests passed. This directly covers reference redirection, beyond prior null-reference fixtures. No additional full-suite or native run is claimed; broader acceptance remains open.
- Bundled selects skill integration: documented multi-reference merging, per-input remapped master resolution, explicit reviewed cut order and definition-conflict handling. The workflow distinguishes merged templates from native relinks and warns against name/array-order identity assumptions. Fresh tarball installation passed with all 127 tools and five skills, including validation of every packaged skill tool reference; package-merge-skill.log. Named-client application qualification and broader acceptance remain open.
- Merge verification checksum binding: output SHA is now captured before graph inspection and must remain identical after final template inspection. This prevents changed bytes after graph verification from receiving a new successful checksum. A deterministic valid-AAF metadata mutation between checks is rejected while failed output and unchanged sources are preserved. All five focused merge Python tests passed. Full-suite/native evidence remains from earlier recorded runs; current CI was pending at observation.
- Merge final binding validation: full current-code check passed with 341 TypeScript tests, 28 Python tests, 127 tools, both transports, five bundled skills and fresh tarball installation. Log check-merge-final-binding.log. Real stdio merge/build/inspect of preview/slideshow references also passed after the output-binding fix: aaf-merge-mcp-41fdd306-a006-4aea-9bfb-12ce0b10973b. This newly generated artifact has not been separately imported; earlier native proof remains tied to its recorded artifact. Current GitHub matrix/CodeQL jobs were pending at observation. Overall scope remains active.
- Cross-input reference binding guard: production merging now refuses an unresolved nonzero SourceClip mob ID that would become resolvable in the combined graph through another input. A regression preserves sources and rejects before output; unchanged unresolved slot references remain allowed. The initial blanket unresolved-reference rejection failed real Avid exports and was replaced with this specific graph-semantic guard. All six focused merge tests and real MCP merge/build/inspect passed: aaf-merge-mcp-000e377b-abe2-4fe4-b6d1-79852fcf144a. Failed initial attempt retained. No new full-suite/native run is claimed.
- Capability discovery refresh: avid_get_capabilities now advertises reference merging, explicit stereo selects and source-clock preparation with their requirements. llms.txt removes stale 0.2.0/all-live-editing-requires-Extension claims, separates native and bridge paths, and distinguishes development capabilities from the published installed build. Build and stdio smoke passed with 127 tools; discovery-merge-smoke.log. This improves client discovery only; release publication, named-client and broader host acceptance remain open.
- Installed-package merge proof: packed to a unique retained archive, installed with lifecycle scripts disabled into an isolated directory, audited dependencies, and ran the real stdio merge/build/inspect workflow from that installed entry and its own working directory. Evidence: aaf-merge-mcp-e4e0744d-3466-4a91-ba20-7344dc65621f, including installed entry hash; archive hash/audit retained under installed-merge-* qualification directory. Real preview/slideshow references passed with packaged Python modules. Existing machine Python remains an explicit dependency; this is not clean-machine or named-client UI proof. Added --server-entry to the reusable qualification harness; execution and diff checks passed.
- Fresh Python package qualification: isolated installed tarball passed real MCP merge/build/inspect with new Python 3.12.1 venv, system site-packages false, exactly pyavb 1.4.0 and pyaaf2 1.7.1, and clean pip check. Evidence aaf-merge-mcp-021f5471-bebb-4a53-bc89-10f68245bc3f; environment clean-python-merge-755ddbd3-ab74-4a88-9193-f7d6aacdc17c retains install/freeze/check/config. Added explicit --python option to qualification harness. Execution and diff checks passed. Existing interpreter/Node/Avid and named-client application setup remain separate.
- CI mutation-test repair: all four Windows/macOS Node 20/24 validation jobs on edfedd5 failed because the post-verification mutation hook compared an unresolved temporary output path with the merger canonical path. Canonicalized the comparison, exercised an explicit parent-segment spelling on every host, and asserted the mutation ran exactly once. Six focused merge tests passed (python-merge-canonical-path.log); the pre-fix full Python suite passed locally with 29 tests, illustrating the environment-dependent coverage gap. Hosted rerun remains required. Claude settings navigation was inspected; application connector qualification remains open.
- Client working-directory isolation: removed current-directory and parent-of-package Python inspector lookup. Source and built modules resolve only their package-relative inspector. Fresh tarball test launches a child from a folder containing a conflicting inspector, verifies the packaged path, then temporarily withholds the packaged inspector and verifies PYTHON_SIDECAR_MISSING instead of fallback. Build, fresh installation, 127-tool discovery, ping and five packaged skills passed; package-sidecar-isolation.log. All hosted checks for preceding b8f9b1c passed, including Windows/macOS Node 20/24. Named-client UI execution remains open.
- Setup doctor readiness fix: dependency probes return unavailable status rather than throwing; doctor previously wrapped those results as ok=true. Python and ffprobe now derive ok from actual availability and expose the diagnostic error while preserving probe data. Five focused setup/doctor tests and build passed. Built CLI with real nonexistent executable paths returned ok=false for both (setup-doctor-missing.json). Optional/native checks remain separate; no new machine or named-client acceptance claim.
- Setup qualification continuation: full bc334c1 local check passed with 343 TypeScript/29 Python tests, stdio/HTTP and fresh package tests (check-client-isolation-doctor.log). Hosted macOS identified raw-versus-canonical temporary path comparison in the package-only inspector test; compare real paths now, retaining missing-package failure coverage. Added a separate FFmpeg doctor result and tool-specific version-banner checks, preventing a successful ffprobe command from qualifying as FFmpeg. Ten focused tests, build and fresh package passed. Real installed FFmpeg/ffprobe/Python qualified; deliberately swapped FFmpeg-to-ffprobe path was rejected (doctor-ffmpeg-present.json, doctor-ffmpeg-swapped.json). This verifies executable discovery, not codec or named-client acceptance.
- Installed Python MCP isolation: fresh package smoke now supports --with-python and full check requests it. With a conflicting CWD inspector present, real avid_get_capabilities reported installed pyavb/pyaaf2 ready; withholding the packaged script reported unavailable with the specific missing-script error, and restoring it recovered readiness. Actual installed stdio sequence passed (package-python-mcp-isolation.log). Added FFmpeg availability to capability discovery so AI clients can distinguish encoder and inspector prerequisites; 15 focused handler/media/doctor tests, build and stdio smoke passed. This is real protocol execution, not named-client GUI acceptance.
- Named-client CLI evidence: installed Windows Claude Code 2.1.260 added our generated stdio entry in an isolated configuration and synthetic project, reported explicit connected statuses through mcp get/list, removed it and reported the exact missing-server response. Existing Code/Desktop configuration hashes were unchanged. Reusable Windows harness retains executable/server hashes and each CLI result. Strict-status run: claude-cli-04ab760f-19ce-4d46-9ad9-b2891cb421a9/evidence.json. Documented scope and official local/config-directory contracts. No model inference or GUI/native Avid action was performed; those acceptance requirements remain open.
- Installed named-client qualification: current built tarball installed with lifecycle scripts disabled into a fresh directory; dependency audit passed. Claude Code 2.1.260 connected to the checksum-bound installed entry, listed it, removed it, reported a deliberately missing entry as failed, then reconnected after correction and confirmed final removal. Client/server-entry hashes and existing Code/Desktop config hashes remained unchanged. Evidence claude-cli-693ede97-5c9f-4c83-829c-059caa9dc262; retained archive/installation/audit claude-installed-c040314a-721f-425f-937b-ec0bd7018508. Harness now accepts an optional absolute server entry plus expected SHA-256. Actual CLI sequence and diff checks passed; GUI/model tool invocation and native reads through the named client remain open.
- QC empty-audio correction: reproduced a false successful -inf loudness report for [3,4) of a four-second video whose audio ends at one second (qc-coverage-35b18d6e-126b-4a95-967a-6304da9988c7). Added measured per-channel samples at declared rate before normalization; zero/missing coverage fails without report writes, while partial amounts are explicit in audioCoverage. Strict real MCP regression passed: qc-coverage-b955d3df-c72c-4a4c-8879-47c66b77a1f1 (48000 samples/one second within four requested seconds). Silent full-range multistream proof passed with 192000 samples: qc-streams-9fad425a-c625-4301-baa6-0b06283cd064. Six nonzero-offset cases passed: qc-offsets-43320396-8081-4e67-9ac1-5bd6d6b318e7. Build and six QC tests passed; sources unchanged. Sample amounts do not prove contiguous timing or perceptual sync. Separate client auth status reported loggedIn=false, so model-driven Claude testing remains pending authentication.
- Sonoma QC amount qualification: real MCP inspection of original preview and verified source-clock copy over [60,90) measured 1443456 versus 1440000 samples/channel at 48 kHz. Independently exported 16-bit stereo PCM byte counts matched each report; original and prepared hashes remained unchanged. Evidence sonoma-qc-amount-cda30236-6e66-48b5-95b8-016efd885e9f. Existing mixed black/silence fixture and Sonoma video/audio QC passed (qc-d7f09f26-718c-4d3f-aafc-499ac1502657). Full check passed: 345 TypeScript, 29 Python, 127 tools, stdio/HTTP, dry pack and fresh installed Python MCP isolation/recovery; check-qc-coverage.log. Updated implementation status to this complete local check. Sample-count agreement does not close timing/perceptual synchronization or full media acceptance.
- Queued QC persistence: real MCP worker jobs completed the short-audio fixture with 48000 samples and explicit partial amount, and failed an empty audio range without a result/report. After journal flush and a fresh MCP server session, completed/failed states, result/error content and automaticReplay=false were preserved. The sole JSON/HTML report pair, report checksum and source checksum were unchanged. Evidence qc-job-restart-2269f773-494d-45eb-8f91-344d9192b5cb; reusable qualify-qc-job-restart.mjs. This verifies terminal job persistence, not interrupted computation or every analysis job kind.
- Job status acknowledgement: current-session status previously returned an in-memory terminal state while its journal write could still be pending. Track each job checkpoint promise and wait through any newer checkpoint before responding to status. Eviction removes tracking; persistence failures remain explicit journalError and never masquerade as a persisted result. Delayed-write and failed-write tests plus existing journal/job tests passed (8 total); build passed. Real QC completed/failed restart qualification without a history flush passed: qc-job-restart-8d9a5e08-7757-4a76-bbce-3cc8518b76c3. This strengthens acknowledged status, not abrupt termination or interrupted-job recovery. Preceding ec4d64a hosted checks all passed on Windows/macOS Node 20/24, landing and CodeQL.
- Cancellation acknowledgement consistency: MCP cancellation now follows cancel with the journal-aware status read. A queued job cannot report acknowledged cancellation before its cancelled record settles; a running worker still reports cancelling until close and does not free the worker slot early. Nine job/journal tests passed, including delayed cancellation publication; build passed. Real Sonoma index plus artifact cancellation and new-session history recovery passed: job-restart-2cb25a7e-57e3-45a6-9916-d2d583681fbc. This does not add rollback or claim all platform process-lifecycle cases are qualified.
- Combined acknowledgement validation: full npm run check passed with 348 TypeScript and 29 Python tests, 127 tools, stdio/HTTP, dry packing and fresh installed Python MCP isolation/recovery (check-job-acknowledgement.log). Reconciled the native packaging roadmap item against the current read/write allowlists and adapter call paths: all historical 16 methods are packaged; current allowlist is 10 reads plus 11 writes. This closes packaging only, not broader host/version/undo acceptance. Updated the stale 19-method inventory and latest complete local-check record.
- Saved QC report tools: added avid_qc_reports and avid_read_qc_report with inspect-only access, indexed/current source checks, media-ID binding, optional expected report hash, stable bounded JSON reads and bounded cursor discovery. Invalid revision filenames cannot poison cursors; unreadable candidates are counted, and empty matching pages remain traversable. Updated ingest/QC skill and discovery assertions to 129 tools. Full check passed with 351 TypeScript/29 Python tests, protocol and fresh installed package/skills/Python isolation (check-qc-report-discovery.log); final cursor guard passed focused tests/build and actual Sonoma report discovery, checksum rejection and fresh-session read: qc-reports-mcp-0cfe3436-1727-4974-9d11-05e90a879f22. Stored report checksums are not authenticated analysis receipts and HTML companions are not verified by these read tools.
- Saved QC consistency: report reads now validate measured sample-count arithmetic, requested range, findings count, duration-match flag and audio stream selection. Legacy artifacts remain readable with explicit audioCoverageStatus=not_recorded; intentional audio omission is separate. Inconsistent stored measurements are rejected or counted unreadable in discovery. Four report tests and build passed; actual Sonoma paginated discovery, expected-hash read and fresh-session equality passed again: qc-reports-mcp-a0b3b60e-ec85-49dc-8312-ee3fb406d645. This checks internal consistency, not measurement authenticity.
- Saved QC stream consistency: report reads now reject no selected streams, one absolute stream index used as both video and audio, and selections contradicting explicit options. Discovery counts these artifacts as unreadable. All 353 TypeScript tests, build and real Sonoma paginated/checksum-checked retrieval after reconnect passed: qc-reports-mcp-bc9256dd-0fe1-4f22-a71f-f4e8f1018c28. The preceding combined check passed 352 TypeScript and 29 Python tests plus 129-tool transports and fresh-package/Python recovery; check-qc-consistency.log. This does not authenticate saved measurements or close broader QC acceptance.
- Forced process-exit qualification: real Sonoma QC running plus queued jobs were persisted, then the harness-owned Windows MCP process tree was terminated using its transport PID. A fresh MCP session returned both as unresolved with original recorded statuses, no result and automaticReplay=false. History matched, journal hashes and source SHA remained unchanged. Script: scripts/research/qualify-job-crash.mjs; evidence: job-crash-bde54231-5f92-4c71-988b-353f1255049c. This closes this specific abrupt tree-exit status-recovery test only; power loss, parent-only death/orphaned workers and computation recovery remain open.
- Parent-only exit observation: qualify-job-crash.mjs --parent-only records server-child PID/creation identities, terminates only its own MCP server, and checks fresh-session unresolved records with no replay, unchanged journals/source and no QC report artifacts. Observed children were absent at the later CIM query. Strict run passed: job-crash-2d0234ca-fd34-4bf7-811f-d8c47bba8e6c. The harness has identity-checked cleanup for surviving observed children. This is one QC process-exit observation, not proof of prompt worker cancellation, descendant containment, model-worker shutdown or power-loss safety.
- Damaged job-history recovery: one corrupt journal no longer hides healthy records. File-bounded pages return unreadable counts and a continuation cursor even when empty; direct reads remain strict. Invalid UUID filenames and directories are ignored. All 354 TypeScript tests and build passed. Actual MCP forced-exit recovery plus an injected damaged record returned an empty first page and both unresolved healthy jobs on continuation: job-crash-8fee1631-2ba5-46c2-b29c-85d920ccf368. Source preserved; no automatic replay.
- Combined recovery validation: npm run check passed with 354 TypeScript tests, 29 Python tests, 129 tools, stdio/HTTP checks and fresh-package installation with Python isolation/recovery; log check-job-history-recovery.log. The bundled export skill now requires following history cursors even on empty pages. A subsequent fifth job-journal test verifies inaccessible first-page continuation to an accessible record without returning the inaccessible record contents. No full 355-test run is claimed for this checkpoint.
- Fresh installed named-client increment: comparing current checkout versus fresh installed package exposed Zod drift (4.4.3 versus resolved 4.5.4) in advertised tuple constraints. Pinned tested Zod 4.4.3; npm audit reports zero vulnerabilities in this branch. All 355 TypeScript tests passed. Fresh managed install now exactly matches all 129 tool definitions and pings; installed Claude CLI connect/list/remove, broken-startup detection and corrected reinstall passed with existing configurations unchanged. Harness: qualify-installed-claude.mjs. Evidence: installed-claude-d551d8d6-1f7f-44ce-a0fe-77350c5cbac1 and claude-cli-66bc27f8-005b-412a-a474-b8b2fbeceb94. GUI/model-driven and native invocation remain open.
- Installed schema regression gate: standard smoke:package (and therefore npm run check) now compares complete installed tool definitions with a separate checkout server using the same inspect scope and working directory. Count-only success is no longer sufficient; definition drift reports affected tool names, and incomplete discovery is rejected. Fresh installation passed exact equality for all 129 tools plus five skills and actual Python isolation/recovery: package-schema-check.log. This validates current installation contracts, not named-client GUI or live Avid execution.
- Saved sequence complexity increment: new inspect-only avid_saved_sequence_complexity reports per-track node kinds, direct source/reference counts, duration and opaque-node incompleteness from authorized snapshots. Duplicate/missing mob identities are refused. Build, focused tests and fresh installed exact-schema comparison passed with 130 tools/five skills and Python isolation/recovery. Historical native-import Sonoma snapshot returned three tracks, six references, one distinct source and four seconds, snapshot unchanged: complexity-f5e88f23-3526-4781-ba4c-312e6768e8bb.json. The earlier current-bin qualification refused a changed hash; historical snapshot evidence does not establish current bin equivalence. Recursive effects, render cost and live unsaved graph remain open.
- Current saved-bin structure qualification: qualify-snapshot-complexity.mjs --fresh captured the current Sonoma MCP_Import_3405029e.avb through real MCP/Python, checked capture SHA against pre/post file SHA, and compared it to the historical snapshot. Current structure has three tracks (picture, stereo sound, timecode), seven nodes, six source references and four seconds; the historical comparison contains two semantic changes. Evidence complexity-42f4d4c3-9aad-4fa2-bcf9-643f8e05cc61.json; current revision 6f1dd178-0d43-41f9-b64e-708f015e8926. This establishes saved-bin inspection, not unsaved editor state or render fidelity. Turnover skill now consumes the report with channel/timecode caveats; focused source-scope and duplicate-identity rejection checks passed.
- Snapshot consistency: shared snapshot parsing now rejects unsafe nonnegative edit-unit values, duration/source-bound disagreement, duplicate track ordinals and empty/out-of-duration node ranges before queries or structure reports use them. Fresh current Sonoma MCP capture/report passed: complexity-06183075-b6a8-4b07-846d-de1b7d936344.json. Historical differences inspected: source mob renamed Sonoma_SourceClock_Stereo to prepared and another mob added; no claim of identical bin semantics. Build and 29 Python tests passed. First TypeScript run lost a Vitest worker and was invalid; complete retry passed all 357 tests (test-snapshot-integrity-retry.log).
- Cross-runtime snapshot fixtures: qualify-snapshot-fixtures.mjs writes real AVBs using existing pyavb fixture builders, then calls snapshot capture, complexity and timeline-range tools through stdio with the real Python sidecar. Subclip clipped ranges/source offsets, stereo per-channel offsets and opaque-effect incompleteness passed; all AVB hashes unchanged. Evidence snapshot-fixtures-4e4e2d04-a0db-435c-928a-d5c52ba22b16. This tests three generated AVB structures across the Python/TypeScript boundary; native import and general transition/effect coverage remain open.
- Transition mapping qualification: a generated real AVB contains two 60-frame source clips with a 10-frame TNFX transition, yielding a 110-frame sequence. Python and actual MCP preserve nodes [0,60), [50,60), [50,110). Query [55,58) returns source ranges [1055,1058) and [2005,2008), with the transition opaque and completeness false. All five Python timeline tests and four cross-runtime fixture cases passed; source AVBs unchanged. Evidence snapshot-fixtures-bd330ee8-084c-4ffe-a5c4-382467cfae28. This establishes this generated overlap mapping, not effect identity, native import/render or general transition acceptance.
- Combined sequence-structure check: npm run check completed successfully with 357 TypeScript tests, 30 Python tests, 130 tools, stdio/HTTP checks, dry packing and fresh installation matching all tool definitions, five bundled skills and Python isolation/recovery. Log check-sequence-structure.log. Implementation status reconciled with structural-report and installed-Claude evidence. GitHub run 34002625531 for 7dab822 still had Windows/macOS validation jobs in progress at inspection; no cross-platform completion claimed.
- Source usage pagination: replaced the terminal 500-row truncation with bounded pages, totalReferences and nextAfter while retaining default 500-row compatibility. A 503-reference regression retrieves every row without duplication. Real Python/MCP fixture workflows paged source usage one row at a time for subclip, stereo and transition cases, preserving AVB hashes: snapshot-fixtures-e0819ed5-08f4-4c27-bdc0-5d5232091823. Turnover skill follows continuation cursors. Build and focused tests passed; full TypeScript results retained in test-source-usage-pagination.log. Direct-source scope remains unchanged; recursive and opaque references remain incomplete.
- Snapshot diff pagination: complete semantic comparisons can now be retrieved beyond the previous 200-change cutoff using after/limit, nextAfter and totalChanges. A 203-change regression retrieved all changes; invalid and exhausted cursors were exercised. Actual MCP one-change pages of a fresh-versus-historical Sonoma comparison matched the unpaged result: complexity-07952236-b936-4210-9fda-88793c00d9a8.json. Build and focused tests passed; full TypeScript result retained in test-snapshot-diff-pages.log. Turnover skill follows cursors; opaque/live-state limits remain unchanged.
- Snapshot comparison identity guard: duplicate bin paths and duplicate mob IDs within a bin now reject the comparison instead of Map construction silently overwriting records. Pair keys use structured encoding. Focused duplicate tests, build and fresh Sonoma MCP comparison passed: complexity-fe196b91-8fbf-444f-80f0-d62c04e4b740.json. Full test result retained in test-snapshot-identities.log. CI for prior transition commit 7dab822 completed successfully (34002625531 and CodeQL 34002625507); newer heads remain separately tracked.
- Bounded timeline result collection: range queries now stop building output after limit+1 matching nodes, preserving global node cursors and track filtering. Service-level page/ordinal validation matches the MCP bounds. A filtered two-track fixture crosses the 200-row page boundary with correct source offsets; build and real Python/MCP subclip/stereo/opaque/transition cases passed (snapshot-fixtures-d53944dd-56d1-428c-b6d7-cfd55a0d1bac). Full TypeScript result retained in test-bounded-timeline-pages.log. Snapshot parsing still reads the bounded snapshot in full; this change bounds derived result allocation, not total snapshot memory.
- Installed pagination invocation gate: standard fresh-package smoke now writes synthetic snapshots only inside its temporary output root and invokes installed diff/source-usage/timeline-range tools across continuation pages. It checks terminal cursors, reference/change totals and source overlap offsets. Fresh installation passed all three handlers, exact 130-tool schema equality, five skills and actual Python isolation/recovery: package-pagination-invocation.log. These are synthetic handler checks, distinct from retained Sonoma and generated-AVB evidence.
- Mixed edit-rate evidence: inspector warnings now include mobRate, componentRate and explicit omitted-mapping status. A generated 30 fps mob with a 24 fps component remains incomplete, omits the unsupported component and retains the same-rate direct node. Six Python timeline tests and all five real Python/MCP fixture cases passed; source AVBs unchanged. Evidence snapshot-fixtures-db6a3b87-1e0d-4aba-85a5-04fbe4b34e88. No mixed-rate conversion, native playback or full source mapping is claimed.
- Mixed-rate offset correction: inspection of pyavb positions() confirmed component lengths are added without rate conversion. Later source offsets after a mixed-rate component were therefore not qualified. Inspector now stops the remainder of that sequence, propagates uncertainty through enclosing sequences and records UNRESOLVED_SEQUENCE_OFFSETS; known prefixes survive. Seven focused timeline tests cover prefix retention and nested propagation. Five real MCP fixtures passed (snapshot-fixtures-0865b22f-9e4b-409f-834d-6d9c12b93f59); fresh Sonoma capture still passed (complexity-2912c4eb-2c10-4f6c-a31d-0044396659cb.json). This supersedes the preceding mixed-rate checkpoint retaining a later same-rate node.
- Source usage coverage evidence: results now include per-bin completeness, stored warning counts and up to ten warning details per bin with explicit truncation. A zero-match mixed-rate query through real MCP preserves MIXED_EDIT_RATE and UNRESOLVED_SEQUENCE_OFFSETS rather than only returning a false completeness flag. Build, focused tests and five AVB fixture cases passed: snapshot-fixtures-df6144bc-50ad-40b3-ad55-12e646a7ecc6. Full test log: test-usage-coverage.log. Turnover skill warns against unused-media conclusions from incomplete matches.
- Coverage validation follow-up: the first full TypeScript run lost a Vitest fork and was invalid (test-usage-coverage.log). A complete retry passed all 362 tests (test-usage-coverage-retry.log). Focused and actual MCP results above remain valid; no first-run full-suite success is claimed.
- Test-runner resource bound: recurrent prior Vitest worker exits prompted inspection of process tests/mocks and host capacity (32 available logical CPUs, approximately 18 GiB free at observation). A verbose four-worker diagnostic passed all 362 tests in 8.84 seconds. Vitest now defaults to at most four workers; normal npm test passed all 362 in 9.37 seconds (test-workers-bounded-default.log). Root cause of earlier worker exits is not established, and this does not prove the failures cannot recur. No tests or assertions were disabled.
- Complete turnover/coverage validation: npm run check passed with 362 TypeScript tests under the four-worker default, 32 Python tests, 130 tools, stdio/HTTP transports, dry packing and fresh installation. Installed schema equality, synthetic diff/usage/range continuation and Python missing-sidecar recovery all passed. Log check-turnover-coverage.log. Implementation status now distinguishes delivered paginated structure/coverage reports from unfinished rate conversion, live graphs and effects/render-cost work. GitHub jobs for 5bbe2eb were still running at inspection; no latest-head CI completion claimed.
- Snapshot diff coverage: baseline/candidate per-bin completeness and bounded omission warnings are now returned with every comparison, reusing the source-usage coverage representation. A warning-only candidate change remains an empty semantic diff but exposes incomplete candidate coverage. Actual mixed-rate self-comparison through MCP retains unresolved-offset warnings despite zero visible changes: snapshot-fixtures-d5a1c218-8884-44a9-ba48-6ad46eded731. Build, focused tests and five AVB fixtures passed; full TypeScript log test-diff-coverage.log. No equivalence claim is made for incomplete captures.
- Snapshot discovery increment: new inspect-only avid_saved_snapshots exposes paginated authorized historical revision summaries, bounded directory scanning and unavailable counts without content disclosure. Focused reconnect/scope/damage tests and build passed. Five generated AVB workflows discovered each actual MCP capture: snapshot-fixtures-b23bf442-dec9-47eb-9586-95b268a8bb5c. Fresh-package discovery continuation is now tested alongside existing pagination/schema/Python gates; log package-snapshot-discovery.log. Inventory increases to 131 tools; discovery is historical and does not assert current bin equality.
- Snapshot reconnect qualification: copied a retained historical Sonoma snapshot into an isolated output library with a damaged first entry. Real MCP discovery returned an empty first page plus continuation, recovered the revision, and returned identical discovery after reconnect. The recovered revision produced its six-reference structure report. Narrowing roots hid the snapshot and counted both entries unavailable. Original/copy snapshot hashes unchanged. Evidence snapshot-discovery-restart-04cd7f03-2dc4-4f55-948a-7a9087de1b72; script qualify-snapshot-discovery-restart.mjs. This is historical recovery, not current-bin equality.
- Snapshot publication: capture now writes complete bytes to a unique temporary file before exclusive same-directory link publication. Existing revisions are preserved on collision; oversized payloads fail before writing, and ordinary failures remove temporary attempts. Focused publication tests, build and five real Python/MCP fixture captures passed: snapshot-fixtures-873cf649-fb5e-4e98-b2eb-7d5c790b2d74. Full TypeScript result retained in test-snapshot-publication.log. Requires hard-link support; abrupt-process temporary artifacts and power-loss durability are not claimed solved.
- Snapshot publication race qualification: two concurrent publishers targeting one revision produce exactly one success, preserve the winning complete bytes and leave no ordinary temporary attempts. Discovery ignores an abandoned incomplete temporary file. Actual historical Sonoma discovery/reconnect and restricted-root checks passed with the abandoned file retained unchanged: snapshot-discovery-restart-90bd29e4-7b0b-4857-a928-4137956f6490. All 367 TypeScript tests passed (test-snapshot-concurrent-publication.log). This is concurrent filesystem publication and staged-file discovery evidence, not power-loss durability or crash-time cleanup.
- Incremental snapshot capture bound: cumulative serialized bin payloads are checked after each inspected bin, before retaining it or proceeding to later bins. A mocked-sidecar fixture of six 7 MiB bin payloads stops on the fifth response, skips the sixth and publishes no snapshot. Final whole-record size checking remains. Build and fresh actual Sonoma capture passed: complexity-c7031c1d-9535-4fa7-8d30-cadc12cf3ad7.json. Full TypeScript result retained in test-capture-aggregate-limit.log. This bounds retained payload growth, not total process RSS or sidecar allocation.
- Canonical capture deduplication: bin paths are deduplicated after allowed-root resolution, preventing equivalent path spellings from generating duplicate bin identities and incompatible snapshot diffs. A mocked-sidecar test confirms one inspection and a valid self-diff. All five actual Python/MCP AVB fixture cases supplied canonical and dot-component aliases and captured exactly one bin: snapshot-fixtures-14c166d0-126e-4a07-8684-d80113302e5e. Build passed; full test log test-capture-alias.log. Input count and authorization limits remain enforced.
- Combined discovery/capture validation: npm run check passed with 369 TypeScript tests, 32 Python tests, 131 tools, both transports, dry packing and fresh installation. Installed schema equality, discovery/pagination handlers, five skills and Python isolation/recovery all passed; log check-snapshot-discovery-capture.log. Status reconciled with historical discovery, exclusive publication and bounded/deduplicated capture. Latest GitHub jobs were still running at inspection; no latest-head CI completion claimed.
- Recovered snapshot mob inventory: new inspect-only avid_saved_snapshot_mobs supports bounded identity pages with bin provenance and total count. A 102-mob regression verifies continuation and scope rejection. Actual MCP reconnect recovered a historical Sonoma revision, paged mobs one at a time, selected the sequence by returned name and used its returned ID for structure reporting: snapshot-discovery-restart-f534d7de-2df1-4720-98b8-699990c208cb. Build and fresh 132-tool package/schema/Python checks passed; logs package-snapshot-mobs.log and test-snapshot-mobs.log. Turnover skill now completes this discovery chain.
- Installed snapshot recovery chain: standard package smoke now discovers a synthetic revision, pages its mob inventory and uses the returned revision/mob IDs for timeline continuation. Fresh installation passed this chain plus exact 132-tool definition equality, five skills, diff/source-usage pagination and Python isolation/recovery. Log package-recovery-chain.log. This automated package-handler evidence complements historical Sonoma reconnect proof; no native host state or model-client invocation is implied.
- Historical bin availability: discovery now reports missing-bin counts and mob inventory reports binPresent separately from captured completeness. A deleted synthetic bin remains historically queryable while its parent is authorized; narrowing roots refuses access. Existing bin presence does not imply captured-hash equality. Build/focused tests and real historical Sonoma discovery/reconnect passed: snapshot-discovery-restart-ce9b77bb-b2d6-4c87-a931-26a6bf09b853. Full TypeScript log test-missing-snapshot-bins.log. Only the synthetic test bin was deleted.
- Current native read refresh: confirmed AvidMediaComposer PID 130720 remains the loopback 9100 listener owner, then ran inspect-only MCP app/project/bin/clip reads through the qualified native adapter. Disposable Sonoma project is 1080p30 and selected sequence reports 120 frames. Independent Python inspection verified saved picture/two-audio-track cuts [0,60)->2850 and [60,120)->3300; saved bin SHA and original Sonoma MP4 SHA unchanged. Evidence native-refresh-e0b70d0d-dbf5-44cc-b060-576998dfb912/evidence.json. No restart, mutation, render-fidelity or undo claim is added.
- Fresh installed native-read qualification: qualify-installed-native.mjs packs and checksum-verifies a managed installation, then runs actual inspect-only Avid app/project/bin/clip queries using its absolute installed entry. Saved picture and stereo audio source ranges and unchanged bin/original MP4 hashes passed. Evidence installed-native-9dbb445d-006a-4012-b656-6f3ca0d64a88 and native-refresh-3291d5de-35e4-4ea2-960d-779a86f1c9d4. Independent saved-bin oracle uses checkout Python and existing virtual environment; no clean-machine, GUI/model invocation, mutation or render claim. Combined npm run check passed: 371 TypeScript tests, 32 Python tests, 132 exact-matching installed tools, five skills, both transports and Python isolation/recovery; log check-installed-native.log.
- Development onboarding documentation reconciled: README now distinguishes native operations from the Extension catalog, compatibility rules from host qualification, and published-package commands from the unreleased branch. Source setup explicitly selects the feature branch and virtual-environment Python; client setup generates an absolute entry. Verified remote branch exists and executed the documented configuration-generation command with the real Sonoma root/interpreter. No new application onboarding or clean-machine qualification is claimed.
- Setup path diagnostics: doctor no longer reports an empty allowed-root list as healthy and separately checks that an explicitly configured output path exists as a directory. Individual-file read roots remain supported; no directory creation, write-permission or capacity guarantee is implied. Typecheck/build and all 372 TypeScript tests passed (test-doctor-paths.log). Built CLI reproduced empty-scope and file-as-output failures in doctor-invalid-paths.json. This is diagnostic coverage, not clean-machine dependency installation.
- Installed setup command regression gate: fresh-package smoke now generates all five client formats using the installed CLI from an unrelated working directory, checks identical absolute server commands and inspect authority, and connects through the generated entry instead of a hand-written command. Both default and --with-python runs passed with 132 exact-matching tools, five skills and existing snapshot/sidecar checks; logs package-generated-setup-default.log and package-generated-setup.log. This does not establish named-application UI onboarding. Latest-head CI was queued/in progress at inspection.
- Optional Jumper client foundation: refreshed public API 1.0 contract and implemented original bounded loopback health/text-search client with explicit authorized media selection, header-only search licensing, redirect refusal, response/time limits and metadata-only returned matches. Real loopback fixture tests cover scope, secret/image omission, oversized responses and failures. Typecheck/build and all 374 TypeScript tests passed (test-jumper-client.log). Not exposed as an MCP tool; listener pairing, runtime version verification and licensed provider tests remain open. Saved refreshed spec SHA256 cd3ac6844de566065bf9c95934b9831167e5c2c1428beade5f66ce805ae56d4d.
- Optional provider failure qualification: client configuration and roots are copied/frozen after validation, preventing caller mutation from changing scope or bounds. JSON media type matching is exact. Actual loopback tests verified stalled-body timeout, misleading content type, malformed JSON, incompatible health schema and successful subsequent recovery without response-content leakage. Typecheck/build and all 375 TypeScript tests passed; log test-jumper-failures.log. Licensed-provider, pairing and MCP exposure requirements remain open.
- CI path-identity correction: inspected failed macOS jobs 34004813091 and 34004671870. Tests compared lexical /var temporary paths with correctly canonicalized /private/var provider paths and installed server entries. Assertions now compare canonical filesystem identity while retaining absolute single-entry and inspect-authority checks. Focused provider tests passed; full local check completed with 375 TypeScript tests, 32 Python tests, 132 tools and five skills (check-jumper-foundation.log). Hosted macOS rerun remains required; no Mac native qualification is implied.
- Completion matrix reconciled with retained implementation and acceptance evidence for installation, AAF/stereo, transcripts/diarization, people recovery, snapshots and optional provider foundation. Full scope remains intact. CI run 34004942155 for f251ead confirmed both macOS Node 20/24 validation jobs and landing successful; Windows jobs and CodeQL were still running at inspection. This closes the reproduced macOS path-assertion failures, not Mac native-host qualification.
- Windows provider ownership primitive: separate loopback verifier checks exact listener count/address/port, canonical executable path, SHA256 and optional PID/start-time identity. Actual harness-owned Node listener passed and wrong hash/identity were refused. Typecheck/build and all 376 TypeScript tests passed (test-loopback-owner.log). This preflight is not cryptographic connection authentication and is not yet integrated into provider requests. CI run 34004942155 for f251ead completed successfully across landing and all Windows/macOS Node 20/24 validation jobs.
- Optional provider paired dispatch: configured owner binary/hash/PID-start identity is copied and checked before every provider HTTP request. Actual Windows fixture search sent its test license only after ownership verification; changed process identity produced no additional HTTP request. Typecheck/build and all 376 TypeScript tests passed (test-paired-provider.log). Optional pairing supports contract fixtures; future MCP exposure must require it. User-facing pairing, preflight-to-connection race and licensed-runtime acceptance remain open.
- Provider pairing CLI: --pair-jumper with explicit executable hash and optional port performs read-only Windows listener qualification and prints owner/base URL fields without HTTP or credentials. Conflicting setup flags are refused. Actual owned-listener CLI execution matched independently obtained process identity; all 376 TypeScript tests and build passed (test-provider-pair-cli.log). Supplied hashes require independent provenance; no vendor authenticity, licensed-runtime or MCP integration claim.
- Provider configuration foundation: environment loader returns disabled with no provider settings and requires complete credentials/executable/hash/process identity whenever configured. Constructor now runtime-validates pairing, closing missing-identity JavaScript input bypass of the optional identity check. Typecheck/build and all 377 TypeScript tests passed (test-provider-configuration.log). Loader remains unregistered with MCP; licensed provider and server exposure remain open.
- Optional provider MCP integration: avid_jumper_read exposes paired Windows health/scoped text search with inspect authority, lazy complete environment configuration and no startup dependency on Jumper. Actual in-memory MCP dispatch reached a real harness-owned loopback fixture through ownership verification. Unconfigured calls fail closed, including fresh installed package. Full check passed: 377 TypeScript tests, 32 Python tests, 133 exact-matching tools, five skills and both transports (check-provider-mcp.log). This is fixture execution, not licensed Jumper runtime/version validation; connection-race and broader provider operations remain open.
- Provider MCP refusal matrix: six real MCP dispatch tests verify authority denial, partial configuration, contradictory health fields, missing search scope, empty selection and excessive limits. All reject before mocked listener inspection, omit the configured test credential and preserve core ping. All 383 TypeScript tests passed (test-provider-mcp-boundaries.log). These are boundary tests; licensed provider and broader completion requirements remain open.
- Fresh installed provider dispatch: qualify-installed-provider.mjs installed a new checksummed tarball, paired a real harness-owned Windows listener and invoked scoped search over stdio from the installed absolute entry in a foreign working directory. Header/selection assertions, image omission and fixture/archive/entry hash preservation passed. Evidence installed-provider-12daf05c-26d7-45c3-8a11-946bfe924763. Synthetic path and fixture key only; no licensed runtime, decoded-media/search quality or connection-race qualification.
- Optional provider transcript search: avid_jumper_read transcript operation sends explicit selection/search_all=false, bounded query/results and optional transcript speaker filter. Returned selected-media paths and positive finite source-second intervals are validated; unresolved paths are refused. Real HTTP fixtures tested valid/reversed/unresolved/out-of-selection results, and paired Windows MCP dispatch passed. Typecheck/build, all 383 TypeScript tests and fresh 133-tool package/schema/Python checks passed; logs test-provider-transcript.log, test-provider-transcript-paired.log and package-provider-transcript.log. Substring behavior is vendor-contract based; licensed runtime and audio alignment remain unqualified.
- Provider output budget: visual/transcript search result data is bounded at 256 KiB serialized UTF-8 JSON independently of HTTP response bytes. Oversize returns an actionable error without silently truncating transcript text or inventing continuation. A real HTTP fixture under the transport bound but over the output bound was refused. Typecheck/build and all 383 TypeScript tests passed (test-provider-output-limit.log). MCP wrapper overhead and licensed runtime remain separate limits/acceptance.
- Provider process-replacement qualification: research harness stopped its first Windows HTTP process, started a new process on the same port, refused the stale PID/start pairing before any replacement HTTP request, and restored health with fresh pairing. Both owned children stopped. Evidence provider-restart-4a8dfc98-0abd-44f8-8989-93dd5cc2c986. This verifies observed stale pairing recovery, not licensed-runtime restart or connection-establishment race exclusion.
- Provider result provenance: health and both search modes report passed versus not-configured ownership preflight independently of unverified runtime version. Actual paired MCP fixture and unpaired HTTP fixture assertions passed; typecheck/build and all 383 TypeScript tests passed (test-provider-provenance.log). The field records pre-dispatch ownership evidence, not connection authentication or licensed compatibility.
- Combined provider checkpoint: full npm run check passed with 383 TypeScript tests, 32 Python tests, 133 exact-matching installed tools, five skills, stdio/HTTP and package-sidecar isolation/recovery (check-provider-transcript-combined.log). CI and CodeQL for 241b22e both completed successfully; later heads remained in flight at inspection. Status now includes transcript search and observed stale-pairing recovery while preserving licensed-runtime, connection-race and broader original requirements.
- Provider capability diagnostics: avid_get_capabilities now reports absent/invalid/configured syntax status, platform support and explicitly unverified listener/runtime status without returning configuration values or probing processes/network. Tests cover absent, partial-secret and complete configurations. Typecheck/build and all 383 TypeScript tests passed (test-provider-status.log). Configuration is not connectivity or licensed-runtime qualification.
- Core ASR negative evidence: real MCP with cached multilingual tiny transcribed an eight-second pure tone as Thank you. and seeded white noise as Thank you for watching!; digital silence was rejected. All generated source hashes remained unchanged. Evidence nonspeech-cbd88a25-bd6a-4798-b377-8159b3986baa; see NONSPEECH_QUALIFICATION.md. This is a demonstrated non-speech quality failure, not acceptance completion. Speech-presence validation and broader positive/negative fixtures remain required; no phrase blacklist or uncalibrated probability threshold introduced.
- Speech-presence comparison: existing installed diarization via real MCP returned zero spans for synthetic silence/tone/seeded noise and two/three spans for existing English/Mandarin synthetic speech. All source hashes unchanged. Evidence speech-presence-4abbe458-cff1-4534-ac39-1e179feefbfa. This supports further evaluation of an optional speech-presence guard but does not justify a default VAD threshold, quiet-speech rejection or broad accuracy completion; transcription behavior unchanged.
- Speech-presence stress evidence: quiet (-40 dB) and seeded-noise-mixed derivatives of English/Mandarin synthetic speech still produced spans (2/3 respectively); silence/tone/noise negatives remained zero. Nine-fixture real MCP evidence speech-presence-f1617092-3f28-41a0-8abe-b180b18b94f5. Original/analyzed hashes preserved. This is whole-file presence evidence, not timing completeness, measured SNR, calibrated VAD or broad recordings acceptance; no default gate changed.
- Speech-presence reporting: existing speaker-analysis reads now expose selected-view interval-union coverage across analyzed PCM, independent of pagination, with explicit unverified/no-span semantics. Regression checks verify overlapping spans count once and pages share identical coverage. Typecheck/build and all 383 TypeScript tests passed; actual five-fixture MCP reports passed in speech-presence-b067aa02-ee7a-43b1-b2ea-509be29d629d. No automatic transcription gate or broad speech-accuracy claim.
- Reviewed presence regression: removing every reviewed span yields zero effective coverage while machine view preserves original spans/union duration and pagination. Parent analysis checksum and source checksum remain unchanged; no model required to read corrected views. All 384 TypeScript tests passed (test-reviewed-presence.log). Empty effective coverage is not conflated with a machine no-speech verdict.
- Hosted ownership timeout: CI run 34006274787 Windows Node 20 failed closed when PowerShell listener inspection exceeded 10 seconds; timed-out tree termination succeeded. Ownership probe now has a bounded 30-second allowance with matching CLI/integration-test budgets, no retries or bypasses. Focused actual Windows ownership/MCP/CLI test and build passed (test-owner-probe-deadline.log). Full speech-presence check also completed with 384 TypeScript tests, 32 Python tests, 133 tools/five skills (check-speech-presence.log); hosted deadline-fix verification remains pending.
- Non-speech transcript review workflow: actual false tone/noise transcript revisions were imported into an isolated library, segmented, then aligned after inspect-only reconnect. All segments reported no_speech_overlap, zero speech seconds and no candidates; transcript/source hashes unchanged. Evidence nonspeech-review-13209188-fc62-4ed0-a5f8-53e530459f87. This is an actionable review signal, not automatic deletion or proof of no speech on arbitrary recordings.
- Packaged review guidance: ingest/QC and selects skills now explain checksum-selected transcript/segmentation review, pagination, machine/effective differences, unavailable-runtime semantics and no-overlap source-audio review without automatic text suppression. Fresh installation passed all five skill/tool-reference checks, 133-tool schema equality and Python isolation/recovery (package-transcript-review-skills.log). This packages the verified review procedure; model accuracy remains open.
- Transcript alignment scope accounting: each page now reports full revision/intersecting/wholly-outside segment counts independent of cursor. A regression covers before/inside/partial/after segments, stable counts across pages and partial outside-analysis duration; transcript hash unchanged. Typecheck/build and all 385 TypeScript tests passed (test-alignment-scope-counts.log). Complete pagination is no longer easily confused with complete transcript-range coverage.
- Decoded speaker coverage: alignment now reports requested versus actual PCM bounds separately and uses actual decoded duration for intersection counts, overlap and partial-range assignment authority. Regression covers truncated extraction, wholly undecoded text, explicit partial assignment and unchanged parent transcript hash. Full npm run check passed: 386 TypeScript tests, 32 Python tests, 133 MCP tools, five packaged skills, stdio/HTTP and fresh installation with Python isolation (check-decoded-speaker-coverage.log). Prior head e45efd3 CI 34006721310 and CodeQL 34006721320 passed; this checkpoint does not establish model accuracy or full-plan completion.
- Speech timing benchmark: added a reproducible MCP harness with independently decoded, zero-PCM-verified two-second leading/trailing silence around English and Mandarin positives. Both reported zero speech overlap in padding; original-window union coverage was 6.108751 seconds (76.38%) and 10.344376 seconds (82.23%). Source/generated hashes preserved. Evidence speech-timing-01b67bfd-ec90-433c-b021-c7b1b4eb59c1; fractions include pauses and are not word recall. Broader actual-recording accuracy remains open.
- Summary subtree review: node inspection now exposes every potentially truncated node in the selected subtree, including deeper leaves beneath a clean overview. Read-only derived metadata leaves saved records unchanged and works without a model after restart. Typecheck/build and 387 TypeScript tests passed before deepening the new regression; the final deeper hierarchy regression passed all seven summary tests. Fresh-package validation passed with 133 tools/five skills (package-summary-subtree-quality.log; Python isolation not requested). This preserves heuristic warnings, not factual completeness proof.
- Summary revision discovery recovery: malformed files now produce bounded unavailable records and do not hide later healthy revisions. Pagination limits examined files, including unrelated media; clients follow nextAfter through empty matching pages. Unknown media identities are explicit and error output omits damaged content. Regression covers malformed/unrelated/healthy pages and denied source scope. Typecheck/build and all 388 TypeScript tests passed (test-summary-discovery-recovery.log); fresh package validation passed (package-summary-discovery-recovery.log; Python isolation not requested).
- Actual summary revision recovery: generated a local model summary from synthetic Sonoma editorial notes, inserted isolated damaged/unrelated records, and resumed the saved cursor in a fresh inspect-only MCP process without model configuration. Healthy discovery succeeded; source, healthy summary and damaged fixture hashes preserved; sentinel private content absent. Harness qualify-summary-revision-recovery.mjs, evidence summary-revision-recovery-e7b24787-589b-4747-963c-7e3210303caa. Tool description now explicitly teaches empty-page continuation. Build and fresh-package exact tool-definition/client configuration checks passed (package-summary-recovery-contract.log).
- Visual summary discovery recovery: bounded file pages now report unavailable revisions without content, skip identifiable unrelated media, and preserve nextAfter through empty matching pages. Regression covers a new model-free library instance, healthy recovery, source scope refusal and source/summary hash preservation. Typecheck/build and 389 TypeScript tests passed (test-visual-summary-discovery.log); fresh package exact tool definitions and client setup passed (package-visual-summary-discovery.log; Python isolation not requested). Caption provenance and real model accuracy remain separate acceptance requirements.
- Combined summary/recovery validation at dc8a4e3: full npm run check passed with 389 TypeScript tests, 32 Python tests, 133 tools, five skills, stdio/HTTP, dry packing and fresh installation including actual Python missing/restored isolation (check-summary-recovery-combined.log). Reconciled optional-provider matrix against current implementation and retained connection-race/licensed-runtime gaps. This baseline does not close broader host, model accuracy, client or release acceptance.
- Runtime import integrity: modelRuntime now verifies the managed installation receipt and full dependency tree before dynamic import on each loader call. Missing receipts require explicit adoption; changed trees are refused without repair. Typecheck/build and existing 389 tests passed; an additional pre-execution sentinel regression then passed both runtime-loader tests. Actual cached CLIP, Whisper variants, DistilBART and Florence offline qualification passed with fetch blocked (offline-models-8c0d3996-1261-4f2e-8ad0-1d89cd2ad05b). Repeated hashing costs filesystem work; this is receipt consistency, not publisher authentication or an atomic check/import guarantee.
- Installed runtime integrity qualification: unchanged installed dependencies loaded, a disposable copied runtime with one added file was refused, and removing only that owned file restored loading. Original tree receipt/hash remained unchanged. Measured verification/import 2.946 seconds first load and 2.769 seconds cached import; changed-copy refusal 2.780 seconds. Harness qualify-runtime-import-integrity.mjs and evidence runtime-import-integrity-09093496-acc3-4bd5-ba5d-6fc317beb544. No model weights loaded; these warm-filesystem observations do not establish cold-start or inference performance.
- Runtime status now exposes inferencePreflight states verified/adoption_required/tree_changed with next steps and modelLoadVerified=false. Tests cover receipt adoption, unchanged runtime and changed-tree status without extra installer calls. Typecheck/build and seven runtime tests passed; actual CLI on installed dependencies returned verified/passed with model loading explicitly unverified (runtime-preflight-status.json).
- Runtime loader bounded metadata: removed the duplicate unbounded dependency package.json read and imports the canonical entry returned by receipt/status validation. Regression rejects oversized metadata before module execution. Typecheck/build and seven runtime tests passed; fresh-package validation passed (package-runtime-bounded-import.log; Python isolation not requested).
- Fresh installed native refresh at 7a4446a: managed tarball install passed actual inspect-only app/project/bin/clip reads against running Windows Avid. Independent checkout Python confirmed the saved 120-frame/30 fps Sonoma picture and two audio tracks map [0,60) to source 2850 and [60,120) to 3300. Bin/source and installed entry/archive hashes unchanged. Evidence installed-native-09af2bb0-7926-49c2-939c-d579886cf3a3 and native-refresh-feb2ae17-4d48-401f-9e48-93ad21a86010. No native mutation, new restart or render proof. CI 34007569688 for runtime-import commit 32432a8 passed Windows/macOS Node20/24 and landing; later-head CI remains separately tracked.
- Native track metadata: added GetMobTrackInfo as the 22nd allowlisted method and tracks query under current project/bin/clip scope. Actual MCP Sonoma response reported V1/A1/A2 with two segments each and timecode track; independent saved-bin range inspection and source/bin hashes passed (native-refresh-dafade78-9795-4e3c-b6c5-7fa3c57d4ecf). Typecheck/build and 390 tests passed before adding the bin-membership refusal regression; all 15 native tests then passed. Fresh package exact tool definitions passed (package-native-tracks.log). Track counts/flags do not establish a complete live timeline graph or timeline editing.
- Source-master track qualification: actual MCP returned three source media tracks with one segment each, while the sequence returned two per media track. Saved-bin sequence ranges and unchanged source/bin hashes passed (native-refresh-304002d6-4df5-475e-ad16-8c8064f47701); repeatable native reopen harness covers both. CI 34007791592 exposed Windows short-path module duplication in the test oracle and a five-second speech recovery timeout. Test oracle now imports the canonical entry; that checkpoint test has a bounded 15-second allowance. All 11 affected runtime/speech tests passed locally; hosted rerun pending.
- Qualified track defaults: GetMobTrackInfo decoding now materializes schema-defined defaults only for that method, including picture type and false flags. Real MCP sequence/source reads verified explicit boolean fields and source picture type, with matching saved ranges and unchanged source/bin hashes (native-refresh-a29f3e16-4b25-4d29-bfc0-97420d162c5e). Build, 15 native tests and fresh-package validation passed (package-native-track-defaults.log). Protocol defaults do not independently verify the editor UI or complete timeline state.
- Combined runtime/native-track check passed: 391 TypeScript tests, 32 Python tests, 133 tools/five skills, stdio/HTTP and fresh package with Python isolation (check-native-tracks-runtime.log). Native tool description now documents track inputs and graph limitations. Hosted ef1c0d1 CI passed Windows20/macOS20/24 but Windows24 hit a different filesystem-heavy visual-summary test at the default five seconds (ci-ef1c0d1-failed.log); set Windows CI default test deadline to 15 seconds, retaining explicit deadlines and production timeouts. Fourteen affected tests passed with CI=true locally; hosted confirmation remains pending.
- Native track response validation: refuse absent lists, malformed/negative segment counts, duplicate labels and inventories exceeding 256 tracks. Explicit empty lists remain distinct from missing bodies. Typecheck/build and 16 native tests passed. Actual sequence and master MCP reads passed against the running editor, with saved range/source/bin hash checks (native-refresh-9d6bf025-ddef-4e8a-9299-2b1eb1eb0a29); fresh package checks passed (package-native-track-validation.log).
- Native viewer orientation: qualified GetViewerMobs and exposed bin-scoped viewers query with MOB/type/frame/timecode and count-only omission of out-of-bin entries. Schema defaults materialized; native inventory bounded at 16 entries. Typecheck/build, 17 native tests and fresh-package checks passed (package-native-viewers.log). Actual MCP reported loaded Sonoma viewer entries and passed source/bin hash and saved timeline checks (native-refresh-a3566298-3696-4a1e-ad4d-5a607218dfc3). No seek, playback, source-time mapping or atomic snapshot claim.
- show_clip now verifies the requested MOB in Source via viewer readback instead of reading markers. Tests distinguish Record from Source and retain consumed-token behavior: typecheck/build, 18 native tests and fresh-package checks passed (package-source-viewer-verification.log). Actual source-master loading was accepted but exact identity verification failed; a subsequent read-only native probe reported Source MOB eb7d32e712898806-327fd8bbc16d-18d9 rather than requested master 36b2e93612888806-a3b2d8bbc16d-18d9. No equivalence is inferred and no automatic replay occurred. The initial harness asserted before saving its result; corrected harness now retains evidence before asserting on future authorized experiments. Real viewer identity mapping remains open.
- Viewer identity investigation: requested source master remains a bin item, but the different Source MOB is not and GetMobInfo returns no metadata (viewer-identity-investigation.json). No equivalence inferred. Separate --sequence preview/apply experiment passed exact Source MOB verification and preserved source/bin hashes (source-viewer-ddfd864a-c182-4834-9205-4c1eecd06e39). Added NATIVE_VIEWER_QUALIFICATION.md and repeatable sequence option. Master mapping, playback and seeking remain open.
- Viewer read consistency: recheck authorized project path and bin membership after viewer RPC; refuse observed changes before returning positions. Added membership-change regression; typecheck/build and 19 native tests passed. Live read-only MCP Sonoma verification passed with unchanged source/bin hashes and saved ranges (native-refresh-1db80238-3ae0-46c8-b69d-06a3f040fc58); fresh package passed (package-viewer-membership.log). Bracketed observations are not atomic and cannot detect intervening changes that revert.
- Native rename: added constrained Name-column SetMobInfo action with expected current name, existing project/bin membership and edit authority, single-use token, deprecated per-MOB failure check and Name readback. Typecheck/build, 20 native tests and fresh-package checks passed (package-native-rename.log). Real MCP temporary rename and explicit original-name restoration passed on disposable Sonoma sequence; source hash preserved (native-rename-88f3ecac-a835-4c28-852c-5334cbbb4f1c). Save/reopen persistence, atomic undo and arbitrary metadata remain unqualified.
- Rename refusal coverage: simulated per-MOB rejection despite matching readback and completed-but-unchanged Name both return renameVerified=false with observed post-state retained. Consumed-token retries are rejected and exactly one SetMobInfo call occurs. All 21 native tests and typecheck passed. This is deterministic failure-path evidence, not a new live-host rename or persistence test.
- Rename persistence: --persist harness renamed the disposable sequence, closed/saved/reopened and verified the new name, then explicitly restored the original name and repeated save/reopen. Independent saved-bin parsing at both stages confirmed duration 120, rate30 and all picture/stereo source ranges [0,60)->2850 and [60,120)->3300. Source MP4 hash unchanged; bin hashes recorded without byte-equality claim. Evidence native-rename-edf54b93-0b28-4568-ab5a-43c635d99f58. No application restart or atomic undo proof.
- Rename authority regression: inspection-only preview succeeds but apply refuses before additional native calls, consumes the token and performs no write. All 22 native tests and typecheck passed. Hosted CI 34008950666 passed the preceding bdc71c7 rename-refusal checkpoint; final/newer-head CI remains separately tracked.
- Combined viewer/rename baseline at 31c77a9: full npm run check passed with 398 TypeScript tests, 32 Python tests, 133 tools/five skills, stdio/HTTP and fresh installation with Python isolation (check-native-viewer-rename.log). README now describes track/viewer inspection and qualified rename persistence while retaining source-master identity and full-editing gaps. No release or full-plan completion claim.
- Rename character qualification: separated clip names from filesystem bin-name rules. Live multilingual probe returned Café plus question marks replacing Chinese characters, causing renameVerified=false; explicit guarded restoration using the observed name succeeded (unicode-rename-restoration.json). New names are restricted to printable ASCII, while permitting punctuation and rejecting blanks/controls/overlength. Existing expectedName remains exact for restoration. Typecheck/build and 23 native tests passed. Unicode support remains unqualified; --unicode research option now exercises refusal with the restricted schema.
- Unicode refusal end-to-end: --unicode now expects preview rejection and sends no apply request. Actual MCP test preserved observed Name and source/bin hashes (native-rename-a61307d9-ebe5-4deb-a1c2-47ecfa25ccfc). Harness rejects combining this refusal mode with --persist. This verifies the current restriction, not Unicode write support.
- Native receipt semantics: postStateRead now reports whether post-state data was obtained, independently from renameVerified/viewerVerified. Mismatched reads retain data and report read=true, verified=false; failed reads remain false. Existing failure/mismatch regressions updated and all 23 native tests, typecheck/build passed. No new live mutation was needed for this receipt-only correction.
- Bin-state receipts: open/close now report binStateVerified from target GetBinInfo.is_open, separate from read success and persistence. Live investigation found GetBins(OnlyOpen) empty after accepted reopen while direct GetBinInfo reported true (bin-state-verification.json); replaced that unsuitable oracle. Fresh guarded close/reopen passed direct bin-info verification (bin-info-state-verification.json). Updated rename persistence harness to require binStateVerified. Typecheck/build and 24 native tests passed; no full-save or atomicity claim from is_open.
- Open-bin enumeration investigation: explicit project directory with OnlyOpen, AllTypes+OnlyOpen and BinType+OnlyOpen all returned empty while GetBinInfo reported target open. Omitting project_path with AllTypes+OnlyOpen returned a nonempty list including target; OnlyOpen alone remained empty. Evidence open-bin-flag-investigation.json and open-bin-implicit-project.json. Documented request-shape dependence in NATIVE_BIN_STATE.md; no universal API-failure claim. Direct target verification retained; complete list and scoped enumeration remain separately unqualified.
- Open-bin inventory comparison: actual read-only omitted-path AllTypes+OnlyOpen enumeration matched direct GetBinInfo for all 16 project bins, with stable repeated endpoint inventory and no missing/extra entries. All were open, so closed-bin exclusion is not established. Harness validates paths within the disposable project; evidence open-bin-inventory-c1a6fa36-38b1-47cd-b274-57333624c224. Added repeatable qualification script and documented limits.
- Controlled closed-bin exclusion: closed the disposable Sonoma selects bin, confirmed omitted-path AllTypes+OnlyOpen excluded it and direct GetBinInfo still reported closed, then reopened and verified open before evaluating assertions. Evidence closed-bin-exclusion-4e28f37d-43ef-4635-b8cd-7176f1883b8c. Repeatable harness added. This closes the single-target exclusion experiment left open by the all-open inventory comparison; no concurrent-state, other-version or atomic snapshot claim.
- Combined native bin-state checkpoint: npm run check passed with 400 TypeScript tests, 32 Python tests, 133 tools/five skills, stdio/HTTP and fresh-package validation including all five generated client configurations and Python missing/restored isolation. Log check-native-bin-state.log. Hosted CI 34009688674 and CodeQL 34009688595 passed preceding a47082d; this checkpoint does not claim full-plan completion or publication.
- Native open-bin query: avid_native_read open_bins now returns at most 4096 canonical project-contained paths, strips other native fields, refuses malformed/duplicate/outside entries and brackets enumeration with current-project checks. Typecheck/build and 27 native tests passed; fresh-package exact definitions and client setup passed (package-native-open-bins.log). Actual inspect-only MCP returned 16 scoped entries including Sonoma selects (native-open-bins-a62e42f4-d485-4de0-bf65-f5d30a7958d9). A broader native-refresh attempt stopped earlier on empty source-master track metadata; no refreshed track/timeline proof is claimed. Concurrent snapshot guarantees remain open.
- Corrected native-refresh diagnosis: response checkpointing in native-refresh-e4d3f3f5-3324-433e-a092-ca2e5fd6f6eb/responses.json proves both track queries succeeded and the viewer query failed on an empty inventory. Earlier attribution to source-master tracks was incorrect. Direct investigation (native-track-investigation-eac7c67a-9f20-4533-a07a-b58d28419f6b) independently confirmed both track responses and unchanged saved-bin hash. Empty viewer inventory is now valid; show_clip still refuses to verify a loaded MOB from it. General refresh saves each response before assertions and permits no loaded viewers; exact Source-loading qualification remains separate. Typecheck/build and 28 native tests passed. Added explicit NATIVE_TRACK_DATA_UNAVAILABLE for genuinely absent track responses without automatic retry or an empty-track claim.
- Live corrected refresh passed: native-refresh-ba337030-1d25-49bd-960d-f297746affec verified sequence/master track metadata, valid empty viewer inspection, saved 120-frame/30 fps picture and two audio source ranges, unchanged bin hash and original Sonoma MP4 hash. No viewer-load mutation or new Source identity proof was performed.
- Fresh installed native inventory/viewer qualification at 9efd6d0: managed tarball installation passed actual open-bin, sequence/master track and empty-viewer reads, saved 120-frame/30 fps picture/stereo ranges, source/bin hash preservation and installed entry/archive hashes. Evidence installed-native-1a359ca7-bf96-48c0-947a-be5274da8408 and native-refresh-da513f27-64d4-42d9-a8b4-e6a6816095bf. Extended general native harness to include scoped open-bin assertions. Existing Windows host and checkout Python oracle; no clean-machine or GUI/model-driven client claim. CI 34010115910 and CodeQL 34010115891 passed ada42b7; later-head runs tracked separately.
- Combined open-bin/empty-viewer baseline passed: npm run check completed with 404 TypeScript tests, 32 Python tests, 133 tools/five skills, stdio/HTTP and fresh-package exact definitions, five generated client configurations and Python missing/restored isolation (check-native-open-bins-viewers.log). Full original feature and platform acceptance remains incomplete; no release claim.
- Native EDL preset discovery: added GetListOfExportEDLSettings as the 25th allowlisted method and project-bracketed edl_settings query with bounded names and stripped extra fields. Typecheck/build and 29 native tests passed. Actual inspect-only MCP returned Default EDL (native-edl-presets-3ac0eebc-d74e-4e89-8e20-a42528a04e46). Local ExportEDL descriptor has no request destination path; export remains unsupported pending output/dialog/overwrite and timecode/track fidelity qualification. NATIVE_EDL_QUALIFICATION.md records the next acceptance work.
- EDL preset package validation passed: fresh tarball installation matched all 133 tool definitions and five client formats, and exercised generated-command MCP connection/recovery (package-native-edl-presets.log). Python isolation was not requested for this focused package run.
- Native EDL verification prerequisite: implemented internal ordered 30 fps non-drop cut artifact contract with exact reel/track/timecode comparison, frame conversion, matching positive durations, stable SHA and rejection of truncated/unparsed/extra inline content, unsupported modes, transitions and motion effects. Typecheck/build and eight EDL parser/verifier tests passed. Not yet connected to a native export action; generated fixtures do not establish Avid export or broader EDL fidelity.
- EDL UI inspection: computer use opened Tools > List Tool in the running disposable Sonoma project. Default EDL displayed File_129, with CMX_3600 and other formats in its menu. No sequence was loaded; Preview/Save List disabled. Dismissed the menu without changing format or exporting. This establishes that a preset name does not prove CMX output; documented explicit format qualification before native export. Destination/overwrite behavior remains open.
- Actual EDL UI artifact: loaded disposable selects into Composer/List Tool, previewed current File_129 and saved through single-file Save As to a new research path. Output has two CMX-style six-digit AA/V cut events covering four record seconds; exact observed artifact contract passed verifier (sonoma-file129-ui-verification.json), SHA a5aa960b39af8b64a3260b2d90b0d43a4f14440d63f7a86a15e71f4acec6fe0f. Added padded combined-track regression; nine EDL tests passed. No independent source/channel or native RPC export claim; no overwrite exercised or format changed.
- Independent EDL timecode oracle: fresh saved AVB source-chain traversal derived the exact source/record ranges for picture and both audio tracks, including intermediate offsets and non-drop timecode origins. All three saved tracks agreed and the derived contract passed the actual UI EDL (edl-saved-oracle-907dba21-8107-4f6c-901b-11f3bf470ca3), with bin hash unchanged. Added bounded fixture research harness saving graph/contract before verification. Combined AA/V remains an observed layout, not separate exported-channel or raw-media proof.
- Native EDL RPC experiment: one explicit V1/A1/A2 export of disposable sequence completed and wrote a 594-byte file beneath the user's Avid EDL Exports directory, not the preceding UI Save As directory. No post-call dialog observed. Saved-source-chain contract passed native artifact (edl-saved-oracle-36af2aea-0125-42bb-b69c-446a8b0c6f04); raw request/responses retained in native-edl-9cd15855-0467-43f6-add0-2411b310f2a3. Added fixture-only harness with observed-destination existence refusal, exercised before any repeat dispatch. Production export remains unsupported pending destination/collision/lock recovery qualification; no overwrite or separate channel proof.
- Native EDL collision qualification: preserved the exact owned first artifact, seeded its path with a sentinel, and made one explicit collision export. Avid returned .001.edl, left the sentinel unchanged and showed no pending dialog. Explicit restoration returned original bytes. Numbered output matched first native SHA and passed independent saved-bin timecodes (edl-saved-oracle-f380635a-0398-4281-ac52-0aa0b29c586f). Evidence native-edl-53dc8a7b-8e77-4be5-8582-6a342bd4edb1. Research mode requires exact owned hash and refuses an existing numbered output; concurrent allocation and other-host behavior remain unqualified.
- EDL returned-path prerequisite: internal validator now requires one dialog-free response and canonical direct-child .edl within an authorized export directory, rejects paths in supplied prior inventory, and verifies the cut artifact. Seven output/cut tests and typecheck/build passed. Suffix allocation is accepted using the returned path. No production action yet; caller inventory completeness and pre-dispatch destination authorization remain required.
- EDL pre-export inventory prerequisite: added authorized canonical directory scanning with a bounded complete name list and refusal of overflow/symbolic/unsupported entries. Three output-boundary tests and typecheck/build passed; actual read-only Avid EDL Exports inventory found original and .001 artifacts (edl-directory-inventory.json). This supplies prior names to output verification but is not atomic, a content-preservation guarantee, or pre-dispatch control of Avid's destination.

- Guarded native EDL action: preview/apply now requires export authority, explicit authorized observed export directory, bounded inventory, full-duration 30 fps AA/V contract, safe name and existing preset. Single-use/host lock and attempt/response/receipt persistence surround returned-path and cut verification; mismatch retains uncertain lock. Actual MCP .002 export passed independent Sonoma contract (native-edl-mcp-42877291-3eb4-4e5f-9488-bafffa2d475c). Typecheck/build, 31 native tests and fresh-package validation passed. Avid controls destination; arbitrary directory configuration, separate channels and concurrency remain unqualified.

### EDL retained-lock recovery

EDL export failures retain the scoped `native-export-UUID/attempt.json` evidence path. `avid_native_lock_status` returns the lock checksum and an evidence checksum covering the attempt bytes, optional response bytes, and canonical project/bin/export-directory paths. Pass both `expectedSha256` and `expectedEvidenceSha256` to `avid_recover_native_export_lock`. Recovery requires export capability and two stopped-host checks; changed evidence retains the lock. Response evidence must resolve within its attempt directory. Recovery archives the inspected state and releases only the lock, without retrying export or deleting artifacts. This checksum does not validate exported EDL content or hash the source bin.

Validation: 16 recovery tests passed, including absent/present responses, missing or wrong evidence checksums, evidence appearing during either stopped-host check, changed attempt bytes, and out-of-directory response evidence. These are isolated tests with mocked stopped-host checks, not a live Avid shutdown/recovery qualification.

Full validation checkpoint: `npm run check` passed with 420 TypeScript tests, 32 Python tests, stdio/HTTP smoke checks, and fresh-tarball validation of 133 tools and five skills/client configuration formats including Python missing/restored isolation. Log: `.avid-mcp-analysis/check-edl-recovery.log`.

### Windows EDL prior-file alias correction

CI run 34011349873 exposed a preexisting-output comparison failure on Windows Node 20/24: lexical prior paths can differ from `realpath` output through a parent alias. Output verification now resolves every prior entry within the authorized export directory before comparison and refuses missing or out-of-scope prior entries. A junction/symlink parent regression covers this independently of runner-specific short paths. Targeted native/EDL tests: 35 passed; typecheck passed. This does not establish atomic filesystem identity or prevent native writes before post-verification.

### Native bin selection discovery

Read-only research `scripts/research/qualify-native-selection.mjs` queried the qualified Sonoma bin with `GetListOfBinItems` and `only_selected_flag:true`, bracketed by project checks. The full bin returned two members; selected filtering returned only `MCP_Sonoma_AAF_Selects`, with `mob_selected:true` and a matching bin MOB ID. Evidence: `.avid-mcp-analysis/native-selection-4b2ad5b8-e677-4a94-86e2-25b9b1357433/evidence.json`. This is actual native response evidence, not a visible-selection comparison or production selected-clips tool. Empty/multiple selection, membership changes, and UI comparison remain to qualify.

### Selected clips through MCP

`avid_native_read` now accepts `selected_clips` with a scoped bin. It returns bounded, schema-filtered selected MOBs, requires explicit native selected flags, rejects duplicate/out-of-bin identities, and checks full membership and project again before returning. It does not freeze selection or claim an atomic editor snapshot. Native tests: 33 passed; build passed. Real inspect-only Sonoma MCP evidence: `.avid-mcp-analysis/native-selected-clips-ce70cffa-d084-4a4f-abe1-84642579f624/evidence.json`; selected sequence identity matched the earlier native probe. Reproduction: `scripts/research/qualify-native-selection-mcp.mjs`. Empty/multiple selections are unit-tested; corresponding visible-host qualification remains outstanding.

### Visible selection qualification on Sonoma

Computer-use inspection of the two-item fixture bin matched the production MCP selected-clips results on this Windows 2024.12 host: the initial sequence-only highlight agreed with one returned MOB; clicking empty bin space removed highlights and returned zero; Ctrl+A highlighted both rows and the UI footer showed `Selected: 2`, agreeing with both returned MOB IDs. Clicking the sequence row restored its original single selection, confirmed by MCP. No media/timeline editing was performed. Evidence directories under `.avid-mcp-analysis/`: `native-selected-clips-d0dab61e-f80f-4288-8ad2-cbb5775d00b2` (empty), `native-selected-clips-89788039-be2c-43c7-9834-98aa99b9e1b9` (both), and `native-selected-clips-111a5c4f-c0d2-49f0-bed9-0e418419fc16` (restored). Screenshots are retained in the computer-use conversation. These sequential observations do not prove atomicity, locked/shared-bin behavior or other versions.

CI run `34011714295` for `b0ede82` completed successfully, confirming the Windows EDL path-alias fix across its configured matrix. Later selection commits require their own CI result.

### Native replacement-selection write qualification

The research-only `scripts/research/qualify-native-select-write.py` exercised `SelectMobsInBin` with an absolute fixture bin path, explicit MOB IDs, and `add_to_selection:false`. It checks the exact installed binary, listener owner, project, two fixture members, and expected current selection before dispatch; it saves attempt/response evidence before postconditions. Selecting both members returned both IDs, independent selected-only enumeration agreed, and computer-use inspection showed both rows highlighted with `Selected: 2`. A separate `--restore` invocation required the two-member selection and restored the sequence-only selection, confirmed by response and selected-only enumeration. Evidence directories: `.avid-mcp-analysis/native-selection-write-6875a8e6-bba6-4a81-9479-64049a2cbdd9` and `native-selection-write-cb69ddd5-09e9-4020-ba4a-9812b775037d`.

This is bounded research on the disposable bin, not a production MCP mutation or generic RPC tool. Production preview/apply integration, selection-race guards, failure tests, and empty-target/additive behavior remain outstanding.

### Guarded native replacement selection

`select_clips` now integrates qualified `SelectMobsInBin` replacement selection with MCP preview/apply. It requires edit capability, unique nonempty target IDs within the bin, explicit expected current selection, scoped project/bin checks, and the existing single-use token and write lock. Response IDs and fresh selected-only reads must agree with requested IDs for `selectionVerified:true`; postcondition failures remain explicit. Empty target selection and additive selection are not qualified. These are optimistic checks, not an atomic compare-and-swap with editor UI.

Validation: 35 native tests and build passed, including stale selection refusal before write, mismatch reporting, duplicate/out-of-bin target refusal and token replay refusal. Real MCP selection of both Sonoma fixture members passed in `.avid-mcp-analysis/native-select-mcp-7eb4da5d-1336-4205-a15d-f9e7921cf56a`; reproduction script: `scripts/research/qualify-native-select-mcp.mjs`.

A separate MCP preview/apply restored the original sequence-only selection with `selectionVerified:true`: `.avid-mcp-analysis/native-select-mcp-bba1eea9-3ec8-43a8-a6a1-751c921a7cd5`.

### Clear-selection qualification

Follow-up qualification establishes empty replacement selection on the installed host. `SelectMobsInBin` with no MOB IDs and `add_to_selection:false` completed with no body; selected-only enumeration returned empty. A separate expected-empty restoration returned the original sequence. Research evidence: `.avid-mcp-analysis/native-selection-write-f33ddd53-dedd-4a7c-9f72-31187f8581e9` and `native-selection-write-77bfd7f0-147f-4c8c-a042-15b9e7594a1d`.

Production `select_clips` now accepts an empty target list. Only an empty requested selection permits the observed bodyless completion, and a fresh selected-only read must also be empty for `selectionVerified:true`. Tests cover an unchanged selection despite empty completion; it remains unverified. Native tests: 37 passed; build passed. Actual MCP clear evidence: `.avid-mcp-analysis/native-select-mcp-6b5c2962-cbb7-46f8-91c5-ad64cf8a763e`. Fresh-tarball package validation passed with 133 tools/five skills and exact tool definitions across five generated client formats; Python isolation was not requested in this run. Log: `.avid-mcp-analysis/package-selection.log`. Additive native selection remains unqualified.

MCP restoration from empty to the original sequence passed separately: `.avid-mcp-analysis/native-select-mcp-5ab05ef4-04d7-4c1a-af47-840bad1dba1c`.

Selection failure acceptance: 40 native tests passed, adding an applied-change/lost-response simulation, edit-authority refusal, and project-change rejection. Token reuse and stale expected-selection reuse both fail after the injected lost response. These are simulated failures; no live transport disruption was induced. Client workflow and limits are documented in `docs/NATIVE_SELECTION.md`. CI `34012113076` for replacement-selection commit `f2868fc` passed.

### Sonoma transport advancement experiment

Computer-use inspection started with the four-second selects sequence at `01:00:00:00`. Clicking the Record viewer Play control advanced the UI timecode/playhead to `01:00:03:29`. The production `viewers` MCP read independently reported the exact sequence MOB in Record at frame 119/timecode `01:00:03:29`; Center remained at frame 0. Clicking the timeline start returned Record to frame 0, confirmed by another MCP read. Reproduction read harness: `scripts/research/qualify-native-playback-position.mjs`; evidence: `.avid-mcp-analysis/native-playback-position-7d58d027-fa0b-42eb-a46b-a524c866a811` and `native-playback-position-14f1a367-c756-4046-abb8-a7b8003ddf7c`.

The captured monitor remained black. This demonstrates transport advancement and position reporting only; it does not establish visible decoded video, audible output, perceptual sync, or playback fidelity. The cause of the black capture remains unresolved and must not be assumed to be either missing media or a capture limitation. The playhead was restored to the sequence start.

Black-viewer follow-up: closing the floating List Tool did not reveal video. Toggling the V1 monitor control off and back on also left the capture black; monitoring was restored. The List Tool is closed and the playhead remains at sequence start. These checks do not distinguish editor rendering from capture limitations, and playback fidelity remains open.

Full selection checkpoint: `npm run check` passed with 430 TypeScript tests, 32 Python tests, stdio/HTTP transport checks, dry packing and fresh-tarball installation of 133 tools/five skills. Generated configurations for five clients matched; Python isolation was available, rejected when missing and restored. Log: `.avid-mcp-analysis/check-native-selection.log`. These checks do not resolve black viewer capture or broaden native host qualification.

### Native sequence copy investigation

Research script `scripts/research/qualify-native-copy.py` created a new disposable empty bin, then called `CopyBinItems` for the Sonoma sequence using absolute source/destination paths. Avid completed and returned a new MOB ID, found in destination `MCP_Copy_969f92a0264a.avb` with name `MCP_Sonoma_AAF_Selects.Copy.01`. The first harness assertion incorrectly expected source-ID preservation and stopped after saving evidence. No copy was replayed. Inspection establishes a new identity matching the returned ID; the harness assertion now reflects that observed contract, but its revised full run has not been repeated.

Evidence: `.avid-mcp-analysis/native-copy-b70cce3c-904f-488a-8cf3-abfb19e13a35`. Source membership and saved AVB hash were unchanged within the experiment. Read-only follow-up metadata in `metadata.json` reports both original and copy at 30 fps and 120 frames. New identity and matching duration do not prove timeline/source-graph equivalence, media duplication semantics, saved persistence, undo, or rendered fidelity. Destination is retained for further inspection. Production copy integration remains outstanding.

### Copied sequence persistence and saved graph comparison

Initial offline inspection found the newly copied destination still empty on disk despite native membership. The qualified adapter closed and reopened only `MCP_Copy_969f92a0264a.avb`; both bin-state postconditions passed and native enumeration retained the new copy MOB. Evidence: `.avid-mcp-analysis/copy-save-b7c4182f-e43f-49f6-8201-674c19437f97`; harness `scripts/research/qualify-copy-persistence.mjs`. This demonstrates why application completion is not saved persistence.

After that save/reopen, `scripts/research/qualify-native-copy-graph.mjs` compared decoded sequence fields excluding its changed name/identity, plus reachable source nodes. Sequence fields (including tracks, source bounds, rate and duration) and four source nodes matched exactly. Both graphs contain the same unresolved terminal source ID; the comparison reports `sourceGraphComplete:false` explicitly and does not infer what that reference represents. Evidence: `.avid-mcp-analysis/native-copy-graph-f99baf24-f279-46af-b947-e2c15d01ab97`. Initial failed assertions and raw parsed graphs were retained in earlier run directories. This establishes decoded structural equality for the saved fixture, not unknown-field preservation, full source resolution, media duplication semantics or playback fidelity.

### Guarded copy integration

`copy_clip` integrates native `CopyBinItems` with the existing edit-capability, single-use preview/apply and per-user write lock. It accepts one source member and a different, existing empty destination bin in the authorized project. Source/destination saved hashes and membership enter the preview state, so observed destination changes reject dispatch. A returned new MOB ID must match the destination's sole member, source membership must remain unchanged, and project checks must agree for `copyIdentityVerified:true`. Receipts explicitly keep `persistenceVerified:false` and `sourceFidelityVerified:false`; this is not move, file duplication, saved graph or playback verification.

Native tests: 42 passed; build passed. New cases exercise stale/nonempty destinations, same-bin refusal, new returned identity agreement/mismatch and token consumption. Actual copy behavior has only been qualified on the Sonoma sequence fixture; other clip types and shared/locked destinations remain unqualified.

Actual MCP copy passed: `.avid-mcp-analysis/native-copy-mcp-84628185-d316-49be-80bb-8d117effd24d`, producing `.Copy.02` with a response-matched new ID and unchanged native source membership. This run created a fresh disposable destination through MCP, then copied via a separate preview/apply. The new destination remains available for persistence qualification. Harness: `scripts/research/qualify-native-copy-mcp.mjs`.

MCP copy persistence passed through separate close/open preview/apply operations: `.avid-mcp-analysis/native-copy-persistence-mcp-16e7ce15-6ab8-4844-9352-e2b19232df58`. Saved comparison passed in `native-copy-graph-f6140a57-8014-496e-8c9f-53d1b9884a26`: decoded sequence fields and four source nodes equal; same unresolved reference, `sourceGraphComplete:false`. Harnesses: `qualify-native-copy-persistence-mcp.mjs` and `qualify-native-copy-graph.mjs --mcp-copy`. Native tests now total 43 passing, including a lost copy response after destination population: token replay and a new preview into that destination are refused. The injected failure is not a live transport fault test. User workflow is documented in `docs/NATIVE_COPY.md`.

### Master versus sequence copy identities

Batch research copied the fixture master and sequence into new `MCP_Copy_9a8c903d19f4.avb`: the master preserved its MOB ID, while the sequence received a new ID and `.Copy.03` name. Returned identity order differed from destination enumeration order, so no positional source-to-copy mapping is claimed. Evidence: `.avid-mcp-analysis/native-copy-4d692698-6970-4348-bfc0-0ad84c3008bb`. A separate master-only call reproduced preserved identity in `MCP_Copy_6cf367b1d5d5.avb`: `native-copy-173d56f1-01a1-436a-8f8c-7b8c3f9e8864`. Source membership and saved bytes remained unchanged in both runs. These are copy membership observations, not media-file duplication proof.

The production single-item copy check now permits a returned ID equal to the source ID, following the qualified master behavior. It still requires the destination's sole member to match that returned ID and source membership to remain unchanged. This corrects the earlier sequence-specific new-ID assumption. Native tests: 44 passed; build passed. Batch production support, batch mapping and master saved-graph conformance remain outstanding.

Live MCP master copy passed with preserved identity: `.avid-mcp-analysis/native-copy-mcp-0d3ef1c4-0876-4a3c-91fa-0df29cc04bd9`. The destination was created separately through MCP; no save/reopen or rendered-fidelity claim is made for this run.

Batch integration: `copy_clips` accepts unique, nonempty source IDs and shares guarded copy preview/apply with `copy_clip`. Postconditions compare identity sets and requested count, reject duplicate/partial/extra destinations, and preserve explicit false persistence/fidelity fields. Source-to-copy positional mapping is not inferred. Native tests: 48 passed; build passed. Large batches, partial live failures and batch saved-graph conformance remain unqualified.

Live MCP batch copy passed for the Sonoma master and sequence: `.avid-mcp-analysis/native-copy-mcp-8bc93cd7-10d2-4b6b-acd1-cc69a236fdba`. Both destination identities matched the returned set, source membership remained unchanged, and the sequence name was `.Copy.04`. The destination remains available for separate persistence comparison.

Batch persistence: separate MCP close/open operations retained both expected identities in `MCP_CopyMCP_a9c7f7a1ad75.avb`. Evidence: `.avid-mcp-analysis/native-copy-persistence-mcp-5ed38261-ce4a-43bb-b3c9-92e42e427fe5`. Saved comparison in `native-copy-graph-af30b0ad-608b-46ba-a0cd-2f65abd28742` found equal decoded sequence fields and four reachable source nodes, with the same unresolved reference and explicit incomplete source-graph status. Reproduce with `qualify-native-copy-persistence-mcp.mjs --batch` and `qualify-native-copy-graph.mjs --batch`. This expands fixture persistence evidence; it does not establish large-batch performance, file duplication or playback fidelity.

Full copy checkpoint: `npm run check` passed with 438 TypeScript tests, 32 Python tests, stdio/HTTP smoke checks, dry packing and fresh-tarball installation of 133 tools/five skills. Tool schemas exactly matched the checkout; five generated client formats and Python available/missing/restored isolation passed. Log: `.avid-mcp-analysis/check-native-copy.log`.

Source-reference coverage now accompanies saved snapshot range/complexity reports and per-bin diff/usage coverage. It counts direct references, distinct resolved/unresolved/ambiguous IDs, and bounded samples separately from parsing `complete`. `allReferencesResolve` means only that each referenced identity has exactly one matching MOB in that saved bin; it does not validate cycles, source ranges, terminal semantics or media availability. Existing snapshots receive computed coverage without migration. Eighteen snapshot tests, typecheck and build passed. Real Sonoma result: 15 references, five distinct IDs, four resolved and one unresolved, while parsing remains complete. Evidence: `.avid-mcp-analysis/source-reference-coverage-30979c95-b55b-402b-9c7a-31c3e59d5d68`; harness `scripts/research/qualify-source-reference-coverage.mjs`. The unresolved ID remains unclassified.

Cross-bin source coverage now accompanies range and per-bin diff/usage coverage, alongside local resolution. Tests cover a source resolved only in another captured bin and duplicate identities across bins. Nineteen snapshot tests, typecheck and build passed. Real original-plus-copy Sonoma capture found four repeated source IDs and the same one unresolved reference: `.avid-mcp-analysis/source-reference-coverage-1df24a18-0985-4c43-84ef-c578a1074eb3`. Local coverage still reports four uniquely resolved IDs. This exposes ambiguity without choosing a record by name or claiming media loss. Reproduce with `qualify-source-reference-coverage.mjs --with-copy`.

Added `avid_saved_source_resolution` for complete paginated source-ID discovery beyond the ten-example coverage summaries. Tests exercise 23 IDs over three pages and invalid cursors/page limits. Real MCP Sonoma snapshot qualification returned five IDs over three pages (four ambiguous, one unresolved), without duplicate or omitted IDs: `.avid-mcp-analysis/source-resolution-mcp-e0f84c85-0104-40a2-981e-3360a4457e41`. The development inventory is now 134 tools; stdio smoke and 21 targeted server/snapshot tests passed. Prior 438-test full-check evidence predates this new tool.

Fresh-package source-resolution acceptance now invokes the installed tool against a 12-source fixture across pages, covering one resolved identity, one ambiguous identity and ten unresolved identities without duplicates. `npm run smoke:package -- --with-python` passed: 134 tool definitions exactly matched checkout, five client formats agreed, installed snapshot workflows passed, and Python available/missing/restored isolation passed. Log: `.avid-mcp-analysis/package-source-resolution.log`. This is installed-server synthetic classification evidence, complementary to retained real Sonoma MCP reads, not a media availability oracle.

Historical repeated-MOB queries now accept an explicit captured bin path for range and complexity, avoiding a new capture just to disambiguate copies. Tests cover selecting each of two same-ID records, default ambiguity refusal, deleted-file history and invalid/nonmember paths. Twenty-one snapshot tests, typecheck and build passed. Actual MCP queried both captured Sonoma master records by their respective bin paths; each reported 5726 frames, while an unscoped complexity query rejected ambiguity. Evidence: `.avid-mcp-analysis/source-resolution-mcp-5c646eef-52b8-48b7-b76e-f8a2b6628eca/bin-targets.json`. This identifies the chosen saved record; it does not assert equivalent media or merge duplicate records.

### Bounded saved-source range tracing

`avid_trace_saved_sources` adds diagnostic traversal through captured equal-rate picture and sound source chains, with explicit stops for unsupported or ambiguous structures, gaps, overlaps, cycles, depth and source bounds. Local-bin identity takes precedence; absent local identities require a unique captured cross-bin match. Output is capped at 500 steps by rejection, not silent truncation. No terminal media classification or playback claim is added. See `docs/SAVED_SOURCE_TRACING.md`.

Actual Sonoma MCP tracing returned 24 steps across V1/A1/A2 and preserved source offsets, ending at the known unresolved source identity with `incomplete:true`. Evidence: `.avid-mcp-analysis/source-resolution-mcp-5fe637dd-27b2-4c75-b8c1-2893c91b2e30/trace.json`. Six engine tests cover clipped mapping, cycles/depth/rates, local precedence/cross-bin ambiguity, unsupported/gapped/overlapping nodes, target bounds/tracks and output limits.
Full verification passed: 448 TypeScript tests, 32 Python tests, 135-tool stdio/HTTP smoke, dry packing and fresh installation with exact tool definitions, five client formats and Python isolation/recovery. Log: `.avid-mcp-analysis/check-source-trace.log`. These checks do not close the remaining host, media-quality, installer or platform acceptance gaps.

### Managed-package recovery with a running quarantined server

The removal harness now starts and pings an actual installed MCP server from a renamed `UUID.removing-UUID` directory. Recovery must refuse while that server runs, retain the entry checksum and leave the original destination absent. After closing the server, recovery must restore an unchanged tree and the restored entry must pass a new MCP ping. The harness then removes both disposable installations and verifies the external archive checksum.

This passed on Windows using two fresh installations of the 135-tool branch package. Installation, configuration activation, rollback and entry removal passed first. Evidence: `.avid-mcp-analysis/package lifecycle 2ccd2c4a-358e-4fa3-8854-f80074ad1ecc/evidence.json` and `removal-evidence.json`; logs: `package-quarantine-install.log` and `package-quarantine-removal.log` under `.avid-mcp-analysis`. Existing production path-prefix process checks already cover the renamed path; no production change was necessary. This adds real process/recovery evidence, not different-version compatibility, clean-machine dependencies, named-client GUI or arbitrary concurrent-writer qualification.

### Channel-aware saved source tracing

The source tracer now follows both members of the parser-qualified stereo combiner instead of treating their intentional overlap as a generic overlap error. It validates paired full bounds, distinct channel indices, count, sound media kind and nonopaque direct-source components. Unexplained overlaps and malformed pairs remain explicit stopping conditions. Direct trace steps preserve channel labels; deeper steps follow the selected source track independently.

Build and 33 focused snapshot/trace tests passed, including different downstream offsets for the two channels and missing/mismatched/opaque/non-source/picture pair refusal. Real MCP stereo tracing across the PCM fixture cut returned the four expected channel ranges and continued into deeper sound sources: `.avid-mcp-analysis/stereo-timeline-be9a8a35-c17e-48d6-8bab-68608c45c177/evidence.json`. The bin checksum was unchanged; 24 steps remained incomplete at unresolved sources. Ordinary V1/A1/A2 tracing and source-resolution pagination also passed again in `source-resolution-mcp-7094c223-008c-4902-918d-8b0dc3bd4086`. This does not close arbitrary effects, mixed rates, nested subclip target semantics or playback acceptance.

### Installed source-trace contract

Fresh-tarball acceptance now invokes `avid_trace_saved_sources` against a captured synthetic stereo fixture, verifying both channel labels, clipped first-level ranges, different downstream offsets, explicit unresolved endpoints and rejection beyond the MOB duration. This passed with all 135 tool definitions matching the checkout, five client configuration formats, foreign-working-directory startup and Python availability/missing/restored isolation. Log: `.avid-mcp-analysis/package-stereo-trace.log`. The installed fixture verifies packaging and API behavior; the separate Sonoma saved-bin evidence establishes the observed Avid structure. It does not qualify named-client model invocation or mixed-rate/terminal-media mapping.

### Pinned face-model notices

Verified the YuNet MIT and SFace Apache-2.0 notices at the configured OpenCV Zoo revision. Exact upstream bytes now ship under `docs/licenses/`; the face downloader verifies notice length/hash before writing, and runtime reads require matching notices alongside model hashes. Separate license constants preserve existing model manifest compatibility. Missing/changed notices are rejected without silently overwriting them. See `docs/FACE_MODEL_PROVENANCE.md` for source links and repair behavior.

Build and 15 focused tests passed. The existing local model cache passed with installation disabled (`.avid-mcp-analysis/face-license-verification.json`). Fresh-tarball installation passed including both packaged notice checks, exact 135-tool definitions, installed stereo tracing, five client formats and Python isolation/recovery (`package-face-notices.log`). Other models, runtime dependencies, training-data provenance and installer redistribution still need their own audit; this does not close the entire license requirement.

### Full verification after stereo tracing and face provenance

`npm run check` passed on source commit `74f6818`: 455 TypeScript tests, 32 Python tests, 135-tool stdio/HTTP checks, dry packing and fresh-tarball installation. Installed verification exercised snapshot pagination/discovery, stereo source ranges and refusal behavior, exact face-license bytes, five client formats and Python availability/missing/restored isolation. Evidence: `.avid-mcp-analysis/check-face-provenance.log`. GitHub CI run `34014445463` was still running when this local result was recorded; no CI-success or overall-completion claim follows from the local pass.

### Optional conversion-model notice inventory

Added and ran a bounded pinned-revision metadata/notice audit for the five configured Hugging Face models. Exact commit identities matched. README hashes and source URLs are retained in `docs/model-provenance-inventory.json`; no separate LICENSE/NOTICE appeared in those repository listings. DistilBART and Florence metadata declare apache-2.0 and mit respectively; CLIP and both Whisper conversion metadata omit a license identifier. No licensing conclusion is inferred from omission. See `docs/OPTIONAL_MODEL_PROVENANCE.md` for remaining original-model, conversion and runtime provenance work. Local evidence: `.avid-mcp-analysis/model-provenance-69074306-48e5-4aff-a0ec-fafb25106d46/evidence.json`. Only metadata/notices were read; no model cache changed.

### Upstream conversion provenance

The metadata audit now requires an explicit, validated upstream repository identifier from each pinned conversion card, resolves the observed upstream commit and inventories its notice hashes. All five mappings passed. `docs/model-provenance-inventory.json` distinguishes observed upstream revisions from unknown original conversion revisions. Florence's standalone upstream notice is retained byte-for-byte with attribution; the remaining upstream model listings have README-only notice inventories and require original-project reconciliation. Evidence: `.avid-mcp-analysis/model-provenance-70328b00-8f7e-43d8-a868-33ee46022652/evidence.json`. This changes audit artifacts and bundled notice text, not runtime behavior or claims of complete license clearance.

### Original CLIP and Whisper notices in installed packages

Retained the original projects' MIT notices with OpenAI attribution and immutable source commits/hashes, alongside the Florence notice. `docs/original-model-notices.json` records all three. Documentation distinguishes Whisper's explicit original code/weights MIT statement from the inspected Hugging Face Apache-2.0 metadata, without claiming complete conversion-chain clearance. Fresh-tarball smoke passed with exact bytes for all three original notices plus both face notices, 135 matching tool definitions, stereo tracing, client formats and Python recovery. Log: `.avid-mcp-analysis/package-original-notices.log`. Runtime behavior and model caches were unchanged; further provenance work remains listed in `docs/OPTIONAL_MODEL_PROVENANCE.md`.

### Notice delivery during explicit model setup

CLIP, both Whisper selections and Florence now retain the recorded original-project notice in a revision-scoped cache notice directory during explicit setup. The helper verifies bundled bytes, creates files exclusively, reuses matching files, rejects changed notices without overwrite and refuses linked notice directories. Existing offline model loading remains read-only and compatible. Summary/diarization notices and complete conversion-chain clearance remain outstanding.

Build and 10 selected tests passed across model-notice and caption test files. Fresh-tarball verification exercised actual installed helper creation/reuse for all four mappings, plus existing 135-tool definitions, source tracing, notice hashes, client formats and Python recovery. Log: `.avid-mcp-analysis/package-cache-notices.log`. This checks installed notice delivery without downloading weights; it is not a new full model installation or inference benchmark.

### Model-loader notice boundary tests

Added eight loader-level tests covering CLIP, Florence and both Whisper selections: explicit setup invokes the correct revision notice before runtime access; offline loading skips notice creation; a notice error prevents runtime/model loading. The existing speech setup test now uses an isolated temporary cache rather than a relative checkout cache. All 27 selected tests passed across loader notices, notice filesystem behavior, speech and captions. The previous 10-test checkpoint's file label was corrected to model-notice/caption tests; it did not include the nonexistent speech-options test file. These are mocked model-runtime boundary tests, not new full-model download or inference evidence.

### Real CLI model setup with retained notices

The new `qualify-model-notice-setup.mjs` harness ran the actual setup CLI sequentially for CLIP, both Whisper selections and Florence against the existing local cache. All four returned the expected pinned model/revision after loading/disposal; subsequent notice checks matched and reused existing notice files. Per-model results and bounded CLI responses are retained under `.avid-mcp-analysis/model-notice-setup-3f651605-cb3e-4177-8718-ad44764072af`; aggregate log: `model-notice-setup.log`. The earlier isolated Whisper-English command also passed. This advances actual setup integration beyond mocked loaders and packaged notice helpers; clean-machine dependencies, transfer accounting and inference quality remain separate. CI for `31fc17c` was still running when last checked.

### Concurrent notice publication

An initial unforced 50-caller run passed, but did not exercise the interval between final-file creation and write completion. Notice setup now stages complete bytes and publishes with an exclusive hard link, preserving no-overwrite behavior while avoiding that interval. A deterministic paused-write test verifies another caller can publish a complete notice while the first is paused, then the first reuses it. Build and 14 focused tests passed. A live 50-caller run produced exactly one creation, 49 reuses, the expected digest and no staging files remaining: `.avid-mcp-analysis/notice-concurrency-01c031cf-a9f6-43f5-80d5-01ab2658b1c4/evidence.json`. Harness: `qualify-model-notice-concurrency.mjs`. Hard-link support is required; process-kill cleanup and power-loss durability remain unqualified.

### Notice setup after interrupted staging

Added and ran a reproducible child-process interruption harness. An injected filesystem write leaves 12 bytes in the production helper's staging file and pauses before publication; the parent verifies no final file, terminates its owned child and confirms exit before retrying. Actual helper retry publishes the expected full checksum, preserves the partial file and succeeds on later reuse. Evidence: `.avid-mcp-analysis/notice-interruption-05d8cb00-27b8-4fd3-a6a7-56be0800b458/evidence.json`. This qualifies retry after a controlled partial-write interruption; it does not qualify power loss, external-writer interference or stale-file deletion.

### Larger speech-model candidate research

Pinned Whisper base q8 (`1846881b6b3a3024392c1eea3ad983695bc23925`) loaded and inferred locally through the existing optional runtime. A new research harness compared it with tiny on the retained Mandarin fixture using identical production audio decoding and explicit zh transcription. Text matched exactly, raw CER remained 13/41 (including script/numeral differences), and observed inference was 1323 ms versus tiny's 926 ms. Segment timings differed without independent timing ground truth. Source unchanged. Evidence: `.avid-mcp-analysis/speech-base-comparison-0e31cb02-5726-409e-8a20-b4a2de550a9e/evidence.json`; see `docs/SPEECH_BASE_QUALIFICATION.md`. This proves candidate loading/inference compatibility, not an accuracy improvement or production integration. CI for `31fc17c` was verified successful separately.

### English/noisy speech candidate comparison

Extended the pinned-base research harness to accept checksum-bound en/zh fixtures and report English word errors. Generated and tested an original 58-word Microsoft David passage clean and mixed with deterministic pink noise. Tiny scored 0/58 word edits in both; base scored 1/58 (`close` became `closed`) and took longer in both single runs. Both fixture hashes were unchanged. Evidence: `.avid-mcp-analysis/speech-english-fixtures-d6ae59e4-b323-4927-8907-27fe6c6ab59a/evidence.json` with linked full comparisons. No default/model-selection change follows from these limited synthetic results. Real-speaker, broader noise/language and checkpoint qualification remain open; see `docs/SPEECH_BASE_QUALIFICATION.md`.

### Optional Whisper base production choice

Added pinned q8 base to explicit setup, transcription/job options, notice delivery and checkpoint option validation, retaining tiny.en as the default. Automatic transcription language selection uses the chosen base model; the independent language-detection tool remains tiny-based. Actual setup and MCP explicit-English/auto transcription passed with completed base checkpoints and unchanged source in `.avid-mcp-analysis/speech-base-mcp-555476e4-0e0f-42fd-8e19-a7b04c0bcc10`. The retained statuses were independently checked for completed state and expected options. This is an optional choice, not a claimed accuracy upgrade: earlier synthetic comparisons showed no benefit.

Full `npm run check` passed: 473 TypeScript tests, 32 Python tests, 135-tool transports, dry packing and fresh installation including five cached-notice mappings and exact tool definitions. Log: `.avid-mcp-analysis/check-speech-base.log`. The provenance inventory was refreshed for six configured conversion models (`model-provenance-f22a415e-2e88-4ff9-bcc2-a6fea7e4c0b4`). Base long-run/process-kill resume, broader resource and speech-quality qualification remain open; no full-goal completion claim is made.

### Base worker cancellation and checkpoint reuse

The actual MCP resume harness now accepts `--base` and checks the persisted model selection. On 180 seconds of Sonoma, cancellation preserved one of nine windows; reconnect and resume reused it, completed all nine and produced exactly the same 54 segments as uninterrupted inference. Parent checkpoint bytes, saved language decision and source checksum were unchanged; completed-run resume was rejected. Evidence: `.avid-mcp-analysis/speech-resume-3e895d43-c00e-4e2c-9496-68aee2e7d352/evidence.json`. All 12 speech tests passed, including newly parameterized base failure/recovery and automatic-decision persistence cases. This closes the controlled base worker cancellation/reconnect case only; abrupt host termination, power loss, broad resource/accuracy qualification and the overall plan remain open.

### Forced host-tree speech recovery

The Windows-only `--host-tree-kill` qualification passed with base/auto on 180 seconds of Sonoma. The harness obtains its owned host PID from the transport, requires successful forced tree termination and transport closure before reconnecting. Retained Windows output confirms host and descendant termination. Resume reused one checkpoint, completed all nine windows and matched all 54 uninterrupted segments; source and parent checkpoint bytes remained unchanged. Evidence: `.avid-mcp-analysis/speech-resume-53138de8-7950-4c98-896e-e60c2d27dda1/evidence.json`. This extends the controlled cancellation evidence to forced termination of the owned process tree. Parent-only termination with surviving workers, filesystem publication-phase coverage, power loss and broad resource/accuracy qualification remain open. GitHub CI and CodeQL for production base commit `ea17b45` were separately verified successful.

### Summary setup license delivery

Explicit DistilBART setup now verifies and publishes bundled standard Apache-2.0 text before loading the model, matching model-card declarations. The helper distinguishes standard license text from an original model attribution notice; full provenance remains open. Build and 27 focused tests passed. Actual CLI summary setup passed and its retained cache notice matched the recorded checksum (`summary-notice-setup.log`). Fresh-tarball acceptance passed with six cached model mappings and all 135 tool definitions matching (`package-summary-notice.log`); that run's result label still said five mappings, subsequently corrected without changing its six-entry loop. Python MCP isolation was not requested in this targeted package run. See `OPTIONAL_MODEL_PROVENANCE.md` for source and remaining limitations.

### Full verification after summary license integration

`npm run check` passed on source commit `c175122`: 478 TypeScript tests across 81 files, 32 Python tests, 135-tool stdio/HTTP checks, dry packing and fresh-tarball acceptance. Installed verification included six cache notice mappings, exact tool definitions, stereo source tracing, five client formats and Python available/missing/restored isolation. Log: `.avid-mcp-analysis/check-summary-license.log`. `npm audit --omit=dev --json` returned zero advisories for the checkout's npm production dependency graph (`audit-production-summary-license.json`); this does not cover separately installed model/Python runtimes or resolve the distinct GitHub default-branch alert count. The full feature/host/client/quality requirements above remain open.

### Bounded analysis-worker tree cancellation

Analysis jobs now use the existing bounded Windows tree-termination helper instead of an unbounded taskkill invocation. The tree outcome is exposed and retained in the job journal, including failure reasons. Worker closure remains necessary for terminal cancellation; a failed tree attempt falls back only to the same still-owned worker and does not establish descendant termination. Late outcomes persist without targeting a subsequently dispatched worker. Missing outcome fields in old records remain valid and do not imply successful tree termination.

Build and 16 focused tests passed, including delayed/failing tree outcomes and journal persistence. The final eight-job-test rerun also passed. Actual base Sonoma cancellation/reconnect/resume passed with one reused window, nine completed windows and exact uninterrupted segment equality; the terminal MCP result and saved journal both reported successful Windows tree termination. Evidence: `.avid-mcp-analysis/speech-resume-dcec5518-6ed3-4c7b-9911-f469cb575d21/evidence.json`. Parent-only host crashes with surviving workers and broad recovery qualification remain open.

### Tree-termination deadline correction

Inspection found that the shared helper requested termination after five seconds but still waited indefinitely for taskkill closure. It now resolves at that deadline with `succeeded: false` and explicit unverified termination-process/descendant closure, even if its kill request returns false or throws. A late successful exit cannot revise the already returned failure. This is a bounded observation result, not proof that the termination process or descendants stopped; job cancellation still requires worker closure.

Build passed before the final kill-exception handling addition; all 21 focused process/tree/job/journal tests passed on the final source. Deterministic deadline tests cover a never-closing process, failed kill request, thrown kill request and late close. The earlier Sonoma run covers the normal successful path only; no real hung operating-system process was induced for this correction.

### Command-runner termination integration

Added deterministic tests using the actual command runner and actual termination helper together, with simulated child handles. When the owned worker closes before a nonclosing taskkill process, the caller waits for the five-second helper result and rejects with the original timeout code plus explicit unverified descendant evidence. When the worker remains alive, the caller requests direct termination/escalation and remains pending until worker closure. Late taskkill success cannot change an already returned failure. All 23 focused tests passed across runner integration, real child execution, tree termination, jobs and journal. This validates event ordering and evidence propagation; it does not prove termination of a real unresponsive operating-system process.

### Inventory stream and camera metadata

The existing HTML inventory report now includes each recorded stream's codec/profile, dimensions, pixel format, frame rates, time base, duration/start, color declarations, field order and audio format/layout. An explicit allowlist displays available container/stream camera and timecode tags; absent fields are labeled rather than inferred. Values are HTML-escaped. Reports state that declarations do not verify camera identity, fidelity or delivery compliance. The existing report tool and result contract are retained.

Build and two renderer tests passed, covering separate streams, escaped hostile tags, omitted unselected tags and missing metadata. Actual Sonoma indexing/report generation passed with source checksum unchanged: `.avid-mcp-analysis/inventory-report-391b24e5-eb97-4d79-9645-c7d06483da3e/evidence.json`; reproducible harness: `scripts/research/qualify-inventory-report.mjs`. This is generated-HTML evidence; browser layout, camera-specific source fixtures and independent color/metadata accuracy remain unqualified.

### Inventory browser layout qualification

Real Chromium inspection found 524px document width at a 390px viewport from a long Sonoma filename, plus cramped four-column inventory values. Reports now wrap long headings and use labeled stacked inventory fields below 600px; stream detail tables retain two columns. After regenerating the Sonoma report, browser measurements showed document width equal to viewport width at 320, 390 and 1280px, with mobile block cells and desktop table cells. Desktop and mobile screenshots were inspected: `.avid-mcp-analysis/inventory-mobile-after.png` and `inventory-desktop-after.png`; the before screenshot is retained too. Build and real report generation passed, source unchanged (`inventory-report-369f797a-e506-4af0-8b63-904f1eb83045/evidence.json`). The temporary loopback server and owned browser session were closed. This covers the observed report layout, not camera metadata accuracy or full accessibility acceptance.

### Inventory report through MCP and scoped reconnect

The report qualification harness now uses real stdio MCP calls for Sonoma indexing and report generation. After reconnecting with only inspect enabled, it requires the exact CAPABILITY_DENIED code and export requirement, unchanged directory contents and unchanged report bytes. Source SHA-256 is checked before and after. The final harness passed: `.avid-mcp-analysis/inventory-report-a52b3952-8e5d-4b27-832a-56b911706795/evidence.json`. This advances direct-library evidence to transport/tool/capability integration; named-client model invocation and independent metadata accuracy remain open.

### Inventory source validation before publication

Inventory generation now verifies each resolved source checksum against its indexed identity before writing HTML. Previously it checked path availability while using cached metadata, allowing a modified source to be presented under its old checksum. A regression test creates a report, changes only its disposable source fixture, and verifies refusal without a new report or changes to the previous report/source. Build and nine library/renderer tests passed. Actual Sonoma MCP generation plus inspection-only export refusal passed again with source unchanged (`inventory-report-6be3a13d-fbad-4e24-b241-3ee45332d844/evidence.json`). This is a pre-publication content check, not continuous filesystem locking or protection against edits after validation.

### Installed inventory report acceptance

Fresh-tarball verification now imports the installed library and exercises report rendering with a synthetic indexed record: separate stream index/codec, escaped camera tag and mobile field labels. It then changes the disposable source and requires the specific stale-source refusal, unchanged report directory contents and unchanged previous HTML. This passed alongside all 135 matching tool definitions, six notice mappings, five client formats and installed source tracing (`.avid-mcp-analysis/package-inventory-report.log`). Python MCP isolation was not requested in this targeted run. The synthetic record tests packaging and behavior; separate Sonoma MCP/browser evidence covers actual media and layout.

### Full verification after cancellation and inventory changes

Full `npm run check` passed on source commit `64e1bde`: 486 TypeScript tests across 83 files, 32 Python tests, 135-tool stdio/HTTP checks, dry packing and fresh-tarball installation. Installed acceptance included six notice mappings, inventory rendering/source refusal, exact tool definitions, source tracing, five client formats and Python available/missing/restored isolation. Log: `.avid-mcp-analysis/check-inventory-cancellation.log`. Current-head CI was still running when checked; CodeQL for `2d7829a` was successful. This closes the combined local regression checkpoint, not the broader native/client/quality requirements.

### Source-trace diagnostic ranges

Gap, overlap and affected-component stopping diagnostics now identify their clipped interval instead of reporting the whole query. Mixed-rate stops include both rates and source identity/bin with sourceRangeConverted:false; traversal remains stopped and no conversion claim is made. Build and 13 source-trace tests passed, including separate leading/trailing gaps around a mixed-rate node. The real saved Sonoma MCP harness passed with three source-resolution pages and 24 reference/unresolved steps (`source-resolution-mcp-26271672-13c5-4271-808d-e63204570dbb`). Mixed-rate conversion still requires independent semantics and host qualification.

### Source coordinate interpretation on reference steps

Reference records now include origin/target rates and sourceRangeBasis directly alongside numeric offsets. Mixed-rate and unknown-target offsets are labeled unconverted-offsets; a unique equal-rate target uses equal-rate-offsets. This prevents requiring callers to infer coordinate interpretation from a later stopping diagnostic. Existing fields remain available; no mixed-rate conversion or terminal-media qualification is claimed. Build, 13 focused tests and the real 24-step Sonoma MCP trace passed (`source-resolution-mcp-5574b140-8579-4968-a70e-0704b2d33318`).

### Validated alias fallback for media operations

Shared source validation used by library artifacts, inventory reports and thumbnail strips now tries authorized indexed aliases when an existing original path fails the indexed checksum. It selects only a matching copy and updates the operation's source path; if all readable copies differ, it preserves the source-changed refusal. This fixes valid reconnected media being ignored merely because stale content still occupies the original path. Ordinary cached metadata inspection remains unchanged.

Build, eight focused library tests and all 488 TypeScript tests across 83 files passed (`.avid-mcp-analysis/test-source-alias-fallback.log`). The new synthetic alias test preserves both files, reports the matching alias, and rejects after that copy also changes. The normal Sonoma MCP report/capability workflow passed again with source unchanged (`inventory-report-edee143f-4ebb-4883-ab7e-7fd474ddb8b5/evidence.json`). This does not extend alias fallback to every other analysis subsystem or prove arbitrary concurrent relinking.

### MCP alias reconnect with Sonoma-derived media

Added and ran `scripts/research/qualify-alias-reconnect.mjs`. It derives a one-second video-only fixture from the Sonoma preview, indexes two identical disposable paths, closes MCP, changes the original fixture and reconnects. Report generation selects the matching alias; copy output matches the indexed digest and thumbnail extraction succeeds. After changing the alias too, report generation is refused with the source-changed reason, preserving the prior HTML and directory inventory. The actual Sonoma preview checksum remains unchanged. Evidence: `.avid-mcp-analysis/alias-reconnect-61b51221-26d7-4e40-b3a9-470aa1719c3b/evidence.json`. This qualifies these three library operations across reconnect; other analysis subsystems, concurrent relinking and full media fidelity remain separate.

### QC alias recovery

QC generation and saved QC report access now use shared checksum-validating metadata lookup, allowing a matching authorized alias when the original indexed path contains changed bytes. Existing pre/post QC content checks remain. Build and 19 focused library/QC/report tests passed. The extended real MCP alias harness generated video-only QC over [0,0.5), reconnected again and read the saved report from the matching alias. After both disposable copies changed, saved QC reads and inventory generation were refused. Sonoma remained unchanged. Evidence: `.avid-mcp-analysis/alias-reconnect-1d1eec68-624f-483a-8546-b36961e04660/evidence.json`. An initial one-second QC request correctly failed because the encoded fixture was slightly shorter; the final range is explicitly half a second. Other analysis subsystems and concurrent relinking remain open.

### Shot detection alias recovery

Shot detection now uses checksum-validated metadata lookup to choose a matching authorized alias before decoding. Existing video/range validation, complete frame-summary requirements and post-decode checksum verification remain. Build and 11 library/shot tests passed. The real MCP alias harness decoded [0,0.5) from the matching Sonoma-derived copy and required a positive frame count; after both copies changed, a new shot run was refused while preserving the previous shot report and directory contents. Evidence: `.avid-mcp-analysis/alias-reconnect-5759fc1c-cb06-49a4-a1e1-de221ad348dd/evidence.json`. This qualifies reconnect/source selection and decode behavior, not cut-detection accuracy or arbitrary concurrent source replacement.

### Speech and checkpoint alias recovery

Transcription, language detection and speech checkpoint reads now use checksum-validating alias lookup. Build and 15 focused speech/language tests passed, including interrupted-token resume through an alias after the original source changed and refusal once the alias also changed. The extended MCP harness retains audio in its one-second Sonoma-derived fixture, runs local tiny.en transcription over [0,0.5), reconnects and reads the completed speech checkpoint. It rejects checkpoint reads after both disposable copies change. Existing report/copy/thumbnail/shot/QC checks also passed, with the original Sonoma checksum unchanged. Evidence: `.avid-mcp-analysis/alias-reconnect-d964c454-4f07-481e-b6f0-846b848ed6ef/evidence.json`. Real speech generation and checkpoint lookup passed; token-resume-through-alias uses injected model output in the focused test, and the short media run does not qualify speech accuracy.

### Visual indexing alias recovery

Uniform visual-index planning, final source verification and resume-time checkpoint validation now use matching authorized aliases. Cached index inspection retains its prior scope-only behavior. Build and eight visual scope/resume tests passed. The real MCP alias harness indexed one frame at 0.25 seconds from the matching copy, reconnected, and found that same frame with the expected media/time and similarity above 0.999. After both copies changed, reference-frame search was refused; source and prior artifact preservation checks passed. Evidence: `.avid-mcp-analysis/alias-reconnect-faba6f75-d505-4b7e-a99e-1a0d4f32f5cf/evidence.json`. This is a same-frame integration test, not a semantic ranking benchmark or new live interrupted-visual-resume qualification. The harness also reran reports/copies/thumbnails/shots/speech/QC checks.

### Full verification after alias recovery

`npm run check` passed on source commit `911298b`: 489 TypeScript tests across 83 files, 32 Python tests, 135-tool stdio/HTTP checks, dry packing and fresh installation with exact definitions, six notice mappings, inventory/source refusal, source tracing, five client formats and Python available/missing/restored isolation. Log: `.avid-mcp-analysis/check-alias-recovery.log`. Current-head CI was still running when checked; CodeQL for `1e448b0` was successful. Caption/speaker/people alias integration and the broader original feature/quality/host acceptance requirements remain open.

### Caption and batch alias recovery

Frame-caption generation/reads and caption-batch source validation now use matching checksum-verified aliases. Build and ten caption/visual-summary tests passed. The actual MCP alias harness generated one local Florence caption at 0.25 seconds, verified completed batch state and identical caption references after reconnect, then rejected batch verification once both disposable copies changed. The full cumulative alias harness also passed, preserving the original Sonoma checksum. Evidence: `.avid-mcp-analysis/alias-reconnect-b89da7ce-6e3b-4f6a-9019-2f5cb0f7d532/evidence.json`; log: `alias-caption-reconnect.log`. This qualifies generation/read/source recovery, not caption factual quality or a new live interrupted batch-resume test.

### People indexing and run alias recovery

People-index planning and saved-run validation now resolve a matching authorized alias. Frame extraction and face-analysis postchecks remain tied to the selected source; cached collection inspection keeps its prior semantics. Build and 14 people/index-run tests passed. The real MCP alias harness indexed one frame with the installed face runtime, reconnected and verified completed state with one analyzed frame, then rejected run verification after both disposable copies changed. Other cumulative alias checks passed and Sonoma remained unchanged. Evidence: `.avid-mcp-analysis/alias-reconnect-883f4280-ccde-4ccb-bf1b-e4940e4d6492/evidence.json`. This proves the recovery path, not face recognition accuracy, dense coverage or a new live interrupted-resume case.

### Speaker analysis alias recovery

Speaker analysis source validation now selects checksum-matching authorized aliases for generation, saved analysis reads and operations that validate those records. Build and 30 diarization/edit/cleanup/alignment tests passed. The real MCP alias harness generated analysis over [0,0.5), reconnected and read the saved analysis, then required source-changed refusal after both disposable copies changed. All cumulative alias checks passed and the original Sonoma source remained unchanged. Evidence: `.avid-mcp-analysis/alias-reconnect-842393b6-4206-458c-8b4c-af2066cff0ad/evidence.json`; log: `alias-speaker-reconnect.log`. This short fixture verifies runtime/source recovery, not speaker-count accuracy or independently labeled diarization quality.

### Alias access-scope qualification

Added direct and directory-link boundary tests: a checksum-matching alias outside allowed roots cannot satisfy validated lookup, including an in-root directory link resolving outside. A later authorized matching copy remains usable without exposing the private filename in reports. All ten library tests passed. The MCP harness now indexes its original fixture in a nested root, reconnects with only that root authorized after the original changes, and requires report refusal despite the healthy alias elsewhere. Restoring the original broader fixture root restores the cumulative media/model workflow. Real MCP qualification passed with prior report and Sonoma source unchanged: `.avid-mcp-analysis/alias-reconnect-6c17cd28-5d2d-4a99-8aa1-44dc1c548d3a/evidence.json`. This covers configured root boundaries and resolved directory links, not arbitrary filesystem replacement races.

### Full regression after caption, people and speaker alias integration

Full `npm run check` passed on source commit `e1aa3f6`: 491 TypeScript tests across 83 files, 32 Python tests, 135-tool stdio/HTTP checks, dry packing and fresh installation including exact definitions, six notice mappings, inventory/source refusal, source tracing, five client formats and Python available/missing/restored isolation. Log: `.avid-mcp-analysis/check-all-model-aliases.log`. The cumulative real MCP/model alias and narrowed-root evidence remains separate in `alias-reconnect-6c17cd28-5d2d-4a99-8aa1-44dc1c548d3a`. Current-head CI was pending when checked; CodeQL for `87570a2` was successful. Native editing, broader accuracy, client installation and all other open ledger requirements remain incomplete.

### Named-client model invocation preflight

Rechecked installed Windows Claude Code 2.1.260 at `C:/Users/kavyr/AppData/Roaming/Claude/claude-code/2.1.260/claude.exe`. Its `auth status --json` command reported loggedIn:false, authMethod:none and apiProvider:firstParty. Only these nonpersonal status fields were inspected in the visible result; no authentication or configuration was changed. The separate `.local/bin/claude.exe` resolves to older version 2.1.37, so future qualification must keep the intended binary explicit.

This is a current external dependency for model-driven tool invocation: sign-in to the intended client is required before a real model can select and invoke Avid MCP tools. Prior isolated CLI MCP connection/lifecycle evidence remains valid within its stated scope and does not close model invocation. No model request was attempted, and the overall implementation goal remains open with other actionable work.

### Cross-platform alias assertion correction

CI for 20007a4 exposed a test expectation mismatch: validatedMetadata returns canonical paths, while temporary paths can use macOS /var aliases or Windows short user-directory names. The allowed-alias assertion now compares against filesystem realpath, retaining direct and directory-link scope refusal and unchanged external-file checks. All 10 focused library tests passed locally. Cross-platform CI remains a separate gate until the pushed revision completes.


### Collection export alias recovery

OTIO collection export now selects checksum-verified in-scope aliases before authoring external media references. Cached collection reads retain their existing semantics. Eleven library tests and typecheck passed. Actual stdio MCP indexing/save/reconnect/export against an owned one-second Sonoma derivative selected the matching copy after the original changed, retained the exact 15-frame range and source checksum, and refused export without changing prior output after both copies changed. Original Sonoma bytes remained unchanged. Evidence: .avid-mcp-analysis/collection-alias-f0fdbbac-bebd-46ba-a8ea-688af2ac9b6f/evidence.json; harness: scripts/research/qualify-collection-alias.mjs. This does not qualify native OTIO import or concurrent file-replacement races.


### Full regression after collection alias export

Source 272c056 passed npm run check: 492 TypeScript tests across 83 files, 32 Python tests, 135-tool stdio/HTTP checks, dry packing and fresh-tarball installation. Installed tool definitions matched the checkout; five client formats, model notices, inventory reporting, source tracing and Python isolation/recovery passed. Log: .avid-mcp-analysis/check-collection-alias.log.

CI run 34018448921 for 34c36f3 completed successfully across Windows/macOS and Node 20/24 plus landing, confirming the canonical-path test correction. CI for 272c056 was still running at this checkpoint; local validation is not substituted for that result. Native/client/quality and remaining full-scope acceptance stay open.


### Saved collection discovery

avid_list_collections now rediscovers saved local selects after reconnecting with UUID-ordered pagination, bounded scanning and current media-scope validation. Invalid or inaccessible records contribute only an omitted count; a page can be empty with a continuation cursor. Cached discovery does not assert source-byte identity; export retains checksum verification. Tests cover damaged-record continuation, fresh-instance recovery, scope omission and invalid pagination. Actual stdio MCP rediscovered the Sonoma derivative collection and then exported it through the verified alias; evidence: .avid-mcp-analysis/collection-alias-7381cfa2-efea-400e-8203-c9467e44a492/evidence.json. The selects workflow skill documents continuation. Tool inventory is now 136.

CI run 34018539471 for 272c056 completed successfully. The first discovery package probe stopped on the stale 135-tool assertion; smoke/server expectations were updated for the added tool before rerunning the full check.


Full check for collection discovery passed: 493 TypeScript tests, 32 Python tests, 136-tool transport checks and fresh-package installation with exact tool definitions and Python isolation/recovery. Log: .avid-mcp-analysis/check-collection-discovery.log. This proves package compatibility and the bounded collection workflow, not general native editing or AI-client model invocation.


### Installed collection recovery acceptance

Fresh-tarball smoke now invokes collection discovery through the installed stdio server, continues past an invalid record, discovers and reads the next valid revision with exact saved ranges, then confirms omitted-only results when the synthetic indexed source points outside the generated client's allowed root. Fixtures use declared synthetic metadata; this is installed workflow/scope evidence, not native media interpretation. The 136-tool package check passed with exact checkout definitions, five generated client formats and Python isolation/recovery. Log: .avid-mcp-analysis/package-collection-recovery.log. Sonoma reconnect/export evidence remains separately recorded above.


### Complete-file collection publication

Collection save and OTIO export now stage bytes before exclusive hard-link publication, matching the existing local immutable-output pattern. Paused-write tests confirm discovery cannot see partial collection files and no final OTIO exists before completion. Injected write failures leave prior directory entries unchanged; a competing final file is preserved and the caller's stage removed. Seventeen focused tests and typecheck passed. Actual Sonoma derivative MCP discovery/reconnect/export and changed-source refusal passed again: .avid-mcp-analysis/collection-alias-54fb2aa3-28b6-4b46-8a93-432953c36ab6/evidence.json. These checks do not establish power-loss durability or termination at every publication phase. CI for discovery commit a2d90c7 completed successfully.


### Native UI edit fixture preparation

Reacquired the live Avid UI and recorded accessibility/menu constraints; created a distinct disposable sequence through MCP, saved/reopened it and preserved its baseline AVB. See NATIVE_UI_EDIT_BASELINE.md for exact identity and evidence. Trimming/undo remain untested. CI 34018970601 for 902e3d2 failed in the existing Windows listener-ownership test when PowerShell exceeded 30 seconds; CodeQL passed. This failure remains unresolved and is not replaced by native fixture success.


### Native baseline graph and ownership timeout follow-up

The UI-test copy now has independent AVB graph comparison evidence: sequence semantics and four reachable source records equal the original, with one shared unresolved anchor. Current saved-bin bytes equal the preserved baseline. See NATIVE_UI_EDIT_BASELINE.md. The focused Windows loopback-owner test passed unchanged locally in 14.51 seconds (log: .avid-mcp-analysis/loopback-owner-recheck.log). One failed-job rerun of CI 34018970601 was started on the same 902e3d2 commit; attempt 2 remained running at this checkpoint. No timeout increase or ownership-verification bypass was introduced; the cause of the earlier 30-second PowerShell timeout remains unproven.


### Observed UI trim and undo

One dual-roller one-frame trim was performed on the owned copy through computer use, saved, independently decoded, undone and saved again. All three media tracks changed from cut 60 to 61 and incoming source start 3300 to 3301; complete decoded sequence semantics returned to baseline after undo, with other decoded mobs unchanged. See NATIVE_UI_TRIM_QUALIFICATION.md and scripts/research/verify-native-ui-trim.mjs. No shipping UI adapter, post-edit close/reopen or general trim/undo support is claimed. CI 34018970601 attempt 2 passed on unchanged 902e3d2; the first PowerShell ownership-query timeout remains unexplained.


### Native UI undo persistence

Guarded MCP close/reopen of the trimmed-then-undone disposable bin passed after checking its current checksum against the retained undo artifact. The expected single sequence remained and every decoded AVB mob equaled the pre-edit baseline after reopening. Evidence: .avid-mcp-analysis/native-ui-undo-reopen-05c6a9f4-3c68-4b6f-900a-697bc4d1dc52/evidence.json; script: scripts/research/qualify-native-ui-undo-reopen.mjs. Undo-history persistence, redo, intermediate-trim reopening and the shipping UI adapter remain open. CI and CodeQL for 7c1660a completed successfully; later heads have separate checks.


### Reusable saved trim verifier

src/native/trim-verifier.ts now checks exact one-frame dual-roller transformations for explicitly selected picture/sound track ordinals against complete, warning-free decoded graphs. It preserves and compares every decoded mob field, rejects ambiguous identities/tracks, overlaps, opaque/combiner components, empty clips, unresolved or mixed-rate direct sources and unrelated edits. Five focused tests and build passed; the verifier also passed against the captured real Avid trim and inverse undo graphs. It does not execute commands, authenticate input files, prove available media handles or qualify general timeline editing. The UI adapter remains unfinished.


### Full regression and installed trim verification

Full npm run check passed after adding installed-package trim-verifier execution: 503 TypeScript tests, 32 Python tests, 136 tools, stdio/HTTP checks, dry packing and fresh-tarball installation. The installed verifier accepted synthetic forward/inverse transforms and refused an unrelated name change; exact tool definitions, collection discovery, five client formats and Python isolation/recovery also passed. Log: .avid-mcp-analysis/check-trim-verifier.log. Actual Avid trim/undo evidence remains separate; this package check does not execute editor commands or close the UI adapter requirement.


### Redo history after reopen: negative result

After reloading Copy.05 and focusing the timeline, the Edit menu showed Undo, Redo and Undo-Redo List disabled. No history action was invoked, and the saved bin checksum still matched reopened.avb. The earlier redo action is unavailable in this tested close/reopen context. Adapter recovery must invalidate assumed UI history on close/reload/restart and inspect saved state; same-session redo remains untested. See NATIVE_UI_TRIM_QUALIFICATION.md.


### Same-session redo verified

A fresh controlled UI trim/save/undo/save/redo/save/undo/save cycle on the disposable sequence passed saved-graph verification. Redo exactly reproduced every decoded mob of the trimmed state; both undo results matched every baseline mob. Production trim verification passed in both directions. Evidence: .avid-mcp-analysis/native-ui-redo-20260906/verification.json; capture and verification scripts are under scripts/research. The saved fixture is restored with a new binary save hash. No bin closure occurred inside this cycle; the prior negative cross-close history result remains valid. Shipping UI execution and broader edits remain open.


### MCP saved trim verification

avid_verify_saved_trim exposes normalized selected-bin snapshot comparison through MCP with explicit revisions, bin paths, mob identity, cut, direction and track ordinals. Existing snapshot reads enforce current path scope. Actual stdio snapshot capture from the retained Avid baseline/trim bins passed verification; a deliberately incomplete V1-only expectation was refused. Evidence: .avid-mcp-analysis/saved-trim-mcp-0d7379ba-0c81-474e-bf2e-bd61d85d4dd0/evidence.json. Full check passed: 504 TypeScript tests, 32 Python tests, 137 tools and fresh-package exact definitions/Python recovery. Log: .avid-mcp-analysis/check-saved-trim-tool.log. This adds usable verification, not editor command execution; the full UI adapter remains open.


### Installed saved-trim MCP acceptance

Fresh-package smoke now invokes avid_verify_saved_trim through the installed stdio server using synthetic captured snapshot records. It verifies the expected cut and baseline/candidate revision identities, then requires a wrong-direction request to fail with an exact-trim mismatch. The 137-tool package check passed with full definition equality, client configuration checks and Python isolation/recovery. Log: .avid-mcp-analysis/package-saved-trim-mcp.log. This is installed API coverage; retained real Avid trim captures and current editor execution remain distinct evidence.


### Packaged verified trim workflow

The avid-selects skill now guides snapshot-before/edit/save/snapshot-after/verify and verified same-session recovery. It distinguishes record/source viewers, frame/track coordinates, saved-state verification versus command execution, and invalidation of assumed history on close/reload/restart. A separate computer-use executor remains required. All 39 referenced MCP tools were found, and fresh-package validation passed for 137 tools and five skills, including installed saved-trim verification and Python isolation/recovery. Log: .avid-mcp-analysis/package-trim-workflow.log. Named-client execution of this guidance remains unqualified.


### Accepted provider TCP ownership foundation

Added verifyWindowsLoopbackConnection for the exact server-side established loopback tuple, retaining binary/hash/PID-start checks. Two real Windows test files passed: accepted ownership with zero received application bytes, wrong-identity and closed-tuple refusal, and the existing listener/pairing/dispatch workflow. Typecheck passed. Log: .avid-mcp-analysis/loopback-connection.log. This is not yet the HTTP dispatch gate; credentials still use the existing listener-preflight request path, so connection-race mitigation remains open.


### Paired HTTP accepted-connection gate

Paired Jumper requests now retain a fresh socket and release it to HTTP only after accepted-tuple executable/hash/PID-start verification. Failure, closure and timeout destroy the socket; late approval cannot send buffered credentials/body. Three transport tests observed byte withholding, refusal/timeout behavior and bounded response failures. Actual Windows pairing and MCP dispatch tests passed. Full npm run check passed: 508 TypeScript tests, 32 Python tests, 137 tools, five skills, both transports and fresh installation. Log: .avid-mcp-analysis/check-provider-connection-gate.log. Separate fresh-installed paired MCP search passed with source/archive/entry hashes preserved: .avid-mcp-analysis/installed-provider-51670163-45b3-4561-bdc1-77336d5d7e1d/evidence.json. This closes this dispatch integration step, not licensed-provider or runtime-version qualification. OS tuple ownership is not cryptographic vendor authentication or loaded-memory verification.

### Provider connection recovery and IPv6

The transport fixture now closes the accepted socket while verification is pending, starts a replacement listener on the same port, and grants late approval. The original request fails; neither listener receives HTTP bytes and no automatic replacement connection occurs. A new explicit request succeeds. All four transport tests passed. Actual Windows accepted-tuple checks now cover both 127.0.0.1 and ::1, including stale-identity and closed-tuple refusal; both passed. The current built adapter also passed the separate child-process restart harness: stale pairing refused the replacement with zero requests, fresh pairing restored health, both owned child processes stopped. Evidence: .avid-mcp-analysis/provider-restart-fd280f3d-8140-48c1-a651-93d30066db92/evidence.json. This extends failure/recovery coverage; licensed-provider testing remains open. Production code is unchanged from the preceding fully checked commit.

### Optional provider reference image/frame search

Refreshed the public Jumper OpenAPI on 2026-09-06 and implemented image/frame operations on avid_jumper_read. Both resolve authorized reference/cache/selected-media paths before dispatch, set search_all false, accept optional text refinement and use the shared bounded metadata-only visual response validator. Frame time is required, finite and nonnegative; incompatible operation fields and out-of-scope references are refused. Contract fixtures verify exact endpoint/body/header behavior; actual Windows paired MCP tests cover both operations. Full check passed: 515 TypeScript tests, 32 Python tests, 137 tools and fresh-package definition equality/recovery (log: .avid-mcp-analysis/check-provider-reference-search.log). Fresh-installed paired text/image/frame MCP calls also passed with fixture/archive/entry hashes preserved: .avid-mcp-analysis/installed-provider-1357c1de-fa15-44b5-9583-59486ef332c4/evidence.json. These fixtures do not decode reference media or establish licensed-provider ranking/timestamp/runtime compatibility. Other provider operations and broad core parity remain open.

### Selects source-graph preservation and output binding

The AAF selects builder now compares every preserved source mob's stored properties and reachable weak definitions against the template after authoring, excluding only the newly created composition. It binds conformance to an output SHA-256, rechecks template/output after final inspection, and the TypeScript boundary refuses bytes changed after Python verification instead of computing a new hash and blessing them. Python negative tests inject source-name/definition changes and a late output change; TypeScript boundary tests cover intact, changed-output and changed-template results. Full check passed with 515 TypeScript tests, 34 Python tests, 137 tools and fresh-package isolation/recovery; three additional TypeScript boundary tests passed separately after addition. Log: .avid-mcp-analysis/check-aaf-build-preservation.log. Real preview/slideshow multi-source merge/build/inspect through MCP passed with preserved-source verification: .avid-mcp-analysis/aaf-merge-mcp-a521a385-0bf0-436c-87cb-eb0ac5f65646/evidence.json. This is source-reference/conformance evidence; it does not add native import, mixed-rate, implicit-schema, playback or color acceptance.

### Installed AAF authoring acceptance

Python-enabled fresh-package smoke now creates two synthetic external-reference AAFs, then calls the installed MCP server from the fixture directory to merge references, build picture/stereo selects, re-inspect exact source identities/ranges and reject a stale template checksum. It requires sourceGraphVerified and conformanceVerified, checks the returned output hash and preserves both original AAFs, merged template and final output. Package smoke passed with 137 tools, five skills, exact definition equality and existing Python isolation/recovery. Log: .avid-mcp-analysis/package-aaf-preservation.log. The fixture media are locator placeholders, not decoded footage; this adds installed authoring/sidecar coverage, not native Avid or playback evidence. The check runs in the existing Python-enabled CI package job.

### Fractional-rate source preparation

Added and ran a reproducible real-MCP rate qualification over owned short Sonoma derivatives at 24000/1001, 25 and 30000/1001 fps. All passed source-clock preparation with original rate, video essence and packet-clock preservation plus normalized PCM verification. Independent output decoding counted 48/50/60 frames, matching output metadata. Source/output hashes passed and the original Sonoma MP4 was unchanged. Evidence: .avid-mcp-analysis/source-clock-rates-04d3b77a-2741-4124-af09-cac883927dcd/evidence.json. No production code changed. This closes short-file preparation evidence for these rates; it does not close mixed-rate timelines, native fractional-rate imports, retimes, color or perceptual sync acceptance.

### Observed backward trim and undo

Computer use performed a one-frame backward dual-roller trim/save/undo/save on the checksum-verified disposable Copy.05 sequence. The saved cut moved 60 to 59 across V1/A1/A2; the production verifier accepted the requested transformation and its inverse, and every decoded restored mob matched baseline. The sequence was left restored and outside trim mode. Evidence: .avid-mcp-analysis/native-ui-backward-20260906/verification.json. Restored bin SHA is now 8b8ccefa6225a38acc6aae30be05d05b469c14b8758afc12bdd80494df785822. This adds actual negative-direction host evidence; shipping UI execution, general edits and playback remain open. CI and CodeQL for d8201f1 passed; later CI runs were still pending/in progress at inspection.

### Local visual unwanted-concept refinement

Core avid_search_visual and avid_search_visual_frame now accept bounded refinement.exclude concepts and a 0–1 weight. Each sample retains eligibility and receives positive/reference cosine minus weight times maximum nonnegative excluded-concept cosine, with component scores exposed. Defaults remain unchanged; duplicates do not amplify penalties, and all text is token-validated without truncation before embedding inference. Unit tests verify reordering, retained sample count, zero-weight equivalence and invalid/overlong inputs. Actual local CLIP/MCP arithmetic passed for all 32 Sonoma samples; zero weight reproduced the base ranking. Evidence: .avid-mcp-analysis/visual-exclusions-4297bd37-f367-4306-8ad8-374dc8c365e1/evidence.json. The existing default benchmark also reproduced every previous positive and absent-scene rank/score exactly: visual-ranking-83e0f5f3-b2b9-4fee-aa85-d78671d2b25e. Full check passed: 519 TypeScript tests, 34 Python tests, 137 tools, five skills, transports and fresh installation including AAF authoring. Log: .avid-mcp-analysis/check-visual-exclusions.log. This is a soft ranking control, not an absence guarantee or independently demonstrated semantic-quality improvement. Source MP4 unchanged; broad accuracy acceptance remains open.

### Local combined image/frame and text search

Core visual search now accepts an image plus optional text, and source-frame search accepts optional text refinement. The base score is an explicit equal-weight image/text cosine mean; results expose both components, and unwanted-concept penalties apply afterward. Existing single-input queries preserve their scores. Focused tests verify changed ordering, component scores, frame dispatch and overlong-text refusal before embedding inference. Actual CLIP/MCP image and extracted-frame queries passed independent component arithmetic across 32 Sonoma samples, with a six-sample scoped/exclusion check: .avid-mcp-analysis/visual-combined-78392283-2964-43aa-aabe-ba5a2713ef55/evidence.json. The default benchmark exactly matched prior positive and absent-scene rankings/scores: visual-ranking-55f6b273-98ec-48de-a3df-6542fa109b11. Full check passed: 520 TypeScript tests, 34 Python tests, 137 tools, five skills, both transports and fresh-package AAF/isolation/recovery. Log: .avid-mcp-analysis/check-visual-combined.log. Source and supplied reference unchanged. This adds local multimodal query control, not independently established ranking accuracy or identity/absence guarantees.

### Frame query validation before extraction

Frame searches now validate time, limit, scope and all refinement/exclusion token lengths before creating the reference thumbnail. Previously an overlong model query could be rejected only after extraction. Shared token validation preserves the existing 77-token/no-truncation behavior. Typecheck and 11 focused visual tests passed; tests require no artifact or embedding inference for rejected frame queries. The real local CLIP/MCP combined-query harness still passed all score checks and rejected overlong positive/exclusion frame queries with the library's recursive file inventory unchanged. Evidence: .avid-mcp-analysis/visual-combined-69754d86-d6a6-4023-8da1-3312e8ca87cd/evidence.json. Source/reference hashes remained unchanged. No broad semantic-quality claim is added.

### Saved visual source-content integrity

Saved visual sample/search/frame reads now validate each distinct source content hash, using the shared authorized alias resolver instead of only checking path access. Replaced source bytes are refused before inference or reference extraction; matching aliases recover existing results without reindexing embeddings. Unit tests cover all three read paths, alias recovery, narrowed-root refusal and both copies changed. Actual generated-file MCP/model workflow passed the same refusal/recovery sequence and reproduced identical search output: .avid-mcp-analysis/visual-scope-e44daeaa-fe01-49d9-b0f4-407f2b3f4d57/evidence.json. Full check passed with 522 TypeScript tests, 34 Python tests, 137 tools, five skills, both transports and fresh-package AAF/isolation/recovery. Log: .avid-mcp-analysis/check-visual-source-integrity.log. Hashing adds sequential source I/O to cached reads; arbitrary concurrent writers and index/thumbnail integrity remain separate requirements.

### Visual thumbnail-to-embedding integrity

New visual indexes use schema version 2 and retain each image SHA-256 from its embedding checkpoint. Returned sample/search thumbnails are checked against those hashes; changed files fail instead of displaying an image unrelated to the embedding. Results label checked thumbnails sha256_verified and legacy records unverified_legacy. Missing hashes in version 2 are refused; checkpoint comparison supports both formats. Unit tests cover changed thumbnails, restoration, invalid v2 records and legacy compatibility. Actual generated-file MCP testing refused altered thumbnails in search/sample reads, restored identical results after restoring bytes and retained source-alias recovery behavior: .avid-mcp-analysis/visual-scope-a6b85ff6-2363-4c21-84ca-6923221a010c/evidence.json. Full check passed: 523 TypeScript tests, 34 Python tests, 137 tools, five skills, transports and fresh-package AAF/isolation/recovery. Log: .avid-mcp-analysis/check-visual-thumbnail-integrity.log. This is recorded-hash consistency for returned images, not cryptographic index authenticity or protection from concurrent changes after checks.

### Packaged visual refinement and integrity workflow

The avid-selects skill now guides combined reference/text search, bounded unwanted-concept penalties, comparison with baseline rankings, source-content alias recovery and verified/legacy thumbnail indicators. It explicitly avoids equating scores with relevance or exclusion guarantees and prohibits bypassing mismatches by editing cache records. Both observed one-frame trim directions are included. Fresh-package validation passed for 137 tools and five skills, exact definitions, tool references, Python isolation/recovery and installed AAF authoring. Log: .avid-mcp-analysis/package-selects-visual-workflow.log. CI/CodeQL for source-content integrity commit 19c4173 passed; thumbnail-integrity CI was still in progress. Named-client application of these instructions remains unqualified.
